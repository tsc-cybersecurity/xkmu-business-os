# Portal-P3 Implementation Plan

> **Für Agenten:** REQUIRED SUB-SKILL — superpowers:subagent-driven-development.

**Goal:** Portal-User sehen read-only Verträge + Projekte ihrer Firma.

**Spec:** `docs/superpowers/specs/2026-04-23-portal-p3-design.md`

---

## Task P3-1: Portal-API Contracts (list + detail)

**Files:**
- Create: `src/app/api/v1/portal/me/contracts/route.ts`
- Create: `src/app/api/v1/portal/me/contracts/[id]/route.ts`

- [ ] **Step 1: List-Route**

```ts
// src/app/api/v1/portal/me/contracts/route.ts
import { NextRequest } from 'next/server'
import { apiSuccess, apiError } from '@/lib/utils/api-response'
import { withPortalAuth } from '@/lib/auth/with-portal-auth'
import { db } from '@/lib/db'
import { documents } from '@/lib/db/schema'
import { eq, and, desc, sql } from 'drizzle-orm'
import { logger } from '@/lib/utils/logger'

export async function GET(request: NextRequest) {
  return withPortalAuth(request, async (auth) => {
    try {
      const rows = await db
        .select({
          id: documents.id,
          number: documents.number,
          status: documents.status,
          contractStartDate: documents.contractStartDate,
          contractEndDate: documents.contractEndDate,
          contractRenewalType: documents.contractRenewalType,
          contractRenewalPeriod: documents.contractRenewalPeriod,
          contractNoticePeriodDays: documents.contractNoticePeriodDays,
          subtotal: documents.subtotal,
          taxTotal: documents.taxTotal,
          total: documents.total,
          createdAt: documents.createdAt,
        })
        .from(documents)
        .where(and(
          eq(documents.type, 'contract'),
          eq(documents.companyId, auth.companyId),
        ))
        .orderBy(sql`${documents.contractStartDate} DESC NULLS LAST`, desc(documents.createdAt))
      return apiSuccess(rows)
    } catch (error) {
      logger.error('Failed to list portal contracts', error, { module: 'PortalContractsAPI' })
      return apiError('LIST_FAILED', 'Verträge konnten nicht geladen werden', 500)
    }
  })
}
```

- [ ] **Step 2: Detail-Route (mit server-seitiger HTML-Sanitization)**

Der Vertragstext (`contract_body_html`) wird im Portal gerendert. **Sanitize den HTML-String server-seitig** mit dem existierenden `sanitizeHtml`-Helper (`src/lib/utils/sanitize.ts`, nutzt isomorphic-dompurify) bevor er in die Response geht. Damit ist der Client-Code sicher — die JSON-Response enthält bereits vertrauenswürdiges HTML.

```ts
// src/app/api/v1/portal/me/contracts/[id]/route.ts
import { NextRequest } from 'next/server'
import { apiSuccess, apiError, apiNotFound } from '@/lib/utils/api-response'
import { withPortalAuth } from '@/lib/auth/with-portal-auth'
import { db } from '@/lib/db'
import { documents, documentItems } from '@/lib/db/schema'
import { eq, and, asc } from 'drizzle-orm'
import { sanitizeHtml } from '@/lib/utils/sanitize'
import { logger } from '@/lib/utils/logger'

type Params = Promise<{ id: string }>

export async function GET(request: NextRequest, { params }: { params: Params }) {
  return withPortalAuth(request, async (auth) => {
    const { id } = await params
    try {
      const [contract] = await db
        .select({
          id: documents.id,
          number: documents.number,
          status: documents.status,
          contractStartDate: documents.contractStartDate,
          contractEndDate: documents.contractEndDate,
          contractRenewalType: documents.contractRenewalType,
          contractRenewalPeriod: documents.contractRenewalPeriod,
          contractNoticePeriodDays: documents.contractNoticePeriodDays,
          subtotal: documents.subtotal,
          taxTotal: documents.taxTotal,
          total: documents.total,
          notes: documents.notes,
          paymentTerms: documents.paymentTerms,
          contractBodyHtml: documents.contractBodyHtml,
          createdAt: documents.createdAt,
        })
        .from(documents)
        .where(and(
          eq(documents.id, id),
          eq(documents.type, 'contract'),
          eq(documents.companyId, auth.companyId),
        ))
        .limit(1)

      if (!contract) return apiNotFound('Vertrag nicht gefunden')

      const items = await db
        .select({
          id: documentItems.id,
          position: documentItems.position,
          name: documentItems.name,
          description: documentItems.description,
          quantity: documentItems.quantity,
          unit: documentItems.unit,
          unitPrice: documentItems.unitPrice,
          vatRate: documentItems.vatRate,
          lineTotal: documentItems.lineTotal,
        })
        .from(documentItems)
        .where(eq(documentItems.documentId, id))
        .orderBy(asc(documentItems.position))

      // Server-side HTML sanitization (DOMPurify via isomorphic-dompurify)
      const safeBodyHtml = contract.contractBodyHtml
        ? sanitizeHtml(contract.contractBodyHtml)
        : null

      return apiSuccess({
        ...contract,
        contractBodyHtml: safeBodyHtml,
        items,
      })
    } catch (error) {
      logger.error('Failed to load portal contract detail', error, { module: 'PortalContractsAPI' })
      return apiError('LOAD_FAILED', 'Vertrag konnte nicht geladen werden', 500)
    }
  })
}
```

- [ ] **Step 3: Typecheck + Commit**

```bash
npx tsc --noEmit 2>&1 | grep "portal/me/contracts" | head -5
git add "src/app/api/v1/portal/me/contracts/route.ts" "src/app/api/v1/portal/me/contracts/[id]/route.ts"
git commit -m "feat(portal): portal APIs for read-only contracts (list + detail, sanitized HTML)"
```

---

## Task P3-2: Portal-API Projects (list + detail)

**Files:**
- Create: `src/app/api/v1/portal/me/projects/route.ts`
- Create: `src/app/api/v1/portal/me/projects/[id]/route.ts`

- [ ] **Step 1: List mit Task-Count**

```ts
// src/app/api/v1/portal/me/projects/route.ts
import { NextRequest } from 'next/server'
import { apiSuccess, apiError } from '@/lib/utils/api-response'
import { withPortalAuth } from '@/lib/auth/with-portal-auth'
import { db } from '@/lib/db'
import { projects, projectTasks } from '@/lib/db/schema'
import { eq, and, ne, desc, count } from 'drizzle-orm'
import { logger } from '@/lib/utils/logger'

export async function GET(request: NextRequest) {
  return withPortalAuth(request, async (auth) => {
    try {
      const rows = await db
        .select({
          id: projects.id,
          name: projects.name,
          description: projects.description,
          status: projects.status,
          priority: projects.priority,
          projectType: projects.projectType,
          startDate: projects.startDate,
          endDate: projects.endDate,
          tags: projects.tags,
          color: projects.color,
          createdAt: projects.createdAt,
        })
        .from(projects)
        .where(and(
          eq(projects.companyId, auth.companyId),
          ne(projects.status, 'archived'),
        ))
        .orderBy(desc(projects.createdAt))

      const counts = await db
        .select({ projectId: projectTasks.projectId, c: count() })
        .from(projectTasks)
        .groupBy(projectTasks.projectId)
      const countMap = new Map(counts.map(r => [r.projectId, Number(r.c)]))

      return apiSuccess(rows.map(r => ({ ...r, taskCount: countMap.get(r.id) ?? 0 })))
    } catch (error) {
      logger.error('Failed to list portal projects', error, { module: 'PortalProjectsAPI' })
      return apiError('LIST_FAILED', 'Projekte konnten nicht geladen werden', 500)
    }
  })
}
```

- [ ] **Step 2: Detail mit anonymisierten Tasks**

```ts
// src/app/api/v1/portal/me/projects/[id]/route.ts
import { NextRequest } from 'next/server'
import { apiSuccess, apiError, apiNotFound } from '@/lib/utils/api-response'
import { withPortalAuth } from '@/lib/auth/with-portal-auth'
import { db } from '@/lib/db'
import { projects, projectTasks } from '@/lib/db/schema'
import { eq, and, ne, asc } from 'drizzle-orm'
import { logger } from '@/lib/utils/logger'

type Params = Promise<{ id: string }>

export async function GET(request: NextRequest, { params }: { params: Params }) {
  return withPortalAuth(request, async (auth) => {
    const { id } = await params
    try {
      const [project] = await db
        .select({
          id: projects.id,
          name: projects.name,
          description: projects.description,
          status: projects.status,
          priority: projects.priority,
          projectType: projects.projectType,
          startDate: projects.startDate,
          endDate: projects.endDate,
          tags: projects.tags,
          color: projects.color,
          columns: projects.columns,
          createdAt: projects.createdAt,
        })
        .from(projects)
        .where(and(
          eq(projects.id, id),
          eq(projects.companyId, auth.companyId),
          ne(projects.status, 'archived'),
        ))
        .limit(1)

      if (!project) return apiNotFound('Projekt nicht gefunden')

      const tasksRaw = await db
        .select({
          id: projectTasks.id,
          title: projectTasks.title,
          description: projectTasks.description,
          columnId: projectTasks.columnId,
          position: projectTasks.position,
          priority: projectTasks.priority,
          startDate: projectTasks.startDate,
          dueDate: projectTasks.dueDate,
          completedAt: projectTasks.completedAt,
          labels: projectTasks.labels,
        })
        .from(projectTasks)
        .where(eq(projectTasks.projectId, id))
        .orderBy(asc(projectTasks.columnId), asc(projectTasks.position))

      const tasks = tasksRaw.map(t => ({
        ...t,
        status: t.completedAt ? 'done' : 'open',
      }))

      return apiSuccess({ ...project, tasks })
    } catch (error) {
      logger.error('Failed to load portal project detail', error, { module: 'PortalProjectsAPI' })
      return apiError('LOAD_FAILED', 'Projekt konnte nicht geladen werden', 500)
    }
  })
}
```

- [ ] **Step 3: Typecheck + Commit**

```bash
npx tsc --noEmit 2>&1 | grep "portal/me/projects" | head -5
git add "src/app/api/v1/portal/me/projects/route.ts" "src/app/api/v1/portal/me/projects/[id]/route.ts"
git commit -m "feat(portal): portal APIs for read-only projects with anonymized tasks"
```

---

## Task P3-3: Portal-UI Contracts (Liste + Detail)

**Files:**
- Create: `src/app/portal/contracts/page.tsx`
- Create: `src/app/portal/contracts/[id]/page.tsx`

- [ ] **Step 1: Liste**

Client component. Fetch `/api/v1/portal/me/contracts`. Tabelle mit Nummer, Status-Badge, Zeitraum, Total, Button "Anzeigen".

Status-Label-Map (deutsche Anzeige):
- `'active'` → Badge "Aktiv" (default)
- `'draft'` → Badge "Entwurf" (secondary)
- `'expired'` → Badge "Abgelaufen" (secondary)
- `'cancelled'` → Badge "Gekündigt" (destructive)
- sonstige → Klartext

Empty state: "Es liegen derzeit keine Verträge für Ihre Firma vor."

- [ ] **Step 2: Detail**

Client component. Fetch `/api/v1/portal/me/contracts/[id]`. Layout:
- Header mit Nummer + Status-Badge + Back-Link "← Zurück zur Liste"
- Card "Eckdaten": Start/Ende, Kündigungsfrist (`contractNoticePeriodDays` + `contractRenewalPeriod`), Zahlungsbedingungen, Summen (subtotal/tax/total)
- Card "Positionen": Tabelle mit Name, Menge, Einheit, Einzelpreis, MwSt%, Summe
- Card "Vertragstext": Wenn `contractBodyHtml` vorhanden, als HTML gerendert. HTML kommt **bereits sanitized vom Server** (Step 2 von P3-1 nutzt `sanitizeHtml`). Client verwendet `dangerouslySetInnerHTML={{ __html: contract.contractBodyHtml }}` **NUR** auf diesem vom Server pre-sanitized String — expliziter Kommentar im Code dass Daten server-sanitized sind.

Formatierung: Zahlen mit `.toLocaleString('de-DE', { style: 'currency', currency: 'EUR' })`. Datum mit `.toLocaleDateString('de-DE')`.

- [ ] **Step 3: Commit**

```bash
git add "src/app/portal/contracts/page.tsx" "src/app/portal/contracts/[id]/page.tsx"
git commit -m "feat(portal): portal UI — contracts list + detail view"
```

---

## Task P3-4: Portal-UI Projects (Liste + Detail mit Read-only Kanban)

**Files:**
- Create: `src/app/portal/projects/page.tsx`
- Create: `src/app/portal/projects/[id]/page.tsx`

- [ ] **Step 1: Liste**

Karten-Grid (`grid md:grid-cols-2 lg:grid-cols-3 gap-4`), eine Card pro Projekt. Inhalt: Name, truncated Description, Status-Badge, Prio-Dot (klein, farbig), Task-Count, Zeitraum. Ganze Card klickbar → Detail.

Status-Map:
- `'active'` → Badge "Aktiv" (default)
- `'completed'` → Badge "Abgeschlossen" (default)
- `'on_hold'` → Badge "Pausiert" (secondary)

- [ ] **Step 2: Detail mit Read-only Kanban**

Header + Meta-Card + Kanban-Board.

Kanban:
- Columns-Bar horizontal scrollbar, `flex gap-4 overflow-x-auto`
- Pro Column: Header mit Name + Task-Count, darunter Task-Cards
- Task-Card zeigt: Title, Priority-Dot farbig, Due-Date (falls vorhanden), Labels als kleine Badges, Checkmark-Icon wenn `status==='done'`
- KEINE Drag-Handles, KEINE Edit-Buttons — vollständig read-only

Leeres Projekt: Meldung "Noch keine Aufgaben in diesem Projekt."

Task-Rendering by Column-ID:
```tsx
{(project.columns || []).map((col: any) => {
  const colTasks = tasks.filter(t => t.columnId === col.id)
  // render column with colTasks
})}
```

Tasks mit unbekannter columnId (falls Column gelöscht) → in separaten "Ohne Spalte"-Block am Ende.

- [ ] **Step 3: Commit**

```bash
git add "src/app/portal/projects/page.tsx" "src/app/portal/projects/[id]/page.tsx"
git commit -m "feat(portal): portal UI — projects list + read-only kanban detail"
```

---

## Task P3-5: Dashboard-Kacheln live + Nav-Update

**Files:**
- Modify: `src/app/portal/page.tsx`
- Modify: `src/app/portal/layout.tsx`

- [ ] **Step 1: Dashboard**

Dashboard lädt beim Mount parallelen fetch zu contracts + projects + change-requests. Counts ableiten. Kacheln:

```tsx
<Link href="/portal/contracts">
  <Card className="hover:shadow-md transition-shadow cursor-pointer">
    <CardHeader>
      <CardTitle className="flex items-center gap-2">
        <FileText className="h-5 w-5" />Verträge
      </CardTitle>
    </CardHeader>
    <CardContent>
      <p className="text-2xl font-semibold">{contractCount}</p>
      <p className="text-sm text-muted-foreground">Verträge einsehen</p>
    </CardContent>
  </Card>
</Link>
```

Analog für Projekte. "Aufträge" und "Chat" bleiben disabled mit "kommt in Kürze".

- [ ] **Step 2: Nav in Layout**

In `src/app/portal/layout.tsx` Nav um zwei Links erweitern — Reihenfolge:

```
Firmendaten  ·  Verträge  ·  Projekte  ·  Anträge
```

- [ ] **Step 3: Commit**

```bash
git add "src/app/portal/page.tsx" "src/app/portal/layout.tsx"
git commit -m "feat(portal): activate contracts/projects dashboard tiles + nav links"
```

---

## Task P3-6: Integration-Test

**Files:**
- Create: `src/__tests__/integration-real/portal-readonly-flow.test.ts`

- [ ] **Step 1: Scenario**

Gated by `TEST_DATABASE_URL` (analog zu existierenden integration-real Tests).

Setup:
- Erstelle 2 Firmen (A + B) mit Test-Suffix `ffff`
- Erstelle 1 Portal-User für Firma A (via `UserService.createPortalUser`)
- Erstelle 1 Vertrag (`documents.type='contract'`) für Firma A, 1 für Firma B
- Erstelle 1 Projekt für Firma A (status='active'), 1 für Firma B, 1 archiviertes für A

Tests (gegen Drizzle direkt, da die Filter-Logik in Route-Handlern inline ist):

1. Select `documents WHERE companyId = A AND type = 'contract'` liefert NUR Vertrag A.
2. Select `documents WHERE id = (Vertrag B) AND companyId = A` liefert `[]` — Portal-Gate würde 404.
3. Select `projects WHERE companyId = A AND status != 'archived'` liefert NUR das aktive Projekt A (nicht B, nicht archivierte).
4. Archivierte Projekte sind nicht im Result.

Cleanup wie in anderen integration-real Tests (delete by fixture-pattern).

- [ ] **Step 2: Commit**

```bash
git add src/__tests__/integration-real/portal-readonly-flow.test.ts
git commit -m "test(portal): integration test for read-only cross-company isolation"
```

---

## Task P3-7: Manual E2E + Deploy

**Files:** keine

- [ ] **Step 1: Lokaler Durchlauf**

1. `npm run dev`
2. Admin-Login → Firma anlegen → Vertrag + Projekt für Firma anlegen
3. Portal-User für Firma anlegen (invite-flow)
4. Portal-User einloggen → Dashboard zeigt Counts in Kacheln
5. `/portal/contracts` — Vertrag in Liste, Klick → Detail zeigt Positionen + Vertragstext
6. `/portal/projects` — Projekt in Liste, Klick → Kanban read-only
7. Direkter URL-Angriff `/portal/contracts/<id-fremde-firma>` → 404
8. Analog fremde Projekt-ID → 404

- [ ] **Step 2: Deploy**

```bash
git push
```

Spot-Check nach Deploy:
- `/portal/contracts` lädt ohne Fehler
- `/portal/projects` lädt ohne Fehler
- Dashboard-Counts stimmen mit DB überein

---

## Self-Review Checklist

- [ ] Alle 4 APIs filtern STRIKT nach `auth.companyId`
- [ ] 404 (nicht 403) bei Fremd-ID-Zugriff
- [ ] `ne(projects.status, 'archived')` in beiden Project-Routen
- [ ] `documents.type = 'contract'` in beiden Contract-Routen
- [ ] Sensible Felder (createdBy, assignedTo, comments, delegatedTo, estimatedMinutes) NICHT projiziert
- [ ] Contract-Body-HTML wird server-seitig durch `sanitizeHtml` (isomorphic-dompurify) geschleust
- [ ] Client-Rendering des sanitized HTML mit expliziter Code-Kommentar-Begründung
- [ ] Dashboard-Kacheln linken korrekt, Counts werden geladen
- [ ] Nav-Links in Layout in richtiger Reihenfolge
- [ ] Integration-Test prüft Cross-Company-Isolation
- [ ] Keine neuen Tabellen, keine Migration, kein Entrypoint-Update
- [ ] Keine Audit-Log-Writes (read-only = keine Pflicht per P1.5-Regel)
