# Portal-P1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Customer-Portal-Fundament: Rolle `portal_user`, Firmen↔User-Mapping, `/portal/*` mit Auth-Gate, minimales Dashboard, Admin-UX zum Anlegen von Portal-Zugängen (Passwort oder Invite-Link).

**Architecture:** Single Next.js app, route-group `/portal/*` getrennt von `/intern/*`. Rolle `portal_user` + neues `users.companyId` verknüpfen User zur Firma. Middleware gatet zwischen `/portal` (nur portal_user) und `/intern` (nur interne Rollen). Invite-Flow über einmaligen 64-Zeichen-Hex-Token mit 7d TTL. E-Mail-Versand läuft über existierende `email_accounts`-Infrastruktur + `EmailService.sendWithTemplate`.

**Tech Stack:** Next.js 15 App Router, Drizzle ORM (Postgres), bcrypt, jose (JWT), zod, vitest.

**Spec:** `docs/superpowers/specs/2026-04-22-portal-p1-design.md`

---

## File Structure

**Schema / Migration**
- Modify: `src/lib/db/schema.ts` — `users`-Table erweitern
- Create: `src/lib/db/migrations/008_portal_users.sql`
- Modify: `src/lib/db/migrations/index.ts` — Registry
- Modify: `docker/app/entrypoint.prod.sh` — pre-Drizzle ALTER

**Permissions & Auth**
- Modify: `src/lib/types/permissions.ts` — `portal_user`-Rolle
- Modify: `src/lib/auth/session.ts` — JWT-Claim `companyId`
- Create: `src/lib/auth/with-portal-auth.ts` — neue Guard-Funktion
- Create: `src/middleware.ts` — Routing-Gate

**Services**
- Modify: `src/lib/services/user.service.ts` — `createPortalUser`-Helper
- Modify: `src/lib/services/email-template.service.ts` — `portal_invite` Seed

**API**
- Create: `src/app/api/v1/companies/[id]/portal-users/route.ts`
- Create: `src/app/api/v1/users/[id]/portal-access/route.ts`
- Create: `src/app/api/v1/auth/accept-invite/route.ts`
- Create: `src/app/api/v1/portal/me/company/route.ts`
- Modify: `src/app/api/v1/auth/login/route.ts` — Rolle + Redirect-Hint

**Portal UI**
- Create: `src/app/portal/layout.tsx`
- Create: `src/app/portal/page.tsx`
- Create: `src/app/portal/accept-invite/page.tsx`

**Admin UI**
- Modify/Create: `src/app/intern/(dashboard)/contacts/companies/[id]/_components/portal-users-tab.tsx`
- Modify: `src/app/intern/(dashboard)/contacts/companies/[id]/page.tsx` — Tab einhängen

**Tests**
- Create: `src/__tests__/unit/auth/with-portal-auth.test.ts`
- Create: `src/__tests__/unit/services/user-service-portal.test.ts`
- Create: `src/__tests__/integration/api/portal-users.test.ts`
- Create: `src/__tests__/integration/api/accept-invite.test.ts`

---

## Task 1: DB-Schema + Migration

**Files:**
- Modify: `src/lib/db/schema.ts` (users-Tabelle)
- Create: `src/lib/db/migrations/008_portal_users.sql`
- Modify: `src/lib/db/migrations/index.ts`
- Modify: `docker/app/entrypoint.prod.sh`

- [ ] **Step 1: Schema erweitern**

In `src/lib/db/schema.ts`, im `users` pgTable-Block nach `roleId` einfügen:

```ts
  companyId: uuid('company_id').references(() => companies.id, { onDelete: 'set null' }),
  inviteToken: varchar('invite_token', { length: 64 }),
  inviteTokenExpiresAt: timestamp('invite_token_expires_at', { withTimezone: true }),
  firstLoginAt: timestamp('first_login_at', { withTimezone: true }),
```

Und im `(table) => [...]`-Block zwei Indexe ergänzen:

```ts
  index('idx_users_company_id').on(table.companyId),
  index('idx_users_invite_token').on(table.inviteToken),
```

- [ ] **Step 2: Migration-SQL anlegen**

Create `src/lib/db/migrations/008_portal_users.sql`:

```sql
-- ============================================================
-- Migration 008: portal_users (users.companyId + Invite-Flow)
-- Idempotent.
-- ============================================================

ALTER TABLE users
  ADD COLUMN IF NOT EXISTS company_id UUID REFERENCES companies(id) ON DELETE SET NULL,
  ADD COLUMN IF NOT EXISTS invite_token VARCHAR(64),
  ADD COLUMN IF NOT EXISTS invite_token_expires_at TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS first_login_at TIMESTAMPTZ;

CREATE INDEX IF NOT EXISTS idx_users_company_id ON users(company_id);
CREATE INDEX IF NOT EXISTS idx_users_invite_token ON users(invite_token);

-- Seed role 'portal_user'
INSERT INTO roles (name, display_name, description, is_system)
  VALUES ('portal_user', 'Portal-Nutzer', 'Extern angelegter Kunden-Zugang; Firmenbezogen', TRUE)
  ON CONFLICT DO NOTHING;
```

Note: `roles` hat keinen UNIQUE-Constraint auf `name` — `ON CONFLICT DO NOTHING` ohne Target ist für reine "existiert bereits"-Fälle nicht zuverlässig. Deshalb zusätzlich ein Guard:

Tausche den INSERT aus gegen:

```sql
DO $$
BEGIN
  IF NOT EXISTS (SELECT 1 FROM roles WHERE name = 'portal_user') THEN
    INSERT INTO roles (name, display_name, description, is_system)
      VALUES ('portal_user', 'Portal-Nutzer', 'Extern angelegter Kunden-Zugang; Firmenbezogen', TRUE);
  END IF;
END $$;
```

- [ ] **Step 3: Migration-Registry aktualisieren**

In `src/lib/db/migrations/index.ts` den Array um den neuen Eintrag ergänzen (als letztes Element):

```ts
  {
    name: '008_portal_users.sql',
    description: 'users.companyId + Invite-Flow Felder + Rolle portal_user',
  },
```

- [ ] **Step 4: Pre-Drizzle in entrypoint.prod.sh**

Im pre-Drizzle-SQL-Block von `docker/app/entrypoint.prod.sh` (nach dem `blog_categories`-Block) einfügen:

```sql
-- users: Portal-User-Felder
ALTER TABLE users
  ADD COLUMN IF NOT EXISTS company_id UUID REFERENCES companies(id) ON DELETE SET NULL,
  ADD COLUMN IF NOT EXISTS invite_token VARCHAR(64),
  ADD COLUMN IF NOT EXISTS invite_token_expires_at TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS first_login_at TIMESTAMPTZ;
CREATE INDEX IF NOT EXISTS idx_users_company_id ON users(company_id);
CREATE INDEX IF NOT EXISTS idx_users_invite_token ON users(invite_token);
```

- [ ] **Step 5: Typcheck**

Run: `npx tsc --noEmit 2>&1 | grep -E "schema\.ts|migrations" | head -5`
Expected: No output.

- [ ] **Step 6: Commit**

```bash
git add src/lib/db/schema.ts src/lib/db/migrations/008_portal_users.sql src/lib/db/migrations/index.ts docker/app/entrypoint.prod.sh
git commit -m "feat(portal): users.companyId + invite-token fields + portal_user role"
```

---

## Task 2: Permissions — `portal_user`-Rolle registrieren

**Files:**
- Modify: `src/lib/types/permissions.ts`

- [ ] **Step 1: Rolle in VALID_ROLES ergänzen**

Die Datei `src/lib/types/permissions.ts` öffnen, den Export der gültigen Rollen lesen, und `'portal_user'` in die Liste aufnehmen. Wenn es eine Map gibt die Rolle → Berechtigungen abbildet, `portal_user` auf leere Berechtigungen setzen (in P1 hat die Rolle keine CRUD-Flags auf andere Module).

Beispiel — wenn es eine Funktion wie `getDefaultPermissions(role)` gibt:

```ts
if (role === 'portal_user') return []  // No module permissions in P1
```

- [ ] **Step 2: Typen anpassen**

Der Typ `UserRole` (oder wie er heißt) muss `'portal_user'` enthalten. In `RoleBadge`-Anzeige-Komponenten ggf. Label "Portal-Nutzer" ergänzen.

- [ ] **Step 3: Typcheck**

Run: `npx tsc --noEmit 2>&1 | grep "permissions" | head -5`
Expected: No output.

- [ ] **Step 4: Commit**

```bash
git add src/lib/types/permissions.ts
git commit -m "feat(portal): register portal_user role in permissions registry"
```

---

## Task 3: JWT erweitern — `companyId` in Session

**Files:**
- Modify: `src/lib/auth/session.ts`

- [ ] **Step 1: Session-Payload erweitern**

Datei lesen. Den Typ `SessionPayload` (oder wie er heißt) um `companyId?: string | null` erweitern. Bei `createSession(user)` muss `user.companyId` in den JWT-Claim übernommen werden.

```ts
export interface SessionPayload {
  userId: string
  email: string
  role: string
  companyId?: string | null  // Only set for portal_user
  // ...existing fields
}

// In createSession:
const payload: SessionPayload = {
  userId: user.id,
  email: user.email,
  role: user.role,
  companyId: user.companyId ?? null,
}
```

Bei `verifySession` / `getSession` — der Rückgabetyp liefert automatisch das neue Feld.

- [ ] **Step 2: Unit-Test schreiben**

Create `src/__tests__/unit/auth/session-portal.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { createSession, verifySession } from '@/lib/auth/session'

describe('session with companyId', () => {
  it('includes companyId for portal_user and roundtrips', async () => {
    const user = { id: 'u1', email: 'p@ex.com', role: 'portal_user', companyId: 'c1' } as const
    const token = await createSession(user as any)
    const payload = await verifySession(token)
    expect(payload?.companyId).toBe('c1')
    expect(payload?.role).toBe('portal_user')
  })

  it('companyId is null for internal users', async () => {
    const user = { id: 'u2', email: 'i@ex.com', role: 'admin', companyId: null } as const
    const token = await createSession(user as any)
    const payload = await verifySession(token)
    expect(payload?.companyId).toBeNull()
  })
})
```

- [ ] **Step 3: Test ausführen (muss erst failen)**

Run: `npx vitest run src/__tests__/unit/auth/session-portal.test.ts`
Expected: Fails, weil Payload-Typ oder createSession noch kein companyId kennt.

- [ ] **Step 4: Implementierung nachziehen bis Test grün**

Anpassungen in `session.ts` bis beide Tests grün sind.

- [ ] **Step 5: Test ausführen**

Run: `npx vitest run src/__tests__/unit/auth/session-portal.test.ts`
Expected: 2 tests passing.

- [ ] **Step 6: Commit**

```bash
git add src/lib/auth/session.ts src/__tests__/unit/auth/session-portal.test.ts
git commit -m "feat(portal): session JWT carries companyId claim"
```

---

## Task 4: `withPortalAuth`-Helper

**Files:**
- Create: `src/lib/auth/with-portal-auth.ts`
- Create: `src/__tests__/unit/auth/with-portal-auth.test.ts`

- [ ] **Step 1: Failing test schreiben**

Create `src/__tests__/unit/auth/with-portal-auth.test.ts`:

```ts
import { describe, it, expect, vi } from 'vitest'
import { NextRequest } from 'next/server'
import { withPortalAuth } from '@/lib/auth/with-portal-auth'
import * as sessionModule from '@/lib/auth/session'

function mockReq() {
  return new NextRequest('http://localhost/api/v1/portal/me/company')
}

describe('withPortalAuth', () => {
  it('returns 401 when no session', async () => {
    vi.spyOn(sessionModule, 'getSession').mockResolvedValue(null)
    const handler = vi.fn()
    const res = await withPortalAuth(mockReq(), handler)
    expect(res.status).toBe(401)
    expect(handler).not.toHaveBeenCalled()
  })

  it('returns 403 when role is not portal_user', async () => {
    vi.spyOn(sessionModule, 'getSession').mockResolvedValue({
      userId: 'u1', email: 'a@x.de', role: 'admin', companyId: null,
    } as any)
    const handler = vi.fn()
    const res = await withPortalAuth(mockReq(), handler)
    expect(res.status).toBe(403)
    expect(handler).not.toHaveBeenCalled()
  })

  it('returns 403 when portal_user has no companyId', async () => {
    vi.spyOn(sessionModule, 'getSession').mockResolvedValue({
      userId: 'u1', email: 'p@x.de', role: 'portal_user', companyId: null,
    } as any)
    const handler = vi.fn()
    const res = await withPortalAuth(mockReq(), handler)
    expect(res.status).toBe(403)
  })

  it('invokes handler with auth context when valid', async () => {
    vi.spyOn(sessionModule, 'getSession').mockResolvedValue({
      userId: 'u1', email: 'p@x.de', role: 'portal_user', companyId: 'c1',
    } as any)
    const handler = vi.fn().mockResolvedValue(new Response('ok'))
    await withPortalAuth(mockReq(), handler)
    expect(handler).toHaveBeenCalledWith(expect.objectContaining({
      userId: 'u1', companyId: 'c1',
    }))
  })
})
```

- [ ] **Step 2: Test ausführen (muss failen)**

Run: `npx vitest run src/__tests__/unit/auth/with-portal-auth.test.ts`
Expected: Cannot find module 'with-portal-auth'.

- [ ] **Step 3: Implementierung schreiben**

Create `src/lib/auth/with-portal-auth.ts`:

```ts
import { NextRequest } from 'next/server'
import { apiError } from '@/lib/utils/api-response'
import { getSession } from './session'

export interface PortalAuthContext {
  userId: string
  email: string
  companyId: string
}

export async function withPortalAuth(
  request: NextRequest,
  handler: (auth: PortalAuthContext) => Promise<Response> | Response
): Promise<Response> {
  const session = await getSession(request)
  if (!session) {
    return apiError('UNAUTHORIZED', 'Nicht angemeldet', 401)
  }
  if (session.role !== 'portal_user') {
    return apiError('FORBIDDEN', 'Kein Portal-Zugang', 403)
  }
  if (!session.companyId) {
    return apiError('FORBIDDEN', 'Portal-User ohne Firmenzuordnung', 403)
  }
  return handler({
    userId: session.userId,
    email: session.email,
    companyId: session.companyId,
  })
}
```

- [ ] **Step 4: Test ausführen**

Run: `npx vitest run src/__tests__/unit/auth/with-portal-auth.test.ts`
Expected: 4 tests passing.

- [ ] **Step 5: Commit**

```bash
git add src/lib/auth/with-portal-auth.ts src/__tests__/unit/auth/with-portal-auth.test.ts
git commit -m "feat(portal): withPortalAuth guard for /api/v1/portal/*"
```

---

## Task 5: Middleware — Routing-Gate

**Files:**
- Create: `src/middleware.ts` (oder bestehende erweitern — vorher prüfen)

- [ ] **Step 1: Prüfen, ob Middleware existiert**

Run: `ls src/middleware.ts src/proxy.ts 2>/dev/null`

Falls `middleware.ts` nicht existiert, neu anlegen. Falls `proxy.ts` existiert mit relevanter Logik, stattdessen dort erweitern.

- [ ] **Step 2: Middleware-Logik**

Create `src/middleware.ts`:

```ts
import { NextRequest, NextResponse } from 'next/server'
import { getSession } from '@/lib/auth/session'

export async function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl

  // Public portal routes
  if (pathname === '/portal/accept-invite' || pathname.startsWith('/portal/accept-invite/')) {
    return NextResponse.next()
  }

  // Portal gate
  if (pathname.startsWith('/portal')) {
    const session = await getSession(request)
    if (!session) {
      const loginUrl = new URL('/login', request.url)
      loginUrl.searchParams.set('next', pathname)
      return NextResponse.redirect(loginUrl)
    }
    if (session.role !== 'portal_user' || !session.companyId) {
      return new NextResponse('Forbidden', { status: 403 })
    }
    return NextResponse.next()
  }

  // Intern gate — block portal users
  if (pathname.startsWith('/intern')) {
    const session = await getSession(request)
    if (session && session.role === 'portal_user') {
      return new NextResponse('Forbidden', { status: 403 })
    }
    // Existing intern-auth is handled in individual pages/APIs; we only add
    // the portal-user block here.
    return NextResponse.next()
  }

  return NextResponse.next()
}

export const config = {
  matcher: ['/portal/:path*', '/intern/:path*'],
}
```

Hinweis: Falls `getSession` nicht edge-safe ist (z.B. wegen `db`-Import), muss eine edge-freundliche Variante wie `getSessionFromCookieToken` genutzt werden, die nur den JWT verifiziert ohne DB. Prüfen in Step 3.

- [ ] **Step 3: Edge-Safety prüfen**

Run: `npm run build 2>&1 | tail -40`
Expected: build succeeds OR ein Fehler wie "Cannot use … in edge runtime". Falls letzteres, `getSession` durch ein reines JWT-Verify ersetzen:

```ts
import { jwtVerify } from 'jose'

async function verifyToken(token: string) {
  try {
    const secret = new TextEncoder().encode(process.env.SESSION_SECRET!)
    const { payload } = await jwtVerify(token, secret)
    return payload as { userId: string; role: string; companyId?: string | null }
  } catch { return null }
}

const token = request.cookies.get('session')?.value
const session = token ? await verifyToken(token) : null
```

Cookie-Name und secret env-var anpassen an Realität (aus `session.ts`).

- [ ] **Step 4: Manual-Test vorbereiten (kein automatisierter Test in P1)**

Notizzettel-Test nach Deploy:
- Ohne Session `/portal` → 302 auf `/login?next=/portal`
- Admin-Session auf `/portal` → 403
- Portal-Session auf `/intern` → 403

- [ ] **Step 5: Commit**

```bash
git add src/middleware.ts
git commit -m "feat(portal): middleware gates /portal (portal_user only) and /intern (block portal_user)"
```

---

## Task 6: `portal_invite` E-Mail-Template seed

**Files:**
- Modify: `src/lib/services/email-template.service.ts`

- [ ] **Step 1: Template in DEFAULT_EMAIL_TEMPLATES**

In `DEFAULT_EMAIL_TEMPLATES`-Array einfügen (nach `lead_admin_notification`):

```ts
{
  slug: 'portal_invite',
  name: 'Portal: Einladungs-Link',
  subject: 'Ihr Zugang zum Kundenportal von {{firma}}',
  bodyHtml: `<p>Hallo {{name}},</p>
<p>Sie wurden als Zugang zum Kundenportal von <strong>{{firma}}</strong> angelegt. Klicken Sie auf den folgenden Link, um Ihr Passwort festzulegen und den Zugang zu aktivieren. Der Link ist 7 Tage gültig.</p>
<p><a href="{{inviteUrl}}">Zugang aktivieren</a></p>
<p>Mit freundlichen Grüßen<br>{{absender}}</p>`,
  placeholders: [
    { key: 'name', label: 'Name', description: 'Name des Portal-Nutzers' },
    { key: 'firma', label: 'Firma', description: 'Name der Firma' },
    { key: 'inviteUrl', label: 'Einladungs-Link', description: 'Vollständige URL mit Token' },
    { key: 'absender', label: 'Absender', description: 'Name des einladenden Admins' },
  ],
},
```

- [ ] **Step 2: Typcheck**

Run: `npx tsc --noEmit 2>&1 | grep email-template`
Expected: No output.

- [ ] **Step 3: Commit**

```bash
git add src/lib/services/email-template.service.ts
git commit -m "feat(portal): seed portal_invite email template"
```

---

## Task 7: `createPortalUser`-Service

**Files:**
- Modify: `src/lib/services/user.service.ts` (oder neue Datei falls es keinen UserService gibt)
- Create: `src/__tests__/unit/services/user-service-portal.test.ts`

- [ ] **Step 1: Bestehenden UserService prüfen**

Run: `ls src/lib/services/user.service.ts 2>/dev/null`

Falls nicht vorhanden, neu anlegen mit nur der Portal-Funktion. Falls vorhanden, die neue Funktion ergänzen.

- [ ] **Step 2: Failing test**

Create `src/__tests__/unit/services/user-service-portal.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { UserService } from '@/lib/services/user.service'

vi.mock('@/lib/db', () => {
  const db = {
    select: vi.fn().mockReturnThis(),
    from: vi.fn().mockReturnThis(),
    where: vi.fn().mockReturnThis(),
    limit: vi.fn().mockResolvedValue([]),
    insert: vi.fn().mockReturnThis(),
    values: vi.fn().mockReturnThis(),
    returning: vi.fn().mockResolvedValue([{ id: 'new-user-id', email: 'x@y.de' }]),
  }
  return { db }
})

describe('UserService.createPortalUser', () => {
  beforeEach(() => vi.clearAllMocks())

  it('creates user with role=portal_user + companyId + real password hash', async () => {
    const user = await UserService.createPortalUser({
      companyId: 'c1',
      firstName: 'Max',
      lastName: 'Muster',
      email: 'max@kunde.de',
      method: 'password',
      password: 'Secret12345',
    })
    expect(user.id).toBe('new-user-id')
  })

  it('creates user with invite token and dummy password when method=invite', async () => {
    const user = await UserService.createPortalUser({
      companyId: 'c1',
      firstName: 'Max',
      lastName: 'Muster',
      email: 'max@kunde.de',
      method: 'invite',
    })
    expect(user.id).toBe('new-user-id')
  })

  it('rejects duplicate portal_user for same company', async () => {
    const { db } = await import('@/lib/db')
    ;(db.limit as any).mockResolvedValueOnce([{ id: 'existing' }])
    await expect(UserService.createPortalUser({
      companyId: 'c1',
      firstName: 'Max',
      lastName: 'Muster',
      email: 'max@kunde.de',
      method: 'invite',
    })).rejects.toThrow(/bereits vorhanden/i)
  })
})
```

- [ ] **Step 3: Test ausführen**

Run: `npx vitest run src/__tests__/unit/services/user-service-portal.test.ts`
Expected: Failing — Methode nicht vorhanden.

- [ ] **Step 4: Implementierung**

In `src/lib/services/user.service.ts` (neu oder ergänzt) die Funktion:

```ts
import { db } from '@/lib/db'
import { users } from '@/lib/db/schema'
import { eq, and } from 'drizzle-orm'
import bcrypt from 'bcryptjs'
import crypto from 'node:crypto'
import { logger } from '@/lib/utils/logger'

export interface CreatePortalUserInput {
  companyId: string
  firstName: string
  lastName: string
  email: string
  method: 'password' | 'invite'
  password?: string  // required if method='password'
}

function generateInviteToken(): string {
  return crypto.randomBytes(32).toString('hex')
}

async function generateDummyHash(): Promise<string> {
  const random = crypto.randomBytes(24).toString('hex')
  return bcrypt.hash(random, 10)
}

export const UserService = {
  async createPortalUser(input: CreatePortalUserInput) {
    // Duplicate check: same email + same companyId + role portal_user
    const [existing] = await db
      .select({ id: users.id })
      .from(users)
      .where(and(
        eq(users.email, input.email),
        eq(users.companyId, input.companyId),
        eq(users.role, 'portal_user'),
      ))
      .limit(1)
    if (existing) {
      throw new Error('Portal-User mit dieser E-Mail ist für diese Firma bereits vorhanden')
    }

    let passwordHash: string
    let inviteToken: string | null = null
    let inviteTokenExpiresAt: Date | null = null

    if (input.method === 'password') {
      if (!input.password || input.password.length < 10) {
        throw new Error('Passwort muss mindestens 10 Zeichen lang sein')
      }
      passwordHash = await bcrypt.hash(input.password, 10)
    } else {
      passwordHash = await generateDummyHash()
      inviteToken = generateInviteToken()
      inviteTokenExpiresAt = new Date(Date.now() + 7 * 24 * 60 * 60 * 1000)
    }

    const [created] = await db.insert(users).values({
      email: input.email,
      firstName: input.firstName,
      lastName: input.lastName,
      role: 'portal_user',
      status: 'active',
      companyId: input.companyId,
      passwordHash,
      inviteToken,
      inviteTokenExpiresAt,
    }).returning()

    logger.info(`Portal user created: ${created.email} (company=${input.companyId}, method=${input.method})`, { module: 'UserService' })
    return created
  },

  /** Regenerate the invite token (e.g. "resend invite"). */
  async regenerateInviteToken(userId: string) {
    const token = generateInviteToken()
    const expiresAt = new Date(Date.now() + 7 * 24 * 60 * 60 * 1000)
    const [updated] = await db
      .update(users)
      .set({ inviteToken: token, inviteTokenExpiresAt: expiresAt, updatedAt: new Date() })
      .where(eq(users.id, userId))
      .returning()
    return updated
  },

  /** Accept an invite: validate token, set password, clear token. */
  async acceptInvite(token: string, newPassword: string) {
    if (newPassword.length < 10) throw new Error('Passwort muss mindestens 10 Zeichen lang sein')
    const [user] = await db
      .select()
      .from(users)
      .where(eq(users.inviteToken, token))
      .limit(1)
    if (!user) throw new Error('Ungültiger Einladungs-Link')
    if (!user.inviteTokenExpiresAt || user.inviteTokenExpiresAt < new Date()) {
      throw new Error('Einladungs-Link ist abgelaufen')
    }

    const passwordHash = await bcrypt.hash(newPassword, 10)
    const [updated] = await db.update(users).set({
      passwordHash,
      inviteToken: null,
      inviteTokenExpiresAt: null,
      firstLoginAt: new Date(),
      updatedAt: new Date(),
    }).where(eq(users.id, user.id)).returning()
    return updated
  },
}
```

- [ ] **Step 5: Test ausführen**

Run: `npx vitest run src/__tests__/unit/services/user-service-portal.test.ts`
Expected: 3 tests passing.

- [ ] **Step 6: Commit**

```bash
git add src/lib/services/user.service.ts src/__tests__/unit/services/user-service-portal.test.ts
git commit -m "feat(portal): UserService.createPortalUser + acceptInvite + regenerateInviteToken"
```

---

## Task 8: API `POST /api/v1/companies/[id]/portal-users`

**Files:**
- Create: `src/app/api/v1/companies/[id]/portal-users/route.ts`

- [ ] **Step 1: Route anlegen**

```ts
import { NextRequest } from 'next/server'
import { z } from 'zod'
import { apiSuccess, apiError } from '@/lib/utils/api-response'
import { validateAndParse } from '@/lib/utils/validation'
import { withPermission } from '@/lib/auth/require-permission'
import { UserService } from '@/lib/services/user.service'
import { CompanyService } from '@/lib/services/company.service'
import { OrganizationService } from '@/lib/services/organization.service'
import { TaskQueueService } from '@/lib/services/task-queue.service'
import { db } from '@/lib/db'
import { users } from '@/lib/db/schema'
import { eq, and } from 'drizzle-orm'
import { logger } from '@/lib/utils/logger'

type Params = Promise<{ id: string }>

const createSchema = z.discriminatedUnion('method', [
  z.object({
    method: z.literal('password'),
    firstName: z.string().min(1),
    lastName: z.string().min(1),
    email: z.string().email(),
    password: z.string().min(10, 'Passwort muss mindestens 10 Zeichen lang sein'),
  }),
  z.object({
    method: z.literal('invite'),
    firstName: z.string().min(1),
    lastName: z.string().min(1),
    email: z.string().email(),
  }),
])

export async function POST(request: NextRequest, { params }: { params: Params }) {
  return withPermission(request, 'users', 'create', async (auth) => {
    const { id: companyId } = await params
    try {
      const company = await CompanyService.getById(companyId)
      if (!company) return apiError('NOT_FOUND', 'Firma nicht gefunden', 404)

      const body = await request.json()
      const validation = validateAndParse(createSchema, body)
      if (!validation.success) {
        return apiError('VALIDATION_ERROR', 'Ungültige Eingabe', 400)
      }
      const data = validation.data

      const user = await UserService.createPortalUser({
        companyId,
        firstName: data.firstName,
        lastName: data.lastName,
        email: data.email,
        method: data.method,
        password: data.method === 'password' ? data.password : undefined,
      })

      // For invite method: queue the email
      if (data.method === 'invite' && user.inviteToken) {
        const baseUrl = process.env.NEXT_PUBLIC_APP_URL || process.env.NEXTAUTH_URL || 'http://localhost:3000'
        const inviteUrl = `${baseUrl}/portal/accept-invite?token=${user.inviteToken}`
        const org = await OrganizationService.getById()
        await TaskQueueService.create({
          type: 'email',
          priority: 1,
          payload: {
            templateSlug: 'portal_invite',
            to: user.email,
            placeholders: {
              name: `${user.firstName} ${user.lastName}`.trim(),
              firma: company.name,
              inviteUrl,
              absender: org?.name || 'Ihr Team',
            },
          },
          referenceType: 'company',
          referenceId: companyId,
        })
        logger.info(`Invite queued for ${user.email}`, { module: 'PortalUsersAPI' })
      }

      // Mask sensitive fields
      return apiSuccess({
        id: user.id,
        email: user.email,
        firstName: user.firstName,
        lastName: user.lastName,
        role: user.role,
        status: user.status,
        companyId: user.companyId,
        firstLoginAt: user.firstLoginAt,
        hasPendingInvite: !!user.inviteToken,
      }, undefined, 201)
    } catch (error) {
      logger.error('Failed to create portal user', error, { module: 'PortalUsersAPI' })
      const msg = error instanceof Error ? error.message : 'Anlegen fehlgeschlagen'
      return apiError('CREATE_FAILED', msg, 400)
    }
  })
}

export async function GET(request: NextRequest, { params }: { params: Params }) {
  return withPermission(request, 'users', 'read', async () => {
    const { id: companyId } = await params
    try {
      const rows = await db
        .select({
          id: users.id,
          email: users.email,
          firstName: users.firstName,
          lastName: users.lastName,
          role: users.role,
          status: users.status,
          firstLoginAt: users.firstLoginAt,
          inviteTokenExpiresAt: users.inviteTokenExpiresAt,
          hasPendingInvite: users.inviteToken,
          createdAt: users.createdAt,
        })
        .from(users)
        .where(and(eq(users.companyId, companyId), eq(users.role, 'portal_user')))
      return apiSuccess(rows.map(r => ({ ...r, hasPendingInvite: !!r.hasPendingInvite })))
    } catch (error) {
      logger.error('Failed to list portal users', error, { module: 'PortalUsersAPI' })
      return apiError('INTERNAL_ERROR', 'Fehler beim Laden', 500)
    }
  })
}
```

- [ ] **Step 2: Typcheck**

Run: `npx tsc --noEmit 2>&1 | grep portal-users | head -5`
Expected: No output.

- [ ] **Step 3: Commit**

```bash
git add src/app/api/v1/companies/[id]/portal-users/route.ts
git commit -m "feat(portal): API POST/GET /api/v1/companies/[id]/portal-users"
```

---

## Task 9: API `PATCH /api/v1/users/[id]/portal-access`

**Files:**
- Create: `src/app/api/v1/users/[id]/portal-access/route.ts`

- [ ] **Step 1: Route anlegen**

```ts
import { NextRequest } from 'next/server'
import { z } from 'zod'
import { apiSuccess, apiError, apiNotFound } from '@/lib/utils/api-response'
import { validateAndParse } from '@/lib/utils/validation'
import { withPermission } from '@/lib/auth/require-permission'
import { UserService } from '@/lib/services/user.service'
import { TaskQueueService } from '@/lib/services/task-queue.service'
import { CompanyService } from '@/lib/services/company.service'
import { OrganizationService } from '@/lib/services/organization.service'
import { db } from '@/lib/db'
import { users } from '@/lib/db/schema'
import { eq } from 'drizzle-orm'
import { logger } from '@/lib/utils/logger'

type Params = Promise<{ id: string }>

const patchSchema = z.object({
  action: z.enum(['deactivate', 'reactivate', 'resend_invite']),
})

export async function PATCH(request: NextRequest, { params }: { params: Params }) {
  return withPermission(request, 'users', 'update', async () => {
    const { id } = await params
    try {
      const body = await request.json()
      const validation = validateAndParse(patchSchema, body)
      if (!validation.success) return apiError('VALIDATION_ERROR', 'Ungültige Aktion', 400)

      const [user] = await db.select().from(users).where(eq(users.id, id)).limit(1)
      if (!user) return apiNotFound('User nicht gefunden')
      if (user.role !== 'portal_user') return apiError('FORBIDDEN', 'User ist kein Portal-Nutzer', 403)

      if (validation.data.action === 'deactivate') {
        await db.update(users).set({ status: 'inactive', updatedAt: new Date() }).where(eq(users.id, id))
        return apiSuccess({ status: 'inactive' })
      }

      if (validation.data.action === 'reactivate') {
        await db.update(users).set({ status: 'active', updatedAt: new Date() }).where(eq(users.id, id))
        return apiSuccess({ status: 'active' })
      }

      if (validation.data.action === 'resend_invite') {
        const updated = await UserService.regenerateInviteToken(id)
        if (!updated?.companyId) return apiError('INVALID_STATE', 'User hat keine Firma', 400)
        const company = await CompanyService.getById(updated.companyId)
        const org = await OrganizationService.getById()
        const baseUrl = process.env.NEXT_PUBLIC_APP_URL || process.env.NEXTAUTH_URL || 'http://localhost:3000'
        const inviteUrl = `${baseUrl}/portal/accept-invite?token=${updated.inviteToken}`
        await TaskQueueService.create({
          type: 'email',
          priority: 1,
          payload: {
            templateSlug: 'portal_invite',
            to: updated.email,
            placeholders: {
              name: `${updated.firstName ?? ''} ${updated.lastName ?? ''}`.trim(),
              firma: company?.name ?? 'Ihre Firma',
              inviteUrl,
              absender: org?.name || 'Ihr Team',
            },
          },
          referenceType: 'company',
          referenceId: updated.companyId,
        })
        logger.info(`Invite resent to ${updated.email}`, { module: 'PortalAccessAPI' })
        return apiSuccess({ resent: true })
      }

      return apiError('INVALID_ACTION', 'Unbekannte Aktion', 400)
    } catch (error) {
      logger.error('portal-access PATCH error', error, { module: 'PortalAccessAPI' })
      return apiError('INTERNAL_ERROR', error instanceof Error ? error.message : 'Fehler', 500)
    }
  })
}
```

- [ ] **Step 2: Typcheck**

Run: `npx tsc --noEmit 2>&1 | grep portal-access | head -5`
Expected: No output.

- [ ] **Step 3: Commit**

```bash
git add src/app/api/v1/users/[id]/portal-access/route.ts
git commit -m "feat(portal): API PATCH /api/v1/users/[id]/portal-access (deactivate/reactivate/resend)"
```

---

## Task 10: API `POST /api/v1/auth/accept-invite`

**Files:**
- Create: `src/app/api/v1/auth/accept-invite/route.ts`

- [ ] **Step 1: Route mit Rate-Limit**

```ts
import { NextRequest } from 'next/server'
import { z } from 'zod'
import { apiSuccess, apiError } from '@/lib/utils/api-response'
import { validateAndParse } from '@/lib/utils/validation'
import { rateLimit } from '@/lib/utils/rate-limit'
import { UserService } from '@/lib/services/user.service'
import { createSession } from '@/lib/auth/session'
import { logger } from '@/lib/utils/logger'

const schema = z.object({
  token: z.string().min(32).max(128),
  password: z.string()
    .min(10, 'Passwort muss mindestens 10 Zeichen lang sein')
    .regex(/[A-Za-z]/, 'Passwort muss mindestens einen Buchstaben enthalten')
    .regex(/[0-9]/, 'Passwort muss mindestens eine Ziffer enthalten'),
})

export async function POST(request: NextRequest) {
  const limited = await rateLimit(request, 'auth-accept-invite', 10, 60_000)
  if (limited) return limited

  try {
    const body = await request.json()
    const validation = validateAndParse(schema, body)
    if (!validation.success) {
      return apiError('VALIDATION_ERROR', 'Ungültige Eingabe', 400)
    }

    const user = await UserService.acceptInvite(validation.data.token, validation.data.password)
    if (!user) return apiError('INVALID_TOKEN', 'Ungültiger oder abgelaufener Link', 400)

    const token = await createSession({
      id: user.id,
      email: user.email,
      role: user.role,
      companyId: user.companyId,
    } as any)

    const response = apiSuccess({ userId: user.id, role: user.role, redirectTo: '/portal' })
    response.headers.append('Set-Cookie', `session=${token}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=${7 * 24 * 60 * 60}`)
    return response
  } catch (error) {
    logger.error('accept-invite error', error, { module: 'AcceptInviteAPI' })
    const msg = error instanceof Error ? error.message : 'Fehler'
    return apiError('INVITE_FAILED', msg, 400)
  }
}
```

Note: Exakte Cookie-Attribute an den bestehenden Login-Handler angleichen (Cookie-Name, Attribute-Set). Vorher nachschauen in `src/app/api/v1/auth/login/route.ts`.

- [ ] **Step 2: Typcheck**

Run: `npx tsc --noEmit 2>&1 | grep accept-invite | head -5`
Expected: No output.

- [ ] **Step 3: Commit**

```bash
git add src/app/api/v1/auth/accept-invite/route.ts
git commit -m "feat(portal): public API POST /api/v1/auth/accept-invite"
```

---

## Task 11: Login — Rolle + Redirect-Hint

**Files:**
- Modify: `src/app/api/v1/auth/login/route.ts`

- [ ] **Step 1: Response-Body erweitern**

Öffne die Datei. In der Success-Response (`apiSuccess(...)`) das Feld `redirectTo` ergänzen:

```ts
const redirectTo = user.role === 'portal_user' ? '/portal' : '/intern'
return withCookie(apiSuccess({
  user: { id: user.id, email: user.email, role: user.role },
  redirectTo,
}), token)
```

(Exakte Stelle + Variablennamen anpassen an vorhandene Datei; der Kern: Rolle auswerten und `redirectTo` mitsenden.)

- [ ] **Step 2: Login-UI Redirect-Anpassung**

Datei `src/app/(auth)/login/...` oder wo das Login-Formular lebt (vorher suchen mit `grep -rn "redirectTo\|router.push.*intern" src/app | head`). Nach erfolgreichem Login:

```ts
const data = await response.json()
if (data.success) {
  const next = searchParams.get('next')
  // Only honor next= if it matches the role's domain
  if (next && ((data.data.redirectTo === '/portal' && next.startsWith('/portal'))
               || (data.data.redirectTo === '/intern' && next.startsWith('/intern')))) {
    router.push(next)
  } else {
    router.push(data.data.redirectTo || '/intern')
  }
}
```

- [ ] **Step 3: Typcheck**

Run: `npx tsc --noEmit 2>&1 | grep "auth/login" | head -5`
Expected: No output.

- [ ] **Step 4: Commit**

```bash
git add src/app/api/v1/auth/login/route.ts src/app/*/login/*
git commit -m "feat(portal): login returns role-based redirectTo; UI honors it"
```

---

## Task 12: API `GET /api/v1/portal/me/company`

**Files:**
- Create: `src/app/api/v1/portal/me/company/route.ts`

- [ ] **Step 1: Route**

```ts
import { NextRequest } from 'next/server'
import { apiSuccess, apiError, apiNotFound } from '@/lib/utils/api-response'
import { withPortalAuth } from '@/lib/auth/with-portal-auth'
import { CompanyService } from '@/lib/services/company.service'
import { logger } from '@/lib/utils/logger'

export async function GET(request: NextRequest) {
  return withPortalAuth(request, async (auth) => {
    try {
      const company = await CompanyService.getById(auth.companyId)
      if (!company) return apiNotFound('Firma nicht gefunden')
      // Return only portal-safe fields
      return apiSuccess({
        id: company.id,
        name: company.name,
        legalForm: company.legalForm,
        street: company.street,
        houseNumber: company.houseNumber,
        postalCode: company.postalCode,
        city: company.city,
        country: company.country,
        phone: company.phone,
        email: company.email,
        website: company.website,
        industry: company.industry,
        vatId: company.vatId,
      })
    } catch (error) {
      logger.error('portal me/company error', error, { module: 'PortalAPI' })
      return apiError('INTERNAL_ERROR', 'Fehler', 500)
    }
  })
}
```

- [ ] **Step 2: Typcheck**

Run: `npx tsc --noEmit 2>&1 | grep "portal/me"`
Expected: No output.

- [ ] **Step 3: Commit**

```bash
git add src/app/api/v1/portal/me/company/route.ts
git commit -m "feat(portal): API GET /api/v1/portal/me/company"
```

---

## Task 13: Portal-Layout

**Files:**
- Create: `src/app/portal/layout.tsx`

- [ ] **Step 1: Minimales Layout**

```tsx
import type { Metadata } from 'next'
import Link from 'next/link'
import { LogoutButton } from './_components/logout-button'

export const metadata: Metadata = {
  title: 'Kundenportal',
}

export default function PortalLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen bg-muted/20 flex flex-col">
      <header className="border-b bg-background">
        <div className="container mx-auto flex items-center justify-between px-4 py-3">
          <Link href="/portal" className="font-semibold">
            Kundenportal
          </Link>
          <LogoutButton />
        </div>
      </header>
      <main className="flex-1 container mx-auto px-4 py-8">
        {children}
      </main>
      <footer className="border-t py-4 text-center text-xs text-muted-foreground">
        © {new Date().getFullYear()} · Kundenportal
      </footer>
    </div>
  )
}
```

- [ ] **Step 2: Logout-Button**

Create `src/app/portal/_components/logout-button.tsx`:

```tsx
'use client'
import { Button } from '@/components/ui/button'
import { LogOut } from 'lucide-react'
import { useRouter } from 'next/navigation'

export function LogoutButton() {
  const router = useRouter()
  const logout = async () => {
    await fetch('/api/v1/auth/logout', { method: 'POST' })
    router.push('/login')
  }
  return (
    <Button variant="ghost" size="sm" onClick={logout}>
      <LogOut className="h-4 w-4 mr-2" /> Abmelden
    </Button>
  )
}
```

Note: Falls `/api/v1/auth/logout` nicht existiert, die vorhandene Logout-Logik finden und verlinken.

- [ ] **Step 3: Commit**

```bash
git add src/app/portal/layout.tsx src/app/portal/_components/logout-button.tsx
git commit -m "feat(portal): portal layout shell with header + logout"
```

---

## Task 14: Portal-Dashboard

**Files:**
- Create: `src/app/portal/page.tsx`

- [ ] **Step 1: Page**

```tsx
'use client'

import { useEffect, useState } from 'react'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { Loader2, Building2, FileText, Briefcase, ShoppingCart, MessageCircle } from 'lucide-react'

interface PortalCompany {
  id: string
  name: string
  street: string | null
  houseNumber: string | null
  postalCode: string | null
  city: string | null
  country: string | null
  phone: string | null
  email: string | null
  website: string | null
}

export default function PortalDashboard() {
  const [company, setCompany] = useState<PortalCompany | null>(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    fetch('/api/v1/portal/me/company')
      .then(r => r.json())
      .then(d => { if (d?.success) setCompany(d.data) })
      .finally(() => setLoading(false))
  }, [])

  if (loading) {
    return <div className="flex justify-center py-16"><Loader2 className="h-6 w-6 animate-spin" /></div>
  }

  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-2xl font-semibold">Willkommen zurück</h1>
        {company && <p className="text-muted-foreground">Angemeldet für: <strong>{company.name}</strong></p>}
      </div>

      <Card>
        <CardHeader className="flex flex-row items-center justify-between">
          <CardTitle className="flex items-center gap-2"><Building2 className="h-5 w-5" /> Meine Firmendaten</CardTitle>
          <Button variant="outline" size="sm" disabled>Bearbeiten (kommt in Kürze)</Button>
        </CardHeader>
        <CardContent>
          {company ? (
            <dl className="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
              <div><dt className="text-muted-foreground">Firmenname</dt><dd>{company.name}</dd></div>
              <div><dt className="text-muted-foreground">Adresse</dt><dd>{[company.street, company.houseNumber].filter(Boolean).join(' ')}<br/>{[company.postalCode, company.city].filter(Boolean).join(' ')}</dd></div>
              <div><dt className="text-muted-foreground">Telefon</dt><dd>{company.phone || '—'}</dd></div>
              <div><dt className="text-muted-foreground">E-Mail</dt><dd>{company.email || '—'}</dd></div>
              <div><dt className="text-muted-foreground">Website</dt><dd>{company.website || '—'}</dd></div>
            </dl>
          ) : (
            <p className="text-muted-foreground">Firmendaten konnten nicht geladen werden.</p>
          )}
        </CardContent>
      </Card>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        {[
          { icon: FileText, title: 'Verträge', hint: 'kommt in Kürze' },
          { icon: Briefcase, title: 'Projekte', hint: 'kommt in Kürze' },
          { icon: ShoppingCart, title: 'Aufträge', hint: 'kommt in Kürze' },
          { icon: MessageCircle, title: 'Chat', hint: 'kommt in Kürze' },
        ].map(({ icon: Icon, title, hint }) => (
          <Card key={title} className="opacity-60">
            <CardHeader><CardTitle className="flex items-center gap-2"><Icon className="h-5 w-5" />{title}</CardTitle></CardHeader>
            <CardContent><p className="text-sm text-muted-foreground">{hint}</p></CardContent>
          </Card>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/portal/page.tsx
git commit -m "feat(portal): dashboard with company read-only view + P2-P5 placeholders"
```

---

## Task 15: Accept-Invite-Seite

**Files:**
- Create: `src/app/portal/accept-invite/page.tsx`

- [ ] **Step 1: Seite**

```tsx
'use client'

import { Suspense, useState } from 'react'
import { useRouter, useSearchParams } from 'next/navigation'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Loader2 } from 'lucide-react'

function AcceptInviteForm() {
  const router = useRouter()
  const searchParams = useSearchParams()
  const token = searchParams.get('token') || ''
  const [password, setPassword] = useState('')
  const [confirm, setConfirm] = useState('')
  const [submitting, setSubmitting] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const submit = async (e: React.FormEvent) => {
    e.preventDefault()
    setError(null)
    if (password !== confirm) { setError('Passwörter stimmen nicht überein'); return }
    if (password.length < 10) { setError('Passwort muss mindestens 10 Zeichen haben'); return }
    setSubmitting(true)
    try {
      const res = await fetch('/api/v1/auth/accept-invite', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ token, password }),
      })
      const data = await res.json()
      if (data.success) {
        router.push(data.data?.redirectTo || '/portal')
      } else {
        setError(data.error?.message || 'Fehler beim Einlösen')
      }
    } catch {
      setError('Netzwerkfehler')
    } finally {
      setSubmitting(false)
    }
  }

  if (!token) {
    return <p className="text-sm text-destructive">Kein Token vorhanden. Bitte den Link aus der Einladungs-E-Mail verwenden.</p>
  }

  return (
    <form onSubmit={submit} className="space-y-4">
      <div className="space-y-2">
        <Label htmlFor="pw">Neues Passwort</Label>
        <Input id="pw" type="password" value={password} onChange={e => setPassword(e.target.value)} required minLength={10} autoFocus />
      </div>
      <div className="space-y-2">
        <Label htmlFor="pw2">Passwort bestätigen</Label>
        <Input id="pw2" type="password" value={confirm} onChange={e => setConfirm(e.target.value)} required minLength={10} />
      </div>
      {error && <p className="text-sm text-destructive">{error}</p>}
      <Button type="submit" className="w-full" disabled={submitting}>
        {submitting && <Loader2 className="h-4 w-4 mr-2 animate-spin" />}
        Zugang aktivieren
      </Button>
      <p className="text-xs text-muted-foreground">
        Mindestens 10 Zeichen mit je einem Buchstaben und einer Ziffer.
      </p>
    </form>
  )
}

export default function AcceptInvitePage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-muted/20 p-4">
      <Card className="w-full max-w-md">
        <CardHeader>
          <CardTitle>Kundenportal — Zugang aktivieren</CardTitle>
        </CardHeader>
        <CardContent>
          <Suspense fallback={<Loader2 className="h-6 w-6 animate-spin" />}>
            <AcceptInviteForm />
          </Suspense>
        </CardContent>
      </Card>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/portal/accept-invite/page.tsx
git commit -m "feat(portal): public accept-invite page with password set"
```

---

## Task 16: Admin-UI — Portal-Zugänge-Tab auf Firmen-Detail

**Files:**
- Create: `src/app/intern/(dashboard)/contacts/companies/[id]/_components/portal-users-tab.tsx`
- Modify: `src/app/intern/(dashboard)/contacts/companies/[id]/page.tsx`

- [ ] **Step 1: Tab-Komponente**

Create `src/app/intern/(dashboard)/contacts/companies/[id]/_components/portal-users-tab.tsx`:

```tsx
'use client'

import { useCallback, useEffect, useState } from 'react'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Badge } from '@/components/ui/badge'
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from '@/components/ui/dialog'
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
import { toast } from 'sonner'
import { Loader2, Plus, RefreshCw, Ban, CheckCircle2, Dice6 } from 'lucide-react'

interface PortalUser {
  id: string
  email: string
  firstName: string | null
  lastName: string | null
  status: string | null
  firstLoginAt: string | null
  hasPendingInvite: boolean
}

interface Props { companyId: string }

export function PortalUsersTab({ companyId }: Props) {
  const [users, setUsers] = useState<PortalUser[]>([])
  const [loading, setLoading] = useState(true)
  const [dialogOpen, setDialogOpen] = useState(false)
  const [tab, setTab] = useState<'password' | 'invite'>('invite')
  const [saving, setSaving] = useState(false)
  const [form, setForm] = useState({ firstName: '', lastName: '', email: '', password: '' })

  const load = useCallback(async () => {
    setLoading(true)
    try {
      const res = await fetch(`/api/v1/companies/${companyId}/portal-users`)
      const data = await res.json()
      if (data?.success) setUsers(data.data || [])
    } finally {
      setLoading(false)
    }
  }, [companyId])

  useEffect(() => { load() }, [load])

  const reset = () => setForm({ firstName: '', lastName: '', email: '', password: '' })

  const generatePassword = () => {
    const charset = 'ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789'
    let pw = ''
    const arr = new Uint8Array(16)
    crypto.getRandomValues(arr)
    for (const n of arr) pw += charset[n % charset.length]
    setForm(f => ({ ...f, password: pw }))
  }

  const submit = async () => {
    if (!form.firstName || !form.lastName || !form.email) { toast.error('Alle Felder ausfüllen'); return }
    if (tab === 'password' && form.password.length < 10) { toast.error('Passwort mindestens 10 Zeichen'); return }
    setSaving(true)
    try {
      const body = tab === 'password'
        ? { method: 'password', firstName: form.firstName, lastName: form.lastName, email: form.email, password: form.password }
        : { method: 'invite', firstName: form.firstName, lastName: form.lastName, email: form.email }
      const res = await fetch(`/api/v1/companies/${companyId}/portal-users`, {
        method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body),
      })
      const data = await res.json()
      if (data?.success) {
        toast.success(tab === 'invite' ? 'Einladung gesendet' : 'Portal-User angelegt')
        setDialogOpen(false); reset(); load()
      } else {
        toast.error(data?.error?.message || 'Anlegen fehlgeschlagen')
      }
    } finally { setSaving(false) }
  }

  const act = async (userId: string, action: 'deactivate' | 'reactivate' | 'resend_invite') => {
    const res = await fetch(`/api/v1/users/${userId}/portal-access`, {
      method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action }),
    })
    const data = await res.json()
    if (data?.success) { toast.success('Aktion erfolgreich'); load() }
    else toast.error(data?.error?.message || 'Fehler')
  }

  return (
    <Card>
      <CardHeader className="flex flex-row items-center justify-between">
        <CardTitle>Portal-Zugänge</CardTitle>
        <Button onClick={() => { reset(); setDialogOpen(true) }}>
          <Plus className="h-4 w-4 mr-2" /> Portal-User anlegen
        </Button>
      </CardHeader>
      <CardContent>
        {loading ? (
          <div className="text-center py-8 text-muted-foreground"><Loader2 className="h-5 w-5 mx-auto animate-spin" /></div>
        ) : users.length === 0 ? (
          <p className="text-sm text-muted-foreground">Noch keine Portal-Zugänge für diese Firma.</p>
        ) : (
          <div className="divide-y">
            {users.map(u => {
              const active = u.status === 'active'
              const pending = u.hasPendingInvite
              return (
                <div key={u.id} className="py-3 flex items-center gap-4">
                  <div className="flex-1 min-w-0">
                    <div className="font-medium">{`${u.firstName ?? ''} ${u.lastName ?? ''}`.trim() || u.email}</div>
                    <div className="text-sm text-muted-foreground">{u.email}</div>
                  </div>
                  <div className="flex items-center gap-2">
                    {pending && <Badge variant="outline">Eingeladen</Badge>}
                    {!active && <Badge variant="secondary">Deaktiviert</Badge>}
                    {active && !pending && <Badge>Aktiv</Badge>}
                  </div>
                  <div className="flex gap-1">
                    {pending && <Button variant="ghost" size="sm" onClick={() => act(u.id, 'resend_invite')} title="Invite erneut senden"><RefreshCw className="h-4 w-4" /></Button>}
                    {active ? (
                      <Button variant="ghost" size="sm" onClick={() => act(u.id, 'deactivate')} title="Deaktivieren"><Ban className="h-4 w-4 text-red-500" /></Button>
                    ) : (
                      <Button variant="ghost" size="sm" onClick={() => act(u.id, 'reactivate')} title="Aktivieren"><CheckCircle2 className="h-4 w-4 text-green-600" /></Button>
                    )}
                  </div>
                </div>
              )
            })}
          </div>
        )}
      </CardContent>

      <Dialog open={dialogOpen} onOpenChange={setDialogOpen}>
        <DialogContent>
          <DialogHeader><DialogTitle>Portal-User anlegen</DialogTitle></DialogHeader>
          <Tabs value={tab} onValueChange={v => setTab(v as 'password' | 'invite')} className="py-2">
            <TabsList className="grid w-full grid-cols-2">
              <TabsTrigger value="invite">Invite-Link (empfohlen)</TabsTrigger>
              <TabsTrigger value="password">Passwort direkt</TabsTrigger>
            </TabsList>
            <div className="space-y-3 py-3">
              <div className="grid grid-cols-2 gap-3">
                <div className="space-y-1"><Label>Vorname</Label><Input value={form.firstName} onChange={e => setForm({ ...form, firstName: e.target.value })} /></div>
                <div className="space-y-1"><Label>Nachname</Label><Input value={form.lastName} onChange={e => setForm({ ...form, lastName: e.target.value })} /></div>
              </div>
              <div className="space-y-1"><Label>E-Mail</Label><Input type="email" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} /></div>
              <TabsContent value="password" className="space-y-1 mt-0">
                <Label>Passwort (mind. 10 Zeichen)</Label>
                <div className="flex gap-2">
                  <Input type="text" value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} className="font-mono text-sm" />
                  <Button type="button" variant="outline" size="icon" onClick={generatePassword} title="Zufallspasswort"><Dice6 className="h-4 w-4" /></Button>
                </div>
                <p className="text-xs text-muted-foreground">Passwort wird dem Kunden manuell mitgeteilt.</p>
              </TabsContent>
              <TabsContent value="invite" className="mt-0">
                <p className="text-xs text-muted-foreground">Eine E-Mail mit einem 7 Tage gültigen Link geht raus. Der User setzt sein eigenes Passwort.</p>
              </TabsContent>
            </div>
          </Tabs>
          <DialogFooter>
            <Button variant="ghost" onClick={() => setDialogOpen(false)}>Abbrechen</Button>
            <Button onClick={submit} disabled={saving}>
              {saving && <Loader2 className="h-4 w-4 mr-2 animate-spin" />}
              Anlegen
            </Button>
          </DialogFooter>
        </DialogContent>
      </Dialog>
    </Card>
  )
}
```

- [ ] **Step 2: In Firmen-Detail-Seite einhängen**

Datei `src/app/intern/(dashboard)/contacts/companies/[id]/page.tsx` finden. Wenn Tabs existieren, einen neuen Tab-Eintrag "Portal-Zugänge" hinzufügen, der `<PortalUsersTab companyId={company.id} />` rendert. Wenn keine Tab-Struktur: am Ende der Detail-Content-Section als eigene Sektion einbinden.

Da die genaue Struktur der Company-Detail-Seite nicht 1:1 aus der Spec klar ist, beim Einhängen an vorhandene Muster orientieren (analog zum existierenden "Portal-Aktionen"-Tab, "Aktivitäten"-Tab, etc.).

- [ ] **Step 3: Typcheck**

Run: `npx tsc --noEmit 2>&1 | grep -E "portal-users-tab|companies/\[id\]/page" | head -5`
Expected: No output.

- [ ] **Step 4: Commit**

```bash
git add "src/app/intern/(dashboard)/contacts/companies/[id]/"
git commit -m "feat(portal): admin UI — Portal-Zugänge tab on company detail"
```

---

## Task 17: Integration-Test — End-to-End durch die API

**Files:**
- Create: `src/__tests__/integration/api/portal-invite-flow.test.ts`

- [ ] **Step 1: Integration-Test**

Dieser Test läuft gegen die laufende Dev-DB (oder eine Test-DB je nach Setup). Prüfen, welche Integration-Test-Infrastruktur im Repo etabliert ist (z.B. `src/__tests__/integration-real/` oder `src/__tests__/integration/`).

Skeleton:

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
import { UserService } from '@/lib/services/user.service'
import { db } from '@/lib/db'
import { users, companies } from '@/lib/db/schema'
import { eq } from 'drizzle-orm'

describe('Portal invite end-to-end', () => {
  let companyId: string
  let userId: string

  beforeAll(async () => {
    const [c] = await db.insert(companies).values({ name: 'Test GmbH ' + Date.now() }).returning()
    companyId = c.id
  })

  afterAll(async () => {
    if (userId) await db.delete(users).where(eq(users.id, userId))
    await db.delete(companies).where(eq(companies.id, companyId))
  })

  it('creates, invites, accepts, and permits login', async () => {
    // 1. Create with invite method
    const u = await UserService.createPortalUser({
      companyId, firstName: 'E2E', lastName: 'Test',
      email: `e2e-${Date.now()}@t.de`, method: 'invite',
    })
    userId = u.id
    expect(u.inviteToken).toBeTruthy()
    expect(u.inviteTokenExpiresAt).toBeTruthy()

    // 2. Accept invite
    const accepted = await UserService.acceptInvite(u.inviteToken!, 'Secure12345')
    expect(accepted.inviteToken).toBeNull()
    expect(accepted.firstLoginAt).toBeTruthy()

    // 3. Re-using the token must fail
    await expect(UserService.acceptInvite(u.inviteToken!, 'Secure12345')).rejects.toThrow()

    // 4. Duplicate create fails
    await expect(UserService.createPortalUser({
      companyId, firstName: 'E2E2', lastName: 'Dup',
      email: accepted.email, method: 'invite',
    })).rejects.toThrow(/bereits/i)
  })
})
```

- [ ] **Step 2: Test ausführen**

Run: `npx vitest run src/__tests__/integration/api/portal-invite-flow.test.ts`
Expected: all pass.

Falls Integration-Tests im Repo nicht automatisiert laufen (DB nicht verfügbar), diesen Test als "manuell ausführen"-Hinweis in der Task-Notiz notieren und den Task trotzdem abschließen.

- [ ] **Step 3: Commit**

```bash
git add src/__tests__/integration/api/portal-invite-flow.test.ts
git commit -m "test(portal): integration test for invite → accept → reuse-blocked flow"
```

---

## Task 18: Manual E2E + Deploy

**Files:** none

- [ ] **Step 1: Lokal testen**

1. App starten (`npm run dev`)
2. Als Admin einloggen auf `/intern`
3. Zur Firmen-Detailseite navigieren → Tab "Portal-Zugänge" öffnen
4. Button "Portal-User anlegen" → Tab "Invite-Link" → Vorname/Nachname/E-Mail (echte E-Mail verwenden oder MailHog lokal) → Anlegen
5. Task-Queue-Seite öffnen, Task manuell "Ausführen", prüfen dass Mail rausging
6. Link aus Mail (oder aus DB) aufrufen: `/portal/accept-invite?token=...`
7. Passwort setzen → Redirect auf `/portal`
8. Dashboard lädt mit Firmendaten
9. Logout → Login mit neuen Credentials → Redirect wieder auf `/portal`
10. Versuchen, `/intern` manuell in URL zu öffnen → 403

- [ ] **Step 2: Deploy**

```bash
git push
```

Warten bis Deploy durch ist. Prüfen dass Migration 008 gelaufen ist (DB-Check):
```sql
SELECT column_name FROM information_schema.columns WHERE table_name = 'users' AND column_name IN ('company_id','invite_token','invite_token_expires_at','first_login_at');
```
Erwartung: 4 Zeilen.

- [ ] **Step 3: Final-Commit falls nichts mehr zu tun**

Optional: kurzer Status-Commit / Empty-Commit als Marker. Im Regelfall nicht nötig.

---

## Self-Review Notes

**Spec-Coverage-Check**: Alle Spec-Abschnitte adressiert:
- 3.1 Schema → Task 1
- 3.2 Rolle → Task 1 (SQL) + Task 2 (TS-Types)
- 3.3 Migration → Task 1
- 4. Admin-UX → Task 16
- 5.1-5.3 Portal-UX → Tasks 13, 14, 15
- 6. Access-Gating → Task 5 (Middleware) + Task 4 (withPortalAuth) + Task 3 (JWT)
- 7. API-Routes → Tasks 8, 9, 10, 12 + Login 11
- 8. Sicherheit → Task 7 (Token-Gen), Task 10 (Rate-Limit), Task 7 (Duplicate-Check)
- 9. Audit → wird in den Service/Route-Implementierungen ergänzt (siehe Note unten)
- 10. Deaktivierung → Task 9
- 11. Testing → Tasks 3, 4, 7, 17
- 12. Scope-Grenzen → respektiert

**Gap**: Spec §9 (Audit-Trail) erfordert explizite `auditLog`-Einträge in mehreren Operationen. Die einzelnen API-Task-Implementierungen sollten diese direkt miteinbauen (z.B. `portal_user.created` in Task 8 nach Insert, `portal_user.invite_accepted` in Task 10 nach Erfolg). Diese ergänzenden Audit-Writes sind im obigen Plan in den Task-Code **nicht explizit eingezeichnet** — der implementierende Agent soll sie nach dem Vorbild anderer Audit-Schreibstellen im Repo ergänzen (siehe z.B. `src/lib/auth/session.ts`-Login-Logs, `src/lib/services/auditLog.service.ts` falls vorhanden).

Empfehlung: bei Implementierung von Task 7, 8, 9, 10 jeweils **einen zusätzlichen Schritt** "Audit-Log-Eintrag schreiben" einfügen und committen. Das ist zwar vor TDD-Purity-Sicht ein Nachzügler, aber pragmatisch akzeptabel für P1.

**Placeholders**: Keine "TODO"/"TBD" im Plan außer den expliziten "kommt in Kürze"-UI-Texten (die sind gewollt — Platzhalter für P2-P5 im Dashboard).

**Type-Consistency**: `PortalAuthContext` (Task 4) und Usage in Task 12 stimmen überein (`auth.companyId`, `auth.userId`). `CreatePortalUserInput` (Task 7) matcht die API-Zod-Schema-Discriminated-Union (Task 8).
