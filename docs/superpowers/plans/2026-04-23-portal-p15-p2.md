# Portal-P1.5 + P2 Implementation Plan

> **Für Agenten:** REQUIRED SUB-SKILL — superpowers:subagent-driven-development (empfohlen) oder superpowers:executing-plans.

**Goal:** Audit-Foundation + Retrofit P1 + Task-Queue-Retry + Test-Cleanup (Phase 1.5), danach Firmendaten-Antrags-Workflow im Portal (Phase 2).

**Spec:** `docs/superpowers/specs/2026-04-23-portal-p2-design.md`

---

## Phase 1.5 — Foundation Sweep

### Task 1.5-1: AuditLog-Schema + Migration 009 + Service

**Files:**
- Modify: `src/lib/db/schema.ts` — neue Tabelle `audit_logs`
- Create: `src/lib/db/migrations/009_audit_logs.sql`
- Modify: `src/lib/db/migrations/index.ts`
- Modify: `docker/app/entrypoint.prod.sh`
- Create: `src/lib/services/audit-log.service.ts`
- Create: `src/__tests__/unit/services/audit-log.service.test.ts`

- [ ] **Step 1: Drizzle-Schema**

In `src/lib/db/schema.ts` nach dem `users`-Block (oder strukturell passender Stelle) eine neue Tabelle:

```ts
export const auditLogs = pgTable('audit_logs', {
  id: uuid('id').primaryKey().defaultRandom(),
  userId: uuid('user_id').references(() => users.id, { onDelete: 'set null' }),
  userRole: varchar('user_role', { length: 50 }),
  action: varchar('action', { length: 100 }).notNull(),
  entityType: varchar('entity_type', { length: 50 }),
  entityId: uuid('entity_id'),
  payload: jsonb('payload').notNull().default({}),
  ipAddress: varchar('ip_address', { length: 45 }),
  userAgent: text('user_agent'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
}, (table) => [
  index('idx_audit_logs_user_id').on(table.userId, table.createdAt),
  index('idx_audit_logs_entity').on(table.entityType, table.entityId, table.createdAt),
  index('idx_audit_logs_action').on(table.action, table.createdAt),
])

export type AuditLog = typeof auditLogs.$inferSelect
export type NewAuditLog = typeof auditLogs.$inferInsert
```

(`text` ist bereits in Schema-Imports enthalten; prüfen und sonst nachziehen.)

- [ ] **Step 2: Migration 009_audit_logs.sql**

```sql
-- ============================================================
-- Migration 009: audit_logs — revisionssichere Aktions-Historie
-- Idempotent.
-- ============================================================

CREATE TABLE IF NOT EXISTS audit_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE SET NULL,
  user_role VARCHAR(50),
  action VARCHAR(100) NOT NULL,
  entity_type VARCHAR(50),
  entity_id UUID,
  payload JSONB NOT NULL DEFAULT '{}',
  ip_address VARCHAR(45),
  user_agent TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_audit_logs_user_id ON audit_logs(user_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_audit_logs_entity ON audit_logs(entity_type, entity_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_audit_logs_action ON audit_logs(action, created_at DESC);
```

- [ ] **Step 3: Registry in migrations/index.ts** — Eintrag `009_audit_logs.sql`, Description "Revisionssichere Audit-Trail (audit_logs + Indexe)".

- [ ] **Step 4: entrypoint.prod.sh** — Gleichen DDL-Block in pre-Drizzle (nach dem `008_portal_users`-Block).

- [ ] **Step 5: AuditLogService**

Create `src/lib/services/audit-log.service.ts`:

```ts
import { NextRequest } from 'next/server'
import { db } from '@/lib/db'
import { auditLogs } from '@/lib/db/schema'
import type { AuditLog } from '@/lib/db/schema'
import { eq, and, desc } from 'drizzle-orm'
import { logger } from '@/lib/utils/logger'

export interface AuditLogInput {
  userId?: string | null
  userRole?: string | null
  action: string
  entityType?: string | null
  entityId?: string | null
  payload?: Record<string, unknown>
  request?: NextRequest
}

function extractIp(req: NextRequest | undefined): string | null {
  if (!req) return null
  const fwd = req.headers.get('x-forwarded-for')?.split(',')[0]?.trim()
  if (fwd) return fwd
  return req.headers.get('x-real-ip') || null
}

export const AuditLogService = {
  /**
   * Write an audit-log entry. Fail-safe by default (throws on DB failure so callers can 500).
   * For login flows, wrap in try/catch at the caller to avoid blocking auth on audit DB outage.
   */
  async log(input: AuditLogInput): Promise<void> {
    try {
      await db.insert(auditLogs).values({
        userId: input.userId ?? null,
        userRole: input.userRole ?? null,
        action: input.action,
        entityType: input.entityType ?? null,
        entityId: input.entityId ?? null,
        payload: input.payload ?? {},
        ipAddress: extractIp(input.request),
        userAgent: input.request?.headers.get('user-agent') ?? null,
      })
    } catch (error) {
      logger.error('AuditLog write failed', error, { module: 'AuditLogService', action: input.action })
      throw error
    }
  },

  async list(filter: {
    userId?: string
    entityType?: string
    entityId?: string
    action?: string
    limit?: number
    offset?: number
  }): Promise<AuditLog[]> {
    const conditions = []
    if (filter.userId) conditions.push(eq(auditLogs.userId, filter.userId))
    if (filter.entityType) conditions.push(eq(auditLogs.entityType, filter.entityType))
    if (filter.entityId) conditions.push(eq(auditLogs.entityId, filter.entityId))
    if (filter.action) conditions.push(eq(auditLogs.action, filter.action))

    return db
      .select()
      .from(auditLogs)
      .where(conditions.length > 0 ? and(...conditions) : undefined)
      .orderBy(desc(auditLogs.createdAt))
      .limit(filter.limit ?? 100)
      .offset(filter.offset ?? 0)
  },
}
```

- [ ] **Step 6: Unit-Test**

Create `src/__tests__/unit/services/audit-log.service.test.ts` — Tests: `log()` success, `log()` throws on DB-failure, `list()` filters by userId/entityType/action (use `setupDbMock()`).

- [ ] **Step 7: Test + typecheck + commit**

```bash
npx vitest run src/__tests__/unit/services/audit-log.service.test.ts
npx tsc --noEmit 2>&1 | grep -E "audit-log|audit_logs" | head -5
git add src/lib/db/schema.ts src/lib/db/migrations/009_audit_logs.sql src/lib/db/migrations/index.ts docker/app/entrypoint.prod.sh src/lib/services/audit-log.service.ts src/__tests__/unit/services/audit-log.service.test.ts
git commit -m "feat(audit): audit_logs table + AuditLogService for revisionssichere Action-Trail"
```

---

### Task 1.5-2: Retrofit Login/Logout/AcceptInvite mit Audit

**Files:**
- Modify: `src/app/api/v1/auth/login/route.ts`
- Modify: `src/app/api/v1/auth/logout/route.ts`
- Modify: `src/app/api/v1/auth/accept-invite/route.ts`

- [ ] **Step 1: Login-Audit**

In `login/route.ts`, direkt vor dem `return apiSuccess(...)`: AuditLog-Eintrag schreiben.

```ts
// Action slug abhängig von Rolle
const action = result.user.role === 'portal_user' ? 'portal_user.login' : 'internal.login'

// Login nicht blockieren wenn Audit fehlschlägt — try/catch explizit hier
try {
  await AuditLogService.log({
    userId: result.user.id,
    userRole: result.user.role,
    action,
    entityType: 'user',
    entityId: result.user.id,
    request,
  })
} catch (err) {
  logger.error('Login audit write failed (non-blocking)', err, { module: 'AuthLogin' })
}
```

- [ ] **Step 2: Logout-Audit**

`logout/route.ts` muss den NextRequest annehmen (ist aktuell arg-los). Und vor `deleteSession()` oder direkt danach die aktive Session abfragen um User-Info zu bekommen, dann AuditLog-Eintrag + session löschen.

Reihenfolge: session holen → user-Info merken → log (fail-safe try/catch) → session löschen → respond.

```ts
import { NextRequest } from 'next/server'
import { getSession, deleteSession } from '@/lib/auth/session'
import { AuditLogService } from '@/lib/services/audit-log.service'
import { apiSuccess } from '@/lib/utils/api-response'
import { logger } from '@/lib/utils/logger'

export async function POST(request: NextRequest) {
  const session = await getSession()
  if (session) {
    const action = session.user.role === 'portal_user' ? 'portal_user.logout' : 'internal.logout'
    try {
      await AuditLogService.log({
        userId: session.user.id,
        userRole: session.user.role,
        action,
        entityType: 'user',
        entityId: session.user.id,
        request,
      })
    } catch (err) {
      logger.error('Logout audit write failed (non-blocking)', err, { module: 'AuthLogout' })
    }
  }
  await deleteSession()
  return apiSuccess({ message: 'Logged out successfully' })
}
```

- [ ] **Step 3: AcceptInvite-Audit**

In `accept-invite/route.ts`, nach erfolgreichem `createSession`:

```ts
try {
  await AuditLogService.log({
    userId: user.id,
    userRole: user.role,
    action: 'portal_user.invite_accepted',
    entityType: 'user',
    entityId: user.id,
    request,
  })
} catch (err) {
  logger.error('Accept-invite audit write failed', err, { module: 'AcceptInviteAPI' })
}
```

- [ ] **Step 4: Typecheck + Commit**

```bash
npx tsc --noEmit 2>&1 | grep -E "login/route|logout/route|accept-invite/route" | head -5
git add "src/app/api/v1/auth/login/route.ts" "src/app/api/v1/auth/logout/route.ts" "src/app/api/v1/auth/accept-invite/route.ts"
git commit -m "feat(audit): log login/logout/accept-invite events for both portal and internal users"
```

---

### Task 1.5-3: Retrofit Portal-User-Management mit Audit

**Files:**
- Modify: `src/app/api/v1/companies/[id]/portal-users/route.ts` (POST)
- Modify: `src/app/api/v1/users/[id]/portal-access/route.ts` (PATCH)

- [ ] **Step 1: Portal-User-Create Audit**

In `portal-users/route.ts` POST, nach dem erfolgreichen `UserService.createPortalUser(...)`:

```ts
await AuditLogService.log({
  userId: auth.userId,  // der admin — auth aus withPermission
  userRole: auth.role,
  action: 'admin.portal_user.created',
  entityType: 'user',
  entityId: user.id,
  payload: { companyId, email: user.email, method: data.method },
  request,
})
```

(`auth` aus der withPermission-Handler-Signatur; prüfen ob der Name `auth` tatsächlich dispatcht ist.)

- [ ] **Step 2: Portal-Access-Actions Audit**

In `portal-access/route.ts` PATCH, in JEDEM der drei action-Branches:

```ts
// deactivate
await AuditLogService.log({
  userId: auth.userId,
  userRole: auth.role,
  action: 'admin.portal_user.deactivated',
  entityType: 'user',
  entityId: id,
  payload: { previousStatus: user.status },
  request,
})

// reactivate
await AuditLogService.log({
  userId: auth.userId,
  userRole: auth.role,
  action: 'admin.portal_user.reactivated',
  entityType: 'user',
  entityId: id,
  payload: { previousStatus: user.status },
  request,
})

// resend_invite
await AuditLogService.log({
  userId: auth.userId,
  userRole: auth.role,
  action: 'admin.portal_user.invite_resent',
  entityType: 'user',
  entityId: id,
  request,
})
```

Alle drei: vor dem apiSuccess, mit auth aus withPermission's Handler-Arg. Falls die Routes aktuell `async () =>` ohne `auth` schreiben, Handler-Sig auf `async (auth) =>` ändern.

- [ ] **Step 3: Typecheck + Commit**

```bash
npx tsc --noEmit 2>&1 | grep -E "portal-users/route|portal-access/route" | head -5
git add "src/app/api/v1/companies/[id]/portal-users/route.ts" "src/app/api/v1/users/[id]/portal-access/route.ts"
git commit -m "feat(audit): log admin portal-user create/deactivate/reactivate/invite-resent"
```

---

### Task 1.5-4: Task-Queue-Retry API + UI

**Files:**
- Modify: `src/lib/services/task-queue.service.ts` — `retry(id)` Methode
- Create: `src/app/api/v1/task-queue/[id]/retry/route.ts`
- Modify: `src/app/intern/(dashboard)/settings/task-queue/page.tsx` — Retry-Button auf Fehler-Zeilen

- [ ] **Step 1: TaskQueueService.retry**

Neue Methode:

```ts
async retry(id: string): Promise<TaskQueueItem | null> {
  const [item] = await db
    .update(taskQueue)
    .set({
      status: 'pending',
      errorMessage: null,
      scheduledFor: new Date(),
      updatedAt: new Date(),
    })
    .where(and(eq(taskQueue.id, id), eq(taskQueue.status, 'failed')))
    .returning()
  return item ?? null
}
```

(Nur Tasks mit Status `failed` sind retry-fähig — pending/running nicht. Prüfen: heißt das Status-Feld tatsächlich `failed` oder `error`? Vor dem Coden in schema.ts verifizieren und anpassen.)

- [ ] **Step 2: Route**

Create `src/app/api/v1/task-queue/[id]/retry/route.ts`:

```ts
import { NextRequest } from 'next/server'
import { apiSuccess, apiError, apiNotFound } from '@/lib/utils/api-response'
import { withPermission } from '@/lib/auth/require-permission'
import { TaskQueueService } from '@/lib/services/task-queue.service'
import { logger } from '@/lib/utils/logger'

type Params = Promise<{ id: string }>

export async function POST(request: NextRequest, { params }: { params: Params }) {
  return withPermission(request, 'settings', 'update', async () => {
    const { id } = await params
    try {
      const item = await TaskQueueService.retry(id)
      if (!item) return apiNotFound('Task nicht gefunden oder nicht im Fehler-Status')
      return apiSuccess({ status: item.status })
    } catch (error) {
      logger.error('Task-Queue retry failed', error, { module: 'TaskQueueAPI' })
      return apiError('RETRY_FAILED', 'Retry fehlgeschlagen', 500)
    }
  })
}
```

- [ ] **Step 3: UI Retry-Button**

In `task-queue/page.tsx`: bei Task-Zeilen mit Status `failed` einen `RotateCw`-Icon-Button einblenden, der `POST /api/v1/task-queue/{id}/retry` auslöst + Toast + reload.

- [ ] **Step 4: Typecheck + Commit**

```bash
npx tsc --noEmit 2>&1 | grep -E "task-queue" | head -5
git add src/lib/services/task-queue.service.ts "src/app/api/v1/task-queue/[id]/retry/route.ts" "src/app/intern/(dashboard)/settings/task-queue/page.tsx"
git commit -m "feat(task-queue): retry-button for failed tasks (resets to pending + clears error)"
```

---

### Task 1.5-5: Test-Cleanup — tenant_id-Leichen

**Files:**
- Modify/Delete: diverse in `src/__tests__/` (s.u.)

- [ ] **Step 1: Liste der Problem-Dateien erstellen**

Run: `npx tsc --noEmit 2>&1 | grep -E "tenantId|tenants'" | cut -d'(' -f1 | sort -u | head -20`

Erwartung: ca. 10 Test-Dateien mit toten `tenantId`-Referenzen (permission-checks, tenant-isolation, crud/companies, auth-flow, fixtures, mock-request, api-key.test, auth-context.test, test-db.ts etc.).

- [ ] **Step 2: Je Datei entscheiden**

Für jede Datei:
- Wenn die Datei rein auf Multi-Tenant-Verhalten testet (z.B. `tenant-isolation.test.ts`): **löschen** (obsolete).
- Wenn sie einen nützlichen Test enthält: `tenantId`-Props entfernen und/oder durch aktuelle API-Signatur ersetzen.
- `setup/test-db.ts`: `schema.tenants` → `schema.organization` migrieren oder den Helper auf Singleton umstellen.

Nicht-destruktive Entscheidung: Zuerst READ-ONLY-Durchlauf, Agent macht Vorschläge, User entscheidet bei kritischen Dateien.

- [ ] **Step 3: Änderungen durchführen**

Subagent-geführt: pro Datei einen Edit + typecheck der Datei danach.

- [ ] **Step 4: Full-Typecheck final clean**

`npx tsc --noEmit 2>&1 | grep -E "__tests__" | head -5` → expect no output.

- [ ] **Step 5: Commit**

```bash
git add src/__tests__/
git commit -m "chore(tests): remove tenant_id relics from integration + unit tests (post single-tenant migration)"
```

---

## Phase 2 — Portal P2: Firmendaten-Antrag

### Task P2-1: Schema + Migration 010

**Files:**
- Modify: `src/lib/db/schema.ts` — Tabelle `companyChangeRequests`
- Create: `src/lib/db/migrations/010_company_change_requests.sql`
- Modify: `src/lib/db/migrations/index.ts`
- Modify: `docker/app/entrypoint.prod.sh`

- [ ] **Step 1: Drizzle-Schema**

```ts
export const companyChangeRequests = pgTable('company_change_requests', {
  id: uuid('id').primaryKey().defaultRandom(),
  companyId: uuid('company_id').notNull().references(() => companies.id, { onDelete: 'cascade' }),
  requestedBy: uuid('requested_by').notNull().references(() => users.id, { onDelete: 'set null' }),
  requestedAt: timestamp('requested_at', { withTimezone: true }).notNull().defaultNow(),
  proposedChanges: jsonb('proposed_changes').notNull(),
  status: varchar('status', { length: 20 }).notNull().default('pending'),
  reviewedBy: uuid('reviewed_by').references(() => users.id, { onDelete: 'set null' }),
  reviewedAt: timestamp('reviewed_at', { withTimezone: true }),
  reviewComment: text('review_comment'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
}, (table) => [
  index('idx_ccr_company').on(table.companyId, table.status, table.requestedAt),
  index('idx_ccr_status').on(table.status, table.requestedAt),
])
```

- [ ] **Step 2: Migration 010 SQL** (Tabelle + Indexe, idempotent)
- [ ] **Step 3: Registry + entrypoint.prod.sh**
- [ ] **Step 4: Typecheck + Commit**

```bash
git commit -m "feat(portal): company_change_requests schema + migration 010"
```

---

### Task P2-2: ChangeRequestService + Tests

**Files:**
- Create: `src/lib/services/company-change-request.service.ts`
- Create: `src/__tests__/unit/services/company-change-request.service.test.ts`

Methoden:
- `create(companyId, requestedBy, proposedChanges)` — 409-Error wenn bereits pending für companyId existiert.
- `list({ companyId?, status?, requestedBy?, limit?, offset? })`
- `getById(id)`
- `cancel(id, requestedBy)` — nur eigener pending request darf zurückgezogen werden (Delete Row).
- `approve(id, reviewedBy)` — apply `proposedChanges` via `CompanyService.update(companyId, diff)`, dann Update auf `status='approved', reviewedBy, reviewedAt=now`. In einer Transaktion oder mit compensation-Logik, falls Transaktionen im Projekt nicht üblich sind (Drizzle → check `db.transaction`).
- `reject(id, reviewedBy, reviewComment)` — Update Status.

Tests: create/list/cancel/approve/reject; duplicate-pending-rejection; approve-applies-diff; reject-requires-comment.

- [ ] Commit: `feat(portal): CompanyChangeRequestService (create/approve/reject/cancel)`

---

### Task P2-3: Portal-APIs (Create, List, Cancel)

**Files:**
- Create: `src/app/api/v1/portal/me/company/change-request/route.ts` (POST)
- Create: `src/app/api/v1/portal/me/company/change-requests/route.ts` (GET)
- Create: `src/app/api/v1/portal/me/company/change-request/[id]/route.ts` (DELETE)

Alle mit `withPortalAuth`. CompanyId aus Auth-Context, nicht aus Body. Jeder Write mit AuditLog (`portal.company_change_requested`, `portal.company_change_request_cancelled`).

Zod-Schema für `proposedChanges` aus Design-Doc (`.strict()`).

- [ ] Commit: `feat(portal): portal APIs for company change requests (create/list/cancel + audit)`

---

### Task P2-4: Admin-APIs (List, Approve, Reject)

**Files:**
- Create: `src/app/api/v1/companies/[id]/change-requests/route.ts` (GET) — `companies:read`
- Create: `src/app/api/v1/portal/change-requests/route.ts` (GET) — globale Liste, `users:update`
- Create: `src/app/api/v1/portal/change-requests/[id]/approve/route.ts` (POST) — `users:update`
- Create: `src/app/api/v1/portal/change-requests/[id]/reject/route.ts` (POST) — `users:update`, body `{ reviewComment }`

Beide Mutations mit AuditLog (`admin.company_change_request.approved` mit angewandtem Diff im payload, `admin.company_change_request.rejected` mit Kommentar im payload).

- [ ] Commit: `feat(portal): admin APIs for change-request queue (approve/reject + audit)`

---

### Task P2-5: Portal-UI

**Files:**
- Create: `src/app/portal/company/page.tsx` — Edit-Formular
- Create: `src/app/portal/company/requests/page.tsx` — eigene Anträge-Liste
- Modify: `src/app/portal/page.tsx` — Dashboard-Link zum Formular, Status-Banner wenn pending existiert
- Modify: `src/app/portal/layout.tsx` — Nav-Link zu "Firmendaten" und "Meine Anträge" (optional)

Formular: pre-befüllt mit aktuellen portal-safe Feldern. Diff-Berechnung clientseitig (nur geänderte Felder). Submit → POST change-request. Bei pending: Banner "Antrag läuft, Änderungen deaktiviert".

- [ ] Commit: `feat(portal): portal UI — edit form + requests history + dashboard banner`

---

### Task P2-6: Admin-UI

**Files:**
- Create: `src/app/intern/(dashboard)/contacts/companies/[id]/_components/change-requests-tab.tsx`
- Modify: `src/app/intern/(dashboard)/contacts/companies/[id]/page.tsx` — Tab einhängen
- Create: `src/app/intern/(dashboard)/portal/change-requests/page.tsx` — globale Queue
- Modify: Nav-Struktur unter `/intern/(dashboard)/layout.tsx` oder entsprechendem Sidebar-Menu-File — neuer Menu-Eintrag "Portal-Anträge"

Beide Views zeigen: Firma, Kunde, Diff-Darstellung (alt → neu pro Feld), Approve/Reject-Dialog (Reject mit Kommentar-Textarea).

- [ ] Commit: `feat(portal): admin UI — change-requests tab on company detail + global queue page`

---

### Task P2-7: E-Mail-Templates (optional)

**Files:**
- Modify: `src/lib/services/email-template.service.ts` — zwei neue Templates in `DEFAULT_EMAIL_TEMPLATES`

Templates:
- `portal_change_request_admin` — an Admin bei neuem Submit
- `portal_change_request_decision` — an Portal-User bei Approve/Reject

Task-Queue-Email via `TaskQueueService.create` in den Portal-API / Admin-API-Handlern (wie bei portal_invite).

- [ ] Commit: `feat(portal): email templates for change-request lifecycle notifications`

---

### Task P2-8: Integration-Test + Manual E2E

**Files:**
- Create: `src/__tests__/integration-real/company-change-request-flow.test.ts`

Test-Szenario: Portal-User submitted Diff → Admin sieht → Admin approved → `companies` hat neuen Wert → Audit-Log hat beide Einträge → zweiter Submit (neuer Diff) → Reject mit Kommentar.

Manual-E2E-Checklist in Task-Notiz.

- [ ] Commit: `test(portal): integration test for company change-request flow`

---

## Self-Review Checklist (vor Deploy)

- [ ] Audit-Logs werden in ALLEN ändernden Portal-Endpoints + Login/Logout geschrieben
- [ ] `audit_logs` Tabelle hat Index auf (userId, createdAt DESC) + (entityType, entityId)
- [ ] Portal-User kann `name` der Firma nicht ändern (strict zod schema)
- [ ] Duplicate-pending-Check in CompanyChangeRequestService.create
- [ ] Approve appliziert NUR den Diff (keine unerwarteten Firmen-Felder)
- [ ] Reject erzwingt `reviewComment` (zod min length 1)
- [ ] Cancel erlaubt nur eigener pending Request (companyId + requestedBy check)
- [ ] Task-Queue-Retry nur für Status `failed`
- [ ] Test-typecheck clean nach Cleanup
- [ ] Admin-Queue-Link unter `/intern/(dashboard)/portal/change-requests` und als Tab auf Firma
- [ ] ZMigrationen 009 + 010 in prod-entrypoint gespiegelt
