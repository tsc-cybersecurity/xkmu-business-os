---
phase: 04-ui-deliverables-sop-erweiterung
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/app/intern/(dashboard)/management/sops/page.tsx
  - src/components/management/execution-log-panel.tsx
  - "src/app/intern/(dashboard)/management/sops/[id]/page.tsx"
  - "src/app/intern/(dashboard)/management/deliverables/[id]/page.tsx"
autonomous: true
requirements: [UI-02, EXEC-04]

must_haves:
  truths:
    - "SOP-Listenseite hat Filter fuer automation_level (Manuell/Semi-Auto/Vollautomatisch)"
    - "SOP-Listenseite hat Filter fuer maturity_level (1-5)"
    - "SOP-Karte zeigt automation_level und maturity_level als Badges wenn vorhanden"
    - "SOP-Detailseite zeigt Execution-Log-Tab mit bisherigen Ausfuehrungen"
    - "Deliverable-Detailseite zeigt Execution-Log-Sektion mit bisherigen Ausfuehrungen"
    - "Execution-Log zeigt: Datum, executed_by, Status, Duration, Quality-Score"
  artifacts:
    - path: "src/app/intern/(dashboard)/management/sops/page.tsx"
      provides: "Erweiterte SOP-Listenseite mit automation_level- und maturity_level-Filter"
      contains: "automation_level"
    - path: "src/components/management/execution-log-panel.tsx"
      provides: "Wiederverwendbare Execution-Log-Komponente fuer SOP- und Deliverable-Detailseiten"
      exports: ["ExecutionLogPanel"]
      min_lines: 60
    - path: "src/app/intern/(dashboard)/management/sops/[id]/page.tsx"
      provides: "SOP-Detailseite mit Execution-Log-Tab"
      contains: "ExecutionLogPanel"
    - path: "src/app/intern/(dashboard)/management/deliverables/[id]/page.tsx"
      provides: "Deliverable-Detailseite mit Execution-Log-Sektion"
      contains: "ExecutionLogPanel"
  key_links:
    - from: "sops/page.tsx"
      to: "/api/v1/sops"
      via: "fetch mit automation_level und maturity_level Query-Params"
      pattern: "automation_level|maturity_level"
    - from: "ExecutionLogPanel"
      to: "/api/v1/execution-logs"
      via: "fetch mit entity_type und entity_id Query-Params"
      pattern: "api/v1/execution-logs.*entity"
    - from: "sops/[id]/page.tsx"
      to: "ExecutionLogPanel"
      via: "import und Einbindung in Execution-Tab"
      pattern: "ExecutionLogPanel"
    - from: "deliverables/[id]/page.tsx"
      to: "ExecutionLogPanel"
      via: "import und Einbindung in Seite"
      pattern: "ExecutionLogPanel"
---

<objective>
SOP-Listenfilter erweitern und Execution-Log-Anzeige auf beiden Detailseiten integrieren.

Purpose: Nutzer koennen die 93 SOPs nach Automatisierungsgrad und Reifegrad filtern. Die Ausfuehrungshistorie zeigt, wann eine SOP oder ein Deliverable zuletzt ausgefuehrt wurde und mit welchem Ergebnis.
Output: Erweiterte sops/page.tsx + wiederverwendbare ExecutionLogPanel-Komponente + Integration in beide Detailseiten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Bestehende Muster
@src/app/intern/(dashboard)/management/sops/page.tsx
@src/lib/constants/framework.ts

# Vorherige Plaene (fuer Kontext der schon aendbaren Dateien)
@.planning/phases/04-ui-deliverables-sop-erweiterung/04-02-SUMMARY.md

<interfaces>
<!-- API-Kontrakte aus Phase 2 -->

GET /api/v1/sops
Query params (bestehend): category, status, q
Query params (NEU, Phase 1 Felder): automation_level ('manual'|'semi'|'full'), maturity_level (1-5)
Response: { success: true, data: SOP[] }
Jede SOP hat jetzt: automation_level, maturity_level, source_task_id (nullable)

GET /api/v1/execution-logs
Query params: entity_type ('sop'|'deliverable'), entity_id (string), page, limit
Response: {
  success: true,
  data: {
    id: string
    entity_type: 'sop' | 'deliverable'
    entity_id: string
    entity_version: string | null
    started_at: string        // ISO timestamp
    completed_at: string | null
    executed_by: 'agent' | 'human'
    status: 'completed' | 'aborted' | 'escalated'
    abort_reason: string | null
    quality_score: number | null   // 0-100
    duration_minutes: number | null
    cost_estimate_usd: number | null
    human_approved: boolean | null
  }[],
  total: number
}

<!-- Bestehende Konstantan aus framework.ts -->
export const AUTOMATION_LEVEL_ENUM = ['manual', 'semi', 'full'] as const
export const EXECUTION_STATUS_ENUM = ['completed', 'aborted', 'escalated'] as const
export const EXECUTED_BY_ENUM = ['agent', 'human'] as const
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExecutionLogPanel-Komponente erstellen</name>
  <files>src/components/management/execution-log-panel.tsx</files>
  <action>
Erstelle den Ordner `src/components/management/` (sofern nicht vorhanden) und die Datei
`execution-log-panel.tsx`. Diese Komponente ist wiederverwendbar fuer SOP- und Deliverable-Detailseiten.

```tsx
'use client'

import { useEffect, useState, useCallback } from 'react'
import { Card, CardContent } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Loader2, History, CheckCircle2, XCircle, AlertTriangle, ChevronDown } from 'lucide-react'

interface ExecutionLogPanelProps {
  entityType: 'sop' | 'deliverable'
  entityId: string
}

const STATUS_CONFIG: Record<string, { label: string; icon: typeof CheckCircle2; color: string }> = {
  completed: { label: 'Abgeschlossen', icon: CheckCircle2, color: 'text-green-600' },
  aborted:   { label: 'Abgebrochen',   icon: XCircle,      color: 'text-red-500'   },
  escalated: { label: 'Eskaliert',     icon: AlertTriangle, color: 'text-orange-500' },
}

const EXECUTOR_LABELS: Record<string, string> = {
  agent: 'Agent', human: 'Mensch'
}

export function ExecutionLogPanel({ entityType, entityId }: ExecutionLogPanelProps) {
  const [logs, setLogs] = useState<any[]>([])
  const [loading, setLoading] = useState(true)
  const [total, setTotal] = useState(0)
  const [page, setPage] = useState(1)
  const PAGE_SIZE = 10

  const load = useCallback(async (p = 1) => {
    setLoading(true)
    const params = new URLSearchParams({
      entity_type: entityType,
      entity_id: entityId,
      page: String(p),
      limit: String(PAGE_SIZE),
    })
    const res = await fetch(`/api/v1/execution-logs?${params}`)
    const d = await res.json()
    if (d.success) {
      setLogs(prev => p === 1 ? d.data : [...prev, ...d.data])
      setTotal(d.total ?? 0)
    }
    setLoading(false)
  }, [entityType, entityId])

  useEffect(() => { load(1) }, [load])

  const loadMore = () => {
    const next = page + 1
    setPage(next)
    load(next)
  }

  const hasMore = logs.length < total

  if (loading && logs.length === 0) return (
    <div className="flex items-center justify-center py-8">
      <Loader2 className="h-6 w-6 animate-spin text-muted-foreground" />
    </div>
  )

  return (
    <div className="space-y-3">
      <div className="flex items-center justify-between">
        <h3 className="text-sm font-semibold flex items-center gap-2">
          <History className="h-4 w-4" />
          Ausfuehrungshistorie
          <Badge variant="outline">{total}</Badge>
        </h3>
      </div>

      {logs.length === 0 ? (
        <Card>
          <CardContent className="py-8 text-center text-muted-foreground">
            Noch keine Ausfuehrungen protokolliert
          </CardContent>
        </Card>
      ) : (
        <>
          <div className="space-y-2">
            {logs.map(log => {
              const statusCfg = STATUS_CONFIG[log.status] || STATUS_CONFIG.completed
              const StatusIcon = statusCfg.icon
              const startedAt = new Date(log.started_at)
              return (
                <Card key={log.id}>
                  <CardContent className="py-3">
                    <div className="flex items-start justify-between gap-3">
                      <div className="flex items-center gap-2 min-w-0">
                        <StatusIcon className={`h-4 w-4 shrink-0 ${statusCfg.color}`} />
                        <div>
                          <p className="text-sm font-medium">{statusCfg.label}</p>
                          <p className="text-xs text-muted-foreground">
                            {startedAt.toLocaleDateString('de-DE')}
                            {' '}
                            {startedAt.toLocaleTimeString('de-DE', { hour: '2-digit', minute: '2-digit' })}
                          </p>
                        </div>
                      </div>
                      <div className="flex items-center gap-2 flex-wrap justify-end">
                        <Badge variant="outline" className="text-xs">
                          {EXECUTOR_LABELS[log.executed_by] || log.executed_by}
                        </Badge>
                        {log.duration_minutes != null && (
                          <Badge variant="secondary" className="text-xs">
                            {log.duration_minutes} Min.
                          </Badge>
                        )}
                        {log.quality_score != null && (
                          <Badge
                            variant="outline"
                            className={`text-xs ${log.quality_score >= 80 ? 'text-green-600' : log.quality_score >= 50 ? 'text-yellow-600' : 'text-red-500'}`}
                          >
                            Q: {log.quality_score}%
                          </Badge>
                        )}
                        {log.cost_estimate_usd != null && (
                          <Badge variant="outline" className="text-xs text-muted-foreground">
                            ${log.cost_estimate_usd.toFixed(2)}
                          </Badge>
                        )}
                      </div>
                    </div>
                    {log.abort_reason && (
                      <p className="text-xs text-muted-foreground mt-2 pl-6">
                        Grund: {log.abort_reason}
                      </p>
                    )}
                  </CardContent>
                </Card>
              )
            })}
          </div>

          {hasMore && (
            <Button
              variant="outline"
              size="sm"
              onClick={loadMore}
              disabled={loading}
              className="w-full"
            >
              {loading
                ? <Loader2 className="h-4 w-4 animate-spin" />
                : <><ChevronDown className="h-4 w-4 mr-1" />Mehr laden ({total - logs.length} weitere)</>
              }
            </Button>
          )}
        </>
      )}
    </div>
  )
}
```

Wichtig:
- Pagination mit "Mehr laden"-Button (append, nicht replace)
- Quality Score: gruen >= 80, gelb >= 50, rot darunter
- Keine Edit-Funktionalitaet — Execution Logs sind read-only
- Keine eigene Card-Wrapper — Einbettung via Tab oder Sektion entscheidet ueber Layout
  </action>
  <verify>
    <automated>grep -n "ExecutionLogPanel\|api/v1/execution-logs" src/components/management/execution-log-panel.tsx</automated>
  </verify>
  <done>
- Datei existiert als 'use client' Komponente
- Exportiert `ExecutionLogPanel` mit Props `{ entityType, entityId }`
- Laedt von /api/v1/execution-logs?entity_type=...&entity_id=...
- Zeigt Datum, executed_by, Status mit Icon, Duration, Quality Score, Cost als Badges
- "Mehr laden"-Button fuer Pagination
- Leerzustand "Noch keine Ausfuehrungen protokolliert"
  </done>
</task>

<task type="auto">
  <name>Task 2: SOP-Listenfilter erweitern + ExecutionLogPanel in beide Detailseiten integrieren</name>
  <files>
    src/app/intern/(dashboard)/management/sops/page.tsx,
    src/app/intern/(dashboard)/management/sops/[id]/page.tsx,
    src/app/intern/(dashboard)/management/deliverables/[id]/page.tsx
  </files>
  <action>
Drei Dateien werden gezielt erweitert:

---

**A) sops/page.tsx — Zwei neue Filter + neue Badges in Karten**

1. Neue State-Variablen nach den bestehenden Filtern (nach Zeile ~31):
```tsx
const [automationFilter, setAutomationFilter] = useState('all')
const [maturityFilter, setMaturityFilter] = useState('all')
```

2. In der `load`-Funktion (useCallback, Zeile ~35ff) die neuen Params hinzufuegen:
```tsx
if (automationFilter !== 'all') params.set('automation_level', automationFilter)
if (maturityFilter !== 'all') params.set('maturity_level', maturityFilter)
```

3. `load`-Dependency-Array um neue Filter erweitern:
```tsx
}, [catFilter, statusFilter, search, automationFilter, maturityFilter])
```

4. In der Filter-Leiste (nach dem Status-Select, vor dem schliessenden `</div>`):
```tsx
<Select value={automationFilter} onValueChange={setAutomationFilter}>
  <SelectTrigger className="w-48"><SelectValue placeholder="Automatisierung" /></SelectTrigger>
  <SelectContent>
    <SelectItem value="all">Alle Automatisierung</SelectItem>
    <SelectItem value="manual">Manuell</SelectItem>
    <SelectItem value="semi">Semi-Auto</SelectItem>
    <SelectItem value="full">Vollautomatisch</SelectItem>
  </SelectContent>
</Select>
<Select value={maturityFilter} onValueChange={setMaturityFilter}>
  <SelectTrigger className="w-40"><SelectValue placeholder="Reifegrad" /></SelectTrigger>
  <SelectContent>
    <SelectItem value="all">Alle Reifegrade</SelectItem>
    <SelectItem value="1">Reife 1/5</SelectItem>
    <SelectItem value="2">Reife 2/5</SelectItem>
    <SelectItem value="3">Reife 3/5</SelectItem>
    <SelectItem value="4">Reife 4/5</SelectItem>
    <SelectItem value="5">Reife 5/5</SelectItem>
  </SelectContent>
</Select>
```

5. In der SOP-Karte (CardContent, nach den bestehenden Badges fuer Version/Datum):
```tsx
{/* Neue Framework-Badges */}
{(s.automation_level || s.maturity_level) && (
  <div className="flex flex-wrap gap-1 mt-2">
    {s.automation_level && (
      <Badge variant={s.automation_level === 'full' ? 'default' : 'secondary'} className="text-xs">
        {s.automation_level === 'manual' ? 'Manuell' : s.automation_level === 'semi' ? 'Semi-Auto' : 'Vollautomatisch'}
      </Badge>
    )}
    {s.maturity_level && (
      <Badge variant="outline" className="text-xs">Reife {s.maturity_level}/5</Badge>
    )}
  </div>
)}
```

---

**B) sops/[id]/page.tsx — Execution-Log-Tab hinzufuegen**

Datei wurde in 04-02 bereits erweitert. Jetzt ergaenzen:

1. Import nach den bestehenden Imports:
```tsx
import { ExecutionLogPanel } from '@/components/management/execution-log-panel'
```

2. Neuen Tab in der TabsList (nach dem "Framework"-Tab aus 04-02):
```tsx
<TabsTrigger value="execution">Ausfuehrungen</TabsTrigger>
```

3. Neuen TabsContent nach dem "framework"-TabsContent:
```tsx
<TabsContent value="execution">
  <ExecutionLogPanel entityType="sop" entityId={id} />
</TabsContent>
```

---

**C) deliverables/[id]/page.tsx — Execution-Log-Sektion am Ende der Seite**

Datei wurde in 04-02 erstellt. Ergaenzen:

1. Import nach den bestehenden Imports:
```tsx
import { ExecutionLogPanel } from '@/components/management/execution-log-panel'
```

2. Neue Sektion nach der "Produzierende SOPs"-Sektion (vor schliessendem `</div>`):
```tsx
{/* Execution Log */}
<div>
  <ExecutionLogPanel entityType="deliverable" entityId={id} />
</div>
```

---

Alle anderen Teile der Dateien bleiben unveraendert.
  </action>
  <verify>
    <automated>grep -rn "ExecutionLogPanel\|automationFilter\|maturityFilter" "src/app/intern/(dashboard)/management/"</automated>
  </verify>
  <done>
- sops/page.tsx: automationFilter und maturityFilter State + Select-Dropdowns vorhanden, werden als Query-Params an /api/v1/sops uebergeben, SOP-Karten zeigen automation_level und maturity_level Badges
- sops/[id]/page.tsx: "Ausfuehrungen"-Tab mit ExecutionLogPanel (entityType="sop") vorhanden
- deliverables/[id]/page.tsx: ExecutionLogPanel (entityType="deliverable") am Ende der Seite vorhanden
- ExecutionLogPanel-Import in beiden Detailseiten korrekt auf '@/components/management/execution-log-panel'
  </done>
</task>

</tasks>

<verification>
1. `grep -n "automationFilter\|maturityFilter" "src/app/intern/(dashboard)/management/sops/page.tsx"` gibt Treffer
2. `grep -n "ExecutionLogPanel" "src/app/intern/(dashboard)/management/sops/[id]/page.tsx"` gibt Treffer
3. `grep -n "ExecutionLogPanel" "src/app/intern/(dashboard)/management/deliverables/[id]/page.tsx"` gibt Treffer
4. `grep -n "api/v1/execution-logs" src/components/management/execution-log-panel.tsx` gibt Treffer
5. `ls src/components/management/execution-log-panel.tsx` gibt Ergebnis (Datei existiert)
</verification>

<success_criteria>
- SOP-Listenseite hat 2 neue Filter (automation_level, maturity_level) die als Query-Params an die API uebergeben werden
- SOP-Karten zeigen automation_level und maturity_level als Badges wenn die Daten vorhanden sind
- ExecutionLogPanel ist eine eigenstaendige, wiederverwendbare Komponente unter src/components/management/
- SOP-Detailseite hat "Ausfuehrungen"-Tab mit ExecutionLogPanel
- Deliverable-Detailseite hat ExecutionLogPanel-Sektion
- Alle 7 Requirements (DEL-06, DEL-07, DEL-08, UI-01, UI-02, LINK-04, EXEC-04) sind durch die 3 Plaene abgedeckt
</success_criteria>

<output>
Nach Abschluss `.planning/phases/04-ui-deliverables-sop-erweiterung/04-03-SUMMARY.md` erstellen.
</output>
