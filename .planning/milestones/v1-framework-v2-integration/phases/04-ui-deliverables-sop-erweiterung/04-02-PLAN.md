---
phase: 04-ui-deliverables-sop-erweiterung
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/app/intern/(dashboard)/management/deliverables/[id]/page.tsx
  - src/app/intern/(dashboard)/management/sops/[id]/page.tsx
autonomous: true
requirements: [DEL-07, UI-01, LINK-04]

must_haves:
  truths:
    - "Deliverable-Detailseite zeigt alle Felder: Name, Beschreibung, Format, Umfang, Trigger, Modul, Kategorie"
    - "Deliverable-Detailseite zeigt eine Liste der verknuepften SOPs mit Direktlink zur SOP-Detailseite"
    - "SOP-Detailseite zeigt Maturity-Badge (1-5 Sterne oder Zahl), Automation-Level-Badge, Estimated Duration und Executor-Badge"
    - "SOP-Detailseite zeigt das verknuepfte Deliverable mit Name und Direktlink zur Deliverable-Detailseite"
    - "Bidirektionale Navigation funktioniert: SOP -> Deliverable -> SOP ohne Umweg ueber Listen"
  artifacts:
    - path: "src/app/intern/(dashboard)/management/deliverables/[id]/page.tsx"
      provides: "Deliverable-Detailseite mit verknuepften SOPs"
      min_lines: 80
      exports: ["default function DeliverableDetailPage"]
    - path: "src/app/intern/(dashboard)/management/sops/[id]/page.tsx"
      provides: "Erweiterte SOP-Detailseite mit neuen Feldern und Deliverable-Link"
      contains: "maturity_level"
  key_links:
    - from: "deliverables/[id]/page.tsx"
      to: "/api/v1/deliverables/[id]"
      via: "fetch in useCallback"
      pattern: "api/v1/deliverables.*id"
    - from: "deliverables/[id]/page.tsx (linked SOPs)"
      to: "/intern/management/sops/[id]"
      via: "Link component"
      pattern: "management/sops"
    - from: "sops/[id]/page.tsx (produces_deliverable)"
      to: "/intern/management/deliverables/[id]"
      via: "Link component"
      pattern: "management/deliverables"
---

<objective>
Deliverable-Detailseite erstellen und SOP-Detailseite um neue Felder erweitern (bidirektionale Navigation).

Purpose: Nutzer koennen von einem Deliverable alle produzierenden SOPs direkt aufrufen und umgekehrt — das schliesst die SOP-Deliverable-Kette in der UI.
Output: Neue Datei deliverables/[id]/page.tsx + erweitertes sops/[id]/page.tsx mit neuen Framework-Feldern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Bestehende Muster — direkt uebernehmen
@src/app/intern/(dashboard)/management/sops/[id]/page.tsx
@src/lib/constants/framework.ts

# Vorherige Planausgabe (Listenseite als Referenz-Pattern)
@.planning/phases/04-ui-deliverables-sop-erweiterung/04-01-SUMMARY.md

<interfaces>
<!-- API-Kontrakte aus Phase 2 -->

GET /api/v1/deliverables/[id]
Response: {
  success: true,
  data: {
    id: string
    name: string
    description: string | null
    format: string | null
    umfang: string | null
    trigger: string | null
    category: string
    category_code: string
    status: string
    version: string
    module: { id: string, code: string, name: string, category: string, category_code: string, ziel: string | null }
    sops: {       // verknuepfte SOPs (produces_deliverable_id = this.id)
      id: string
      title: string
      status: string
      automation_level: 'manual' | 'semi' | 'full' | null
      maturity_level: number | null   // 1-5
      source_task_id: string | null   // z.B. "KP1-01"
    }[]
  }
}

GET /api/v1/sops/[id]
Response (erweiterte Felder aus Phase 1/2):
{
  success: true,
  data: {
    id: string
    title: string
    status: string
    version: string
    category: string
    purpose: string | null
    scope: string | null
    tools: string[]
    tags: string[]
    reviewDate: string | null
    automation_level: 'manual' | 'semi' | 'full' | null    // NEU Phase 1
    ai_capable: boolean | null                              // NEU Phase 1
    maturity_level: number | null                          // NEU Phase 1, Wert 1-5
    estimated_duration_minutes: number | null              // NEU Phase 1
    subprocess: string | null                              // NEU Phase 1
    source_task_id: string | null                          // NEU Phase 1
    produces_deliverable: {                                // NEU Phase 2 (joined)
      id: string
      name: string
      category: string
      category_code: string
      module: { code: string, name: string }
    } | null
    steps: {
      id: string
      title: string
      description: string | null
      responsible: string | null
      estimatedMinutes: number | null
      warnings: string[]
      checklistItems: string[]
      sequence: number
      executor: 'agent' | 'human' | 'flex' | null   // NEU Phase 1
    }[]
    versions: { id: string, version: string, changeNote: string | null, createdAt: string }[]
  }
}

<!-- Badge-Farben fuer neue Felder (konsistent definieren) -->
AUTOMATION_LEVEL_LABELS: { manual: 'Manuell', semi: 'Semi-Auto', full: 'Vollautomatisch' }
AUTOMATION_LEVEL_VARIANT: { manual: 'secondary', semi: 'outline', full: 'default' }
EXECUTOR_LABELS: { agent: 'Agent', human: 'Mensch', flex: 'Flexibel' }
MATURITY_COLORS: { 1: 'text-red-500', 2: 'text-orange-500', 3: 'text-yellow-500', 4: 'text-blue-500', 5: 'text-green-500' }

<!-- Konstanten aus framework.ts -->
import { getCategoryLabel } from '@/lib/constants/framework'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deliverable-Detailseite erstellen</name>
  <files>src/app/intern/(dashboard)/management/deliverables/[id]/page.tsx</files>
  <action>
Erstelle den Ordner und die Datei `src/app/intern/(dashboard)/management/deliverables/[id]/page.tsx`
als `'use client'`-Komponente. Muster: analog zu `sops/[id]/page.tsx` (useParams, useCallback, load).

Struktur:

```tsx
'use client'

import { useEffect, useState, useCallback } from 'react'
import { useParams } from 'next/navigation'
import Link from 'next/link'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Loader2, ArrowLeft, Package, FileText, ExternalLink } from 'lucide-react'
import { getCategoryLabel } from '@/lib/constants/framework'

const STATUS_LABELS: Record<string, string> = {
  draft: 'Entwurf', review: 'Review', approved: 'Freigegeben', archived: 'Archiviert'
}
const AUTOMATION_LEVEL_LABELS: Record<string, string> = {
  manual: 'Manuell', semi: 'Semi-Auto', full: 'Vollautomatisch'
}
const AUTOMATION_LEVEL_VARIANT: Record<string, 'default' | 'secondary' | 'outline'> = {
  manual: 'secondary', semi: 'outline', full: 'default'
}

export default function DeliverableDetailPage() {
  const { id } = useParams<{ id: string }>()
  const [loading, setLoading] = useState(true)
  const [deliverable, setDeliverable] = useState<any>(null)

  const load = useCallback(async () => {
    const res = await fetch(`/api/v1/deliverables/${id}`)
    const d = await res.json()
    if (d.success) setDeliverable(d.data)
    setLoading(false)
  }, [id])

  useEffect(() => { load() }, [load])

  if (loading) return (
    <div className="flex items-center justify-center py-16">
      <Loader2 className="h-8 w-8 animate-spin text-muted-foreground" />
    </div>
  )
  if (!deliverable) return (
    <div className="py-16 text-center text-muted-foreground">Deliverable nicht gefunden</div>
  )

  return (
    <div className="space-y-4">
      {/* Header */}
      <div className="flex items-center gap-3">
        <Link href="/intern/management/deliverables">
          <Button variant="ghost" size="icon"><ArrowLeft className="h-4 w-4" /></Button>
        </Link>
        <div>
          <h1 className="text-2xl font-bold">{deliverable.name}</h1>
          <div className="flex items-center gap-2 mt-1">
            <Badge variant="outline" className="font-mono text-xs">{deliverable.module?.code}</Badge>
            <Badge variant="secondary">{getCategoryLabel(deliverable.category_code) || deliverable.category}</Badge>
            <Badge variant={deliverable.status === 'approved' ? 'default' : 'secondary'}>
              {STATUS_LABELS[deliverable.status] || deliverable.status}
            </Badge>
            <span className="text-xs text-muted-foreground">v{deliverable.version}</span>
          </div>
        </div>
      </div>

      {/* Metadaten-Card */}
      <Card>
        <CardHeader>
          <CardTitle className="text-sm font-medium text-muted-foreground uppercase tracking-wider">
            Details
          </CardTitle>
        </CardHeader>
        <CardContent className="space-y-3">
          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
            {/* Modul */}
            <div>
              <p className="text-xs font-medium text-muted-foreground">Modul</p>
              <p className="text-sm mt-0.5">
                <span className="font-mono font-semibold">{deliverable.module?.code}</span>
                {' — '}{deliverable.module?.name}
              </p>
            </div>
            {/* Format */}
            {deliverable.format && (
              <div>
                <p className="text-xs font-medium text-muted-foreground">Format</p>
                <p className="text-sm mt-0.5">{deliverable.format}</p>
              </div>
            )}
            {/* Umfang */}
            {deliverable.umfang && (
              <div>
                <p className="text-xs font-medium text-muted-foreground">Umfang</p>
                <p className="text-sm mt-0.5">{deliverable.umfang}</p>
              </div>
            )}
            {/* Trigger */}
            {deliverable.trigger && (
              <div>
                <p className="text-xs font-medium text-muted-foreground">Trigger</p>
                <p className="text-sm mt-0.5">{deliverable.trigger}</p>
              </div>
            )}
          </div>
          {/* Beschreibung */}
          {deliverable.description && (
            <div>
              <p className="text-xs font-medium text-muted-foreground">Beschreibung</p>
              <p className="text-sm mt-0.5 whitespace-pre-wrap">{deliverable.description}</p>
            </div>
          )}
          {/* Modulziel */}
          {deliverable.module?.ziel && (
            <div>
              <p className="text-xs font-medium text-muted-foreground">Modulziel</p>
              <p className="text-sm mt-0.5 text-muted-foreground">{deliverable.module.ziel}</p>
            </div>
          )}
        </CardContent>
      </Card>

      {/* Verknuepfte SOPs */}
      <div>
        <h2 className="text-lg font-semibold flex items-center gap-2 mb-3">
          <FileText className="h-5 w-5" />
          Produzierende SOPs
          <Badge variant="outline">{deliverable.sops?.length ?? 0}</Badge>
        </h2>
        {!deliverable.sops?.length ? (
          <Card>
            <CardContent className="py-8 text-center text-muted-foreground">
              Keine SOPs verknuepft
            </CardContent>
          </Card>
        ) : (
          <div className="space-y-2">
            {deliverable.sops.map((sop: any) => (
              <Link key={sop.id} href={`/intern/management/sops/${sop.id}`}>
                <Card className="hover:shadow-sm transition-shadow cursor-pointer">
                  <CardContent className="py-3 flex items-center justify-between gap-3">
                    <div className="flex items-center gap-2 min-w-0">
                      {sop.source_task_id && (
                        <Badge variant="outline" className="font-mono text-xs shrink-0">
                          {sop.source_task_id}
                        </Badge>
                      )}
                      <span className="text-sm font-medium truncate">{sop.title}</span>
                    </div>
                    <div className="flex items-center gap-2 shrink-0">
                      {sop.automation_level && (
                        <Badge variant={AUTOMATION_LEVEL_VARIANT[sop.automation_level] || 'secondary'} className="text-xs">
                          {AUTOMATION_LEVEL_LABELS[sop.automation_level] || sop.automation_level}
                        </Badge>
                      )}
                      {sop.maturity_level && (
                        <Badge variant="outline" className="text-xs">
                          Reife {sop.maturity_level}/5
                        </Badge>
                      )}
                      <ExternalLink className="h-3.5 w-3.5 text-muted-foreground" />
                    </div>
                  </CardContent>
                </Card>
              </Link>
            ))}
          </div>
        )}
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>grep -n "api/v1/deliverables.*id\|management/sops" "src/app/intern/(dashboard)/management/deliverables/[id]/page.tsx"</automated>
  </verify>
  <done>
- Datei existiert mit 'use client', useParams, fetch zu /api/v1/deliverables/[id]
- Zeigt Metadaten-Card mit Modul, Format, Umfang, Trigger, Beschreibung
- Zeigt verknuepfte SOPs-Liste mit Link zu /intern/management/sops/[id]
- Leerzustand "Keine SOPs verknuepft" wird korrekt angezeigt
  </done>
</task>

<task type="auto">
  <name>Task 2: SOP-Detailseite um neue Framework-Felder und Deliverable-Link erweitern</name>
  <files>src/app/intern/(dashboard)/management/sops/[id]/page.tsx</files>
  <action>
Die bestehende `sops/[id]/page.tsx` wird um drei Bereiche erweitert. Die Datei wird NICHT neu geschrieben —
nur gezielte Ergaenzungen:

**1. Neue Imports am Anfang** (nach bestehenden Imports, vor CATEGORIES-Konstante):
```tsx
import { Package } from 'lucide-react'  // zum bestehenden Lucide-Import hinzufuegen
```

**2. Badge-Mapping-Konstanten** direkt nach den bestehenden STATUS_LABELS/STATUS_VARIANT-Definitionen (nach Zeile ~23) einfuegen:
```tsx
const AUTOMATION_LEVEL_LABELS: Record<string, string> = {
  manual: 'Manuell', semi: 'Semi-Auto', full: 'Vollautomatisch'
}
const AUTOMATION_LEVEL_VARIANT: Record<string, 'default' | 'secondary' | 'outline'> = {
  manual: 'secondary', semi: 'outline', full: 'default'
}
const EXECUTOR_LABELS: Record<string, string> = {
  agent: 'Agent', human: 'Mensch', flex: 'Flexibel'
}
const MATURITY_COLORS: Record<number, string> = {
  1: 'text-red-500', 2: 'text-orange-500', 3: 'text-yellow-500',
  4: 'text-blue-500', 5: 'text-green-600'
}
```

**3. In der Header-Sektion** (nach dem div mit den bestehenden Status/Version-Badges, ca. Zeile ~114):
Neue Badges fuer die SOP-Metadaten-Zeile hinzufuegen:
```tsx
{/* Neue Framework-Metadaten-Zeile direkt unter den bestehenden Badges */}
{(sop.automation_level || sop.maturity_level || sop.estimated_duration_minutes) && (
  <div className="flex items-center gap-2 mt-1 flex-wrap">
    {sop.automation_level && (
      <Badge variant={AUTOMATION_LEVEL_VARIANT[sop.automation_level] || 'secondary'}>
        {AUTOMATION_LEVEL_LABELS[sop.automation_level] || sop.automation_level}
      </Badge>
    )}
    {sop.maturity_level && (
      <Badge variant="outline" className={MATURITY_COLORS[sop.maturity_level]}>
        Reife {sop.maturity_level}/5
      </Badge>
    )}
    {sop.estimated_duration_minutes && (
      <Badge variant="outline">ca. {sop.estimated_duration_minutes} Min.</Badge>
    )}
    {sop.ai_capable && (
      <Badge variant="default" className="bg-violet-600">KI-faehig</Badge>
    )}
  </div>
)}
```

**4. Neuer Tab "Framework"** in der TabsList (nach "Versionen"):
```tsx
<TabsTrigger value="framework">Framework</TabsTrigger>
```

**5. TabsContent fuer "framework"** (nach dem Versionen-TabsContent, vor dem schliessenden Tabs-Tag):
```tsx
<TabsContent value="framework" className="space-y-4">
  {/* Deliverable-Verknuepfung */}
  {sop.produces_deliverable ? (
    <Card>
      <CardHeader>
        <CardTitle className="text-sm font-medium text-muted-foreground uppercase tracking-wider flex items-center gap-2">
          <Package className="h-4 w-4" />Produziertes Deliverable
        </CardTitle>
      </CardHeader>
      <CardContent>
        <Link href={`/intern/management/deliverables/${sop.produces_deliverable.id}`}
          className="flex items-center justify-between p-3 rounded-md border hover:bg-muted/50 transition-colors">
          <div className="flex items-center gap-2">
            <Badge variant="outline" className="font-mono text-xs">
              {sop.produces_deliverable.module?.code}
            </Badge>
            <span className="font-medium">{sop.produces_deliverable.name}</span>
          </div>
          <div className="flex items-center gap-2">
            <Badge variant="secondary" className="text-xs">
              {sop.produces_deliverable.category}
            </Badge>
            <ExternalLink className="h-3.5 w-3.5 text-muted-foreground" />
          </div>
        </Link>
      </CardContent>
    </Card>
  ) : (
    <Card>
      <CardContent className="py-8 text-center text-muted-foreground">
        Diese SOP produziert kein Deliverable
      </CardContent>
    </Card>
  )}

  {/* Weitere Framework-Metadaten */}
  <Card>
    <CardHeader>
      <CardTitle className="text-sm font-medium text-muted-foreground uppercase tracking-wider">
        Prozess-Details
      </CardTitle>
    </CardHeader>
    <CardContent className="space-y-2">
      <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
        {sop.source_task_id && (
          <div>
            <p className="text-xs text-muted-foreground">Task-ID</p>
            <p className="text-sm font-mono font-medium">{sop.source_task_id}</p>
          </div>
        )}
        {sop.subprocess && (
          <div>
            <p className="text-xs text-muted-foreground">Teilprozess</p>
            <p className="text-sm">{sop.subprocess}</p>
          </div>
        )}
        {sop.automation_level && (
          <div>
            <p className="text-xs text-muted-foreground">Automatisierung</p>
            <p className="text-sm">{AUTOMATION_LEVEL_LABELS[sop.automation_level] || sop.automation_level}</p>
          </div>
        )}
        {sop.maturity_level && (
          <div>
            <p className="text-xs text-muted-foreground">Reifegrad</p>
            <p className={`text-sm font-semibold ${MATURITY_COLORS[sop.maturity_level] || ''}`}>
              {sop.maturity_level} / 5
            </p>
          </div>
        )}
      </div>
    </CardContent>
  </Card>
</TabsContent>
```

**6. ExternalLink-Import** zum bestehenden Import-Statement hinzufuegen (sofern noch nicht vorhanden):
Die bestehende Zeile mit Lucide-Imports um `ExternalLink` erweitern.

**7. Steps-Editor: executor-Feld pro Schritt** in der Step-Card (nach dem "Checkliste"-Feld, Zeile ~177):
```tsx
<div>
  <label className="text-sm font-medium block mb-1">Executor</label>
  <Select value={s.executor || ''} onValueChange={v => updateStep(i, 'executor', v || null)}>
    <SelectTrigger>
      <SelectValue placeholder="Kein Executor" />
    </SelectTrigger>
    <SelectContent>
      <SelectItem value="">Keiner</SelectItem>
      <SelectItem value="human">Mensch</SelectItem>
      <SelectItem value="agent">Agent</SelectItem>
      <SelectItem value="flex">Flexibel</SelectItem>
    </SelectContent>
  </Select>
</div>
```

Alle anderen Teile der Datei bleiben unveraendert (Editor, Preview, Versionen-Tab, Save/Publish-Buttons).
  </action>
  <verify>
    <automated>grep -n "produces_deliverable\|maturity_level\|automation_level\|framework" "src/app/intern/(dashboard)/management/sops/[id]/page.tsx"</automated>
  </verify>
  <done>
- SOP-Detailseite zeigt Framework-Metadaten-Badges (automation_level, maturity_level, duration) im Header
- Neuer Tab "Framework" zeigt Deliverable-Link (wenn produces_deliverable vorhanden) und Prozess-Details
- Executor-Select ist in jedem Step-Editor vorhanden
- Bestehende Funktionalitaet (Editor, Preview, Versionen, Save, Publish) bleibt unveraendert
  </done>
</task>

</tasks>

<verification>
1. `ls "src/app/intern/(dashboard)/management/deliverables/[id]/page.tsx"` — Datei existiert
2. `grep -c "management/sops" "src/app/intern/(dashboard)/management/deliverables/[id]/page.tsx"` gibt mindestens 1 (Link zu SOPs)
3. `grep -c "management/deliverables" "src/app/intern/(dashboard)/management/sops/[id]/page.tsx"` gibt mindestens 1 (Link zu Deliverable)
4. `grep "produces_deliverable" "src/app/intern/(dashboard)/management/sops/[id]/page.tsx"` gibt Treffer
5. `grep "maturity_level\|automation_level" "src/app/intern/(dashboard)/management/sops/[id]/page.tsx"` gibt Treffer
</verification>

<success_criteria>
- Deliverable-Detailseite: Metadaten + SOP-Liste mit Direktlinks
- SOP-Detailseite: Neuer "Framework"-Tab mit Deliverable-Link und Prozess-Metadaten
- Bidirektionale Navigation: SOP-Detail hat Link zu Deliverable, Deliverable-Detail hat Links zu SOPs
- Kein Breaking Change an bestehender SOP-Funktionalitaet (Editor, Preview, Versionen)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/04-ui-deliverables-sop-erweiterung/04-02-SUMMARY.md` erstellen.
</output>
