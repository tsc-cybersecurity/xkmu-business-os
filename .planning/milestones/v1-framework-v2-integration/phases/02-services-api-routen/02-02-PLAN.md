---
phase: 02-services-api-routen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/execution-log.service.ts
  - src/app/api/v1/execution-logs/route.ts
  - src/app/api/v1/sops/[id]/route.ts
autonomous: true
requirements:
  - EXEC-02
  - EXEC-03
  - LINK-02

must_haves:
  truths:
    - "GET /api/v1/execution-logs gibt eine paginierte Liste zurueck, filterbar nach entityType und entityId"
    - "POST /api/v1/execution-logs legt einen neuen Ausfuehrungseintrag an und gibt ihn mit 201 zurueck"
    - "GET /api/v1/sops/[id] gibt jetzt auch producesDeliverable (Deliverable-Objekt oder null) zurueck"
    - "Alle Endpoints sind per withPermission abgesichert und scopen Queries auf tenantId"
  artifacts:
    - path: "src/lib/services/execution-log.service.ts"
      provides: "ExecutionLogService mit create, list, getStats"
      exports: ["ExecutionLogService"]
    - path: "src/app/api/v1/execution-logs/route.ts"
      provides: "GET (list + filter) und POST (create)"
      exports: ["GET", "POST"]
    - path: "src/app/api/v1/sops/[id]/route.ts"
      provides: "Erweiterter GET mit producesDeliverable JOIN (LINK-02)"
      exports: ["GET", "PUT", "DELETE"]
  key_links:
    - from: "src/app/api/v1/sops/[id]/route.ts"
      to: "src/lib/services/sop.service.ts"
      via: "SopService.getByIdWithDeliverable() — neue Methode"
      pattern: "getByIdWithDeliverable"
    - from: "src/lib/services/sop.service.ts"
      to: "deliverables table"
      via: "db.select().from(deliverables).where(eq(deliverables.id, doc.producesDeliverableId))"
      pattern: "producesDeliverableId"
---

<objective>
ExecutionLogService und API-Route implementieren sowie SOP-Detail-API um Deliverable-Rueckgabe erweitern.

Purpose: Execution Logs sind die Laufzeitschicht — sie protokollieren wann SOPs/Deliverables ausgefuehrt wurden, von wem und mit welchem Ergebnis. Die SOP-Detail-Erweiterung (LINK-02) schliesst die bidirektionale Verknuepfung: SOP weiss, welches Deliverable es produziert.
Output: 1 neuer Service + 1 neue API-Route + 1 modifizierte bestehende Route
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Aus src/lib/db/schema.ts -->

```typescript
// Tabelle: execution_logs
export const executionLogs = pgTable('execution_logs', {
  id: uuid('id').primaryKey().defaultRandom(),
  tenantId: uuid('tenant_id').notNull(),
  // Was wurde ausgefuehrt?
  entityType: varchar('entity_type', { length: 20 }).notNull(), // 'sop' | 'deliverable'
  entityId: uuid('entity_id').notNull(),
  entityVersion: varchar('entity_version', { length: 20 }),
  // Zeitstempel
  startedAt: timestamp('started_at', { withTimezone: true }).notNull(),
  completedAt: timestamp('completed_at', { withTimezone: true }),
  // Wer/Wie
  executedBy: varchar('executed_by', { length: 10 }).notNull(), // 'agent' | 'human'
  // Ergebnis
  status: varchar('status', { length: 20 }).notNull(),  // 'completed' | 'aborted' | 'escalated'
  abortReason: text('abort_reason'),
  qualityScore: real('quality_score'),       // 0.0 - 1.0
  durationMinutes: real('duration_minutes'),
  costEstimateUsd: real('cost_estimate_usd'),
  flags: text('flags').array().default([]),
  // Verknuepfungen
  linkedClientId: uuid('linked_client_id'),
  linkedProjectId: uuid('linked_project_id'),
  // Genehmigung
  humanApproved: boolean('human_approved').default(false),
  humanApprovedBy: uuid('human_approved_by'),
  humanApprovedAt: timestamp('human_approved_at'),
  createdAt: timestamp('created_at').defaultNow(),
})

export type ExecutionLog = typeof executionLogs.$inferSelect
export type NewExecutionLog = typeof executionLogs.$inferInsert

// Tabelle: sopDocuments (relevant fuer LINK-02)
export const sopDocuments = pgTable('sop_documents', {
  id: uuid('id').primaryKey().defaultRandom(),
  tenantId: uuid('tenant_id').notNull(),
  title: varchar('title', { length: 255 }).notNull(),
  category: varchar('category', { length: 50 }).notNull(),
  version: varchar('version', { length: 20 }),
  status: varchar('status', { length: 20 }),
  producesDeliverableId: uuid('produces_deliverable_id'), // FK -> deliverables (nullable)
  deletedAt: timestamp('deleted_at'),
  // ... weitere Felder
})

// Tabelle: deliverables (fuer den JOIN in SOP-Detail)
export const deliverables = pgTable('deliverables', {
  id: uuid('id').primaryKey().defaultRandom(),
  tenantId: uuid('tenant_id').notNull(),
  moduleId: uuid('module_id'),
  name: varchar('name', { length: 255 }).notNull(),
  description: text('description'),
  format: varchar('format', { length: 100 }),
  category: varchar('category', { length: 50 }),
  categoryCode: varchar('category_code', { length: 10 }),
  status: varchar('status', { length: 20 }),
  version: varchar('version', { length: 20 }),
})
```

<!-- Aus src/lib/constants/framework.ts -->
```typescript
export const ENTITY_TYPE_ENUM = ['sop', 'deliverable'] as const
export type EntityTypeEnum = typeof ENTITY_TYPE_ENUM[number]

export const EXECUTED_BY_ENUM = ['agent', 'human'] as const
export type ExecutedByEnum = typeof EXECUTED_BY_ENUM[number]

export const EXECUTION_STATUS_ENUM = ['completed', 'aborted', 'escalated'] as const
export type ExecutionStatusEnum = typeof EXECUTION_STATUS_ENUM[number]
```

<!-- Bestehende SopService-Signatur — die neue Methode muss diesem Stil folgen -->
```typescript
// Aus src/lib/services/sop.service.ts
export const SopService = {
  async getById(tenantId: string, id: string) {
    const [doc] = await db.select().from(sopDocuments)
      .where(and(eq(sopDocuments.tenantId, tenantId), eq(sopDocuments.id, id), isNull(sopDocuments.deletedAt)))
    if (!doc) return null
    const steps = await db.select().from(sopSteps)
      .where(eq(sopSteps.sopId, id)).orderBy(asc(sopSteps.sequence))
    const versions = await db.select().from(sopVersions)
      .where(eq(sopVersions.sopId, id)).orderBy(desc(sopVersions.createdAt))
    return { ...doc, steps, versions }
  },
  // ... weitere Methoden
}
```

<!-- API-Route-Muster — exakt wie sop-Routen -->
```typescript
type Params = Promise<{ id: string }>
export async function GET(request: NextRequest, { params }: { params: Params }) {
  return withPermission(request, 'module', 'read', async (auth) => {
    const { id } = await params
    const item = await SomeService.getById(auth.tenantId, id)
    if (!item) return apiNotFound('Resource nicht gefunden')
    return apiSuccess(item)
  })
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExecutionLogService implementieren</name>
  <files>src/lib/services/execution-log.service.ts</files>
  <action>
Neue Datei erstellen: `src/lib/services/execution-log.service.ts`

Imports:
```typescript
import { db } from '@/lib/db'
import { executionLogs } from '@/lib/db/schema'
import { eq, and, desc, sql } from 'drizzle-orm'
import type { EntityTypeEnum } from '@/lib/constants/framework'
```

Methoden in `export const ExecutionLogService = { ... }`:

**`create(tenantId, data)`**
- `data` erwartet folgende Pflichtfelder: `entityType`, `entityId`, `executedBy`, `status`, `startedAt`
- Optionale Felder: `entityVersion`, `completedAt`, `abortReason`, `qualityScore`, `durationMinutes`, `costEstimateUsd`, `flags`, `linkedClientId`, `linkedProjectId`
- Implementation:
  ```typescript
  async create(tenantId: string, data: Record<string, unknown>) {
    const [log] = await db.insert(executionLogs).values({
      tenantId,
      entityType: data.entityType as string,
      entityId: data.entityId as string,
      entityVersion: (data.entityVersion as string) || null,
      startedAt: data.startedAt ? new Date(data.startedAt as string) : new Date(),
      completedAt: data.completedAt ? new Date(data.completedAt as string) : null,
      executedBy: data.executedBy as string,
      status: data.status as string,
      abortReason: (data.abortReason as string) || null,
      qualityScore: (data.qualityScore as number) ?? null,
      durationMinutes: (data.durationMinutes as number) ?? null,
      costEstimateUsd: (data.costEstimateUsd as number) ?? null,
      flags: (data.flags as string[]) || [],
      linkedClientId: (data.linkedClientId as string) || null,
      linkedProjectId: (data.linkedProjectId as string) || null,
    }).returning()
    return log
  },
  ```

**`list(tenantId, filters?)`**
- `filters?: { entityType?: string; entityId?: string; status?: string }`
- conditions: `[eq(executionLogs.tenantId, tenantId)]`
- Optionale conditions analog sop.service.ts pattern:
  - `if (filters?.entityType) conditions.push(eq(executionLogs.entityType, filters.entityType))`
  - `if (filters?.entityId) conditions.push(eq(executionLogs.entityId, filters.entityId))`
  - `if (filters?.status) conditions.push(eq(executionLogs.status, filters.status))`
- Query: `db.select().from(executionLogs).where(and(...conditions)).orderBy(desc(executionLogs.startedAt))`
- Kein Pagination im Service (Pagination erfolgt in der API-Route)

**`getStats(tenantId, entityType?, entityId?)`**
- Gibt Aggregat-Statistiken zurueck: Anzahl pro Status, durchschnittliche Qualitaet, Gesamtkosten
  ```typescript
  async getStats(tenantId: string, entityType?: EntityTypeEnum, entityId?: string) {
    const conditions = [eq(executionLogs.tenantId, tenantId)]
    if (entityType) conditions.push(eq(executionLogs.entityType, entityType))
    if (entityId) conditions.push(eq(executionLogs.entityId, entityId))
    const rows = await db.select({
      status: executionLogs.status,
      count: sql<number>`count(*)::int`,
      avgQuality: sql<number>`avg(quality_score)::real`,
      totalCost: sql<number>`sum(cost_estimate_usd)::real`,
    }).from(executionLogs)
      .where(and(...conditions))
      .groupBy(executionLogs.status)
    return rows
  },
  ```
  </action>
  <verify>
    <automated>cd C:/Daten/xkmu-business-os && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "execution-log.service" || echo "No type errors in execution-log.service"</automated>
  </verify>
  <done>
  `src/lib/services/execution-log.service.ts` existiert und exportiert `ExecutionLogService` mit den Methoden `create`, `list`, `getStats`. TypeScript-Kompilierung ohne Fehler in dieser Datei.
  </done>
</task>

<task type="auto">
  <name>Task 2: ExecutionLog-API-Route + SOP-Detail mit Deliverable erweitern</name>
  <files>
    src/app/api/v1/execution-logs/route.ts,
    src/app/api/v1/sops/[id]/route.ts
  </files>
  <action>
**Datei 1 (neu): `src/app/api/v1/execution-logs/route.ts`**

Exakt dasselbe Muster wie `src/app/api/v1/sops/route.ts`:
```typescript
import { NextRequest } from 'next/server'
import { apiSuccess, apiServerError, parsePaginationParams } from '@/lib/utils/api-response'
import { ExecutionLogService } from '@/lib/services/execution-log.service'
import { withPermission } from '@/lib/auth/require-permission'

export async function GET(request: NextRequest) {
  return withPermission(request, 'processes', 'read', async (auth) => {
    const { searchParams } = new URL(request.url)
    const entityType = searchParams.get('entityType') || undefined
    const entityId = searchParams.get('entityId') || undefined
    const status = searchParams.get('status') || undefined
    const { page, limit } = parsePaginationParams(searchParams)
    const all = await ExecutionLogService.list(auth.tenantId, { entityType, entityId, status })
    const total = all.length
    const items = all.slice((page - 1) * limit, page * limit)
    return apiSuccess(items, { page, limit, total, totalPages: Math.ceil(total / limit) })
  })
}

export async function POST(request: NextRequest) {
  return withPermission(request, 'processes', 'create', async (auth) => {
    try {
      const body = await request.json()
      const log = await ExecutionLogService.create(auth.tenantId, body)
      return apiSuccess(log, undefined, 201)
    } catch { return apiServerError() }
  })
}
```

**Datei 2 (modifiziert): `src/app/api/v1/sops/[id]/route.ts`**

Die bestehende Datei erhaelt LINK-02: SOP-GET gibt jetzt auch `producesDeliverable` zurueck.

Strategie: `SopService.getById()` nicht anfassen (rueckwaertskompatibel). Stattdessen in `sop.service.ts` eine neue Methode `getByIdWithDeliverable()` hinzufuegen, die `getById()` aufruft und dann das Deliverable separat nachlaed.

Schritt 1 — `src/lib/services/sop.service.ts` erweitern (neue Methode ans Ende des Objekts anhaengen, NICHT bestehende Methoden aendern):
```typescript
// Neue Methode in SopService (nach allen bestehenden Methoden):
async getByIdWithDeliverable(tenantId: string, id: string) {
  const sop = await this.getById(tenantId, id)
  if (!sop) return null
  let producesDeliverable = null
  if (sop.producesDeliverableId) {
    const { deliverables } = await import('@/lib/db/schema')
    const [del] = await db.select().from(deliverables)
      .where(eq(deliverables.id, sop.producesDeliverableId))
    producesDeliverable = del ?? null
  }
  return { ...sop, producesDeliverable }
},
```

Wichtig: Der Import von `deliverables` muss am Anfang von sop.service.ts zu den bestehenden Imports hinzugefuegt werden (kein dynamischer Import noetig wenn bereits importiert). Aendere den bestehenden Import oben in der Datei:
```typescript
// Vorher:
import { sopDocuments, sopSteps, sopVersions } from '@/lib/db/schema'
// Nachher:
import { sopDocuments, sopSteps, sopVersions, deliverables } from '@/lib/db/schema'
```

Und die Methode ohne dynamischen Import:
```typescript
async getByIdWithDeliverable(tenantId: string, id: string) {
  const sop = await this.getById(tenantId, id)
  if (!sop) return null
  let producesDeliverable = null
  if (sop.producesDeliverableId) {
    const [del] = await db.select().from(deliverables)
      .where(eq(deliverables.id, sop.producesDeliverableId))
    producesDeliverable = del ?? null
  }
  return { ...sop, producesDeliverable }
},
```

Schritt 2 — `src/app/api/v1/sops/[id]/route.ts` anpassen: Nur die GET-Funktion aendern, PUT und DELETE bleiben unveraendert:
```typescript
import { NextRequest } from 'next/server'
import { apiSuccess, apiNotFound, apiServerError } from '@/lib/utils/api-response'
import { SopService } from '@/lib/services/sop.service'
import { withPermission } from '@/lib/auth/require-permission'

type Params = Promise<{ id: string }>

export async function GET(request: NextRequest, { params }: { params: Params }) {
  return withPermission(request, 'processes', 'read', async (auth) => {
    const { id } = await params
    // LINK-02: gibt jetzt producesDeliverable mit zurueck
    const sop = await SopService.getByIdWithDeliverable(auth.tenantId, id)
    if (!sop) return apiNotFound('SOP nicht gefunden')
    return apiSuccess(sop)
  })
}

export async function PUT(request: NextRequest, { params }: { params: Params }) {
  return withPermission(request, 'processes', 'update', async (auth) => {
    try {
      const { id } = await params
      const body = await request.json()
      const sop = await SopService.update(auth.tenantId, id, body)
      if (!sop) return apiNotFound('SOP nicht gefunden')
      return apiSuccess(sop)
    } catch { return apiServerError() }
  })
}

export async function DELETE(request: NextRequest, { params }: { params: Params }) {
  return withPermission(request, 'processes', 'delete', async (auth) => {
    const { id } = await params
    const deleted = await SopService.delete(auth.tenantId, id)
    if (!deleted) return apiNotFound('SOP nicht gefunden')
    return apiSuccess({ deleted: true })
  })
}
```

Hinweis: PUT und DELETE aus der Originaldatei unveraendert uebernehmen — nur GET auf `getByIdWithDeliverable()` umstellen.
  </action>
  <verify>
    <automated>cd C:/Daten/xkmu-business-os && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "(execution-log|sops/\[id\])" || echo "No type errors in execution-log or sop detail route"</automated>
  </verify>
  <done>
  - `src/app/api/v1/execution-logs/route.ts` existiert mit GET + POST
  - `src/lib/services/sop.service.ts` hat neue Methode `getByIdWithDeliverable`
  - `src/app/api/v1/sops/[id]/route.ts` GET verwendet `getByIdWithDeliverable` (gibt `producesDeliverable` zurueck)
  - TypeScript-Kompilierung ohne Fehler
  </done>
</task>

</tasks>

<verification>
TypeScript-Build ohne Fehler fuer alle geaenderten Dateien:
```bash
cd C:/Daten/xkmu-business-os && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20
```

Strukturpruefung (alle Dateien vorhanden):
```bash
ls C:/Daten/xkmu-business-os/src/lib/services/execution-log.service.ts
ls C:/Daten/xkmu-business-os/src/app/api/v1/execution-logs/route.ts
```

Funktionspruefung — SopService hat neue Methode:
```bash
grep -n "getByIdWithDeliverable" C:/Daten/xkmu-business-os/src/lib/services/sop.service.ts
grep -n "getByIdWithDeliverable" C:/Daten/xkmu-business-os/src/app/api/v1/sops/[id]/route.ts
```
</verification>

<success_criteria>
- `GET /api/v1/execution-logs` akzeptiert `entityType`, `entityId`, `status`, `page`, `limit` und gibt paginierte Liste zurueck
- `POST /api/v1/execution-logs` nimmt Pflichtfelder entgegen und gibt 201 mit neuem Log-Eintrag zurueck
- `GET /api/v1/sops/[id]` gibt jetzt `producesDeliverable: { id, name, ... } | null` im Response-Body zurueck (LINK-02)
- Bestehende `SopService.getById()` Signatur unveraendert (keine regressions fuer andere Aufrufer)
- TypeScript strict mode ohne Fehler
- `ExecutionLogService` ist als benanntes Export verfuegbar
</success_criteria>

<output>
Nach Abschluss `.planning/phases/02-services-api-routen/02-02-SUMMARY.md` erstellen mit:
- Was implementiert wurde (Methoden, Routen, Erweiterungen)
- Welche Entscheidungen getroffen wurden (z.B. neue Methode statt Aenderung bestehender)
- Datei-Liste inkl. ob Dateien neu oder modifiziert
</output>
