---
phase: 02-services-api-routen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/deliverable.service.ts
  - src/app/api/v1/deliverables/route.ts
  - src/app/api/v1/deliverables/[id]/route.ts
  - src/app/api/v1/deliverables/modules/route.ts
autonomous: true
requirements:
  - DEL-03
  - DEL-04
  - DEL-05
  - LINK-01
  - LINK-03

must_haves:
  truths:
    - "GET /api/v1/deliverables gibt eine paginierte Liste mit optionalem Filter nach moduleId und categoryCode zurueck"
    - "GET /api/v1/deliverables/[id] gibt ein Deliverable inkl. einer Liste der SOPs zurueck, die es produzieren (via produces_deliverable_id)"
    - "GET /api/v1/deliverables/modules gibt alle Module mit Deliverable-Count zurueck"
    - "POST /api/v1/deliverables legt ein neues Deliverable an und gibt es mit 201 zurueck"
    - "PATCH /api/v1/deliverables/[id] aktualisiert ein bestehendes Deliverable"
    - "DELETE /api/v1/deliverables/[id] loescht ein Deliverable (soft-delete oder hard-delete)"
    - "Alle Endpoints sind per withPermission abgesichert und scopen Queries auf tenantId"
  artifacts:
    - path: "src/lib/services/deliverable.service.ts"
      provides: "DeliverableService mit list, getById, create, update, delete, getModulesWithCount"
      exports: ["DeliverableService"]
    - path: "src/app/api/v1/deliverables/route.ts"
      provides: "GET (list + filter) und POST (create)"
      exports: ["GET", "POST"]
    - path: "src/app/api/v1/deliverables/[id]/route.ts"
      provides: "GET (detail + linked SOPs), PATCH (update), DELETE"
      exports: ["GET", "PATCH", "DELETE"]
    - path: "src/app/api/v1/deliverables/modules/route.ts"
      provides: "GET (alle Module mit Deliverable-Count)"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/v1/deliverables/[id]/route.ts"
      to: "src/lib/services/deliverable.service.ts"
      via: "DeliverableService.getById()"
      pattern: "DeliverableService\\.getById"
    - from: "src/lib/services/deliverable.service.ts"
      to: "sopDocuments.producesDeliverableId"
      via: "LEFT JOIN sopDocuments ON produces_deliverable_id = deliverables.id"
      pattern: "producesDeliverableId"
---

<objective>
DeliverableService und alle zugehoerigen API-Routen implementieren — vollstaendige CRUD-Operationen, paginierte Liste mit Filter, Modul-Uebersicht und Rueckgabe verknuepfter SOPs beim Deliverable-Detail.

Purpose: Phase 2 benoetigt ein vollstaendiges Backend fuer Deliverables, bevor Phase 3 Seed-Daten einspielen und Phase 4 die UI aufbauen kann. Ohne diesen Service sind alle nachgelagerten Phasen blockiert.
Output: 1 Service-Datei + 3 API-Route-Dateien
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Aus src/lib/db/schema.ts — alle relevanten Typen fuer diesen Plan -->

```typescript
// Tabelle: deliverable_modules
export const deliverableModules = pgTable('deliverable_modules', {
  id: uuid('id').primaryKey().defaultRandom(),
  tenantId: uuid('tenant_id').notNull(),
  code: varchar('code', { length: 10 }).notNull(),       // z.B. 'A1', 'B3'
  name: varchar('name', { length: 255 }).notNull(),
  category: varchar('category', { length: 10 }).notNull(), // 'A' | 'B' | 'C' | 'D'
  categoryCode: varchar('category_code', { length: 10 }), // 'V' | 'M' | 'IT' | ...
  ziel: text('ziel'),
  preis: varchar('preis', { length: 100 }),
  createdAt: timestamp('created_at'),
  updatedAt: timestamp('updated_at'),
})

// Tabelle: deliverables
export const deliverables = pgTable('deliverables', {
  id: uuid('id').primaryKey().defaultRandom(),
  tenantId: uuid('tenant_id').notNull(),
  moduleId: uuid('module_id'),   // FK -> deliverable_modules (nullable)
  name: varchar('name', { length: 255 }).notNull(),
  description: text('description'),
  format: varchar('format', { length: 100 }),
  umfang: varchar('umfang', { length: 100 }),
  trigger: varchar('trigger', { length: 255 }),
  category: varchar('category', { length: 50 }),
  categoryCode: varchar('category_code', { length: 10 }),
  status: varchar('status', { length: 20 }).default('draft'),
  version: varchar('version', { length: 20 }).default('1.0.0'),
  createdAt: timestamp('created_at'),
  updatedAt: timestamp('updated_at'),
})

// Tabelle: sopDocuments (relevant fuer LINK-03: SOPs die ein Deliverable produzieren)
export const sopDocuments = pgTable('sop_documents', {
  id: uuid('id').primaryKey().defaultRandom(),
  tenantId: uuid('tenant_id').notNull(),
  title: varchar('title', { length: 255 }).notNull(),
  category: varchar('category', { length: 50 }).notNull(),
  version: varchar('version', { length: 20 }),
  status: varchar('status', { length: 20 }),
  automationLevel: varchar('automation_level', { length: 20 }),
  maturityLevel: integer('maturity_level'),
  producesDeliverableId: uuid('produces_deliverable_id'), // FK -> deliverables
  sourceTaskId: varchar('source_task_id', { length: 50 }),
  deletedAt: timestamp('deleted_at'),
})

// Type exports (bereits in schema.ts vorhanden):
export type DeliverableModule = typeof deliverableModules.$inferSelect
export type NewDeliverableModule = typeof deliverableModules.$inferInsert
export type Deliverable = typeof deliverables.$inferSelect
export type NewDeliverable = typeof deliverables.$inferInsert
```

<!-- Aus src/lib/constants/framework.ts -->
```typescript
export const FRAMEWORK_CATEGORIES = [
  { code: 'V', label: 'Vertrieb' },
  { code: 'M', label: 'Marketing' },
  { code: 'IT', label: 'IT & Cybersicherheit' },
  { code: 'P', label: 'Projektmanagement' },
  { code: 'C', label: 'Compliance & DSGVO' },
  { code: 'F', label: 'Finanzen' },
  { code: 'HR', label: 'Human Resources' },
  { code: 'Q', label: 'Qualitaetssicherung' },
] as const

export const STATUS_ENUM = ['draft', 'review', 'approved', 'archived'] as const
```

<!-- Bestehende Service-Signatur (sop.service.ts) — exakt dieses Muster verwenden -->
```typescript
// Exports: export const ServiceName = { methodA(), methodB(), ... }
// Parameter: tenantId immer als erster Parameter
// Return: null fuer not-found, boolean fuer delete, T fuer gefundene Objekte
// Queries: db.select().from(table).where(and(...conditions))

import { db } from '@/lib/db'
import { eq, and, desc, isNull, ilike } from 'drizzle-orm'

export const SomeService = {
  async list(tenantId: string, filters?: { ... }) { ... },
  async getById(tenantId: string, id: string) { ... },
  async create(tenantId: string, data: Record<string, unknown>) { ... },
  async update(tenantId: string, id: string, data: Record<string, unknown>) { ... },
  async delete(tenantId: string, id: string): Promise<boolean> { ... },
}
```

<!-- API-Route-Muster (sop route.ts) — exakt dieses Muster verwenden -->
```typescript
import { NextRequest } from 'next/server'
import { apiSuccess, apiNotFound, apiServerError } from '@/lib/utils/api-response'
import { withPermission } from '@/lib/auth/require-permission'
import { SomeService } from '@/lib/services/some.service'

export async function GET(request: NextRequest) {
  return withPermission(request, 'module', 'read', async (auth) => {
    const { searchParams } = new URL(request.url)
    // ... params
    const items = await SomeService.list(auth.tenantId, filters)
    return apiSuccess(items)
  })
}

// [id]/route.ts pattern:
type Params = Promise<{ id: string }>
export async function GET(request: NextRequest, { params }: { params: Params }) {
  return withPermission(request, 'module', 'read', async (auth) => {
    const { id } = await params
    const item = await SomeService.getById(auth.tenantId, id)
    if (!item) return apiNotFound('Resource nicht gefunden')
    return apiSuccess(item)
  })
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeliverableService implementieren</name>
  <files>src/lib/services/deliverable.service.ts</files>
  <action>
Neue Datei erstellen: `src/lib/services/deliverable.service.ts`

Imports (exakt wie sop.service.ts):
```typescript
import { db } from '@/lib/db'
import { deliverables, deliverableModules, sopDocuments } from '@/lib/db/schema'
import { eq, and, desc, sql, isNull } from 'drizzle-orm'
```

Methoden in `export const DeliverableService = { ... }`:

**`list(tenantId, filters?)`**
- `filters?: { moduleId?: string; categoryCode?: string; status?: string }`
- conditions: `[eq(deliverables.tenantId, tenantId)]`
- Optionale conditions: `if (filters?.moduleId) conditions.push(eq(deliverables.moduleId, filters.moduleId))`
- Ebenso fuer `categoryCode` (Feld: `deliverables.categoryCode`) und `status` (Feld: `deliverables.status`)
- Query: `db.select().from(deliverables).where(and(...conditions)).orderBy(desc(deliverables.updatedAt))`
- Kein Pagination im Service (rohe Liste — Pagination erfolgt in der API-Route via `parsePaginationParams`)

**`getById(tenantId, id)`** — LINK-03: Verknuepfte SOPs einbinden
- Schritt 1: `const [deliverable] = await db.select().from(deliverables).where(and(eq(deliverables.tenantId, tenantId), eq(deliverables.id, id)))`
- Wenn kein Ergebnis: `return null`
- Schritt 2: Lade Modul wenn `deliverable.moduleId` gesetzt ist:
  ```typescript
  const [module] = deliverable.moduleId
    ? await db.select().from(deliverableModules).where(eq(deliverableModules.id, deliverable.moduleId))
    : []
  ```
- Schritt 3: Lade SOPs die dieses Deliverable produzieren (per LINK-03):
  ```typescript
  const producingSops = await db.select({
    id: sopDocuments.id,
    title: sopDocuments.title,
    category: sopDocuments.category,
    status: sopDocuments.status,
    automationLevel: sopDocuments.automationLevel,
    maturityLevel: sopDocuments.maturityLevel,
    sourceTaskId: sopDocuments.sourceTaskId,
  }).from(sopDocuments).where(
    and(
      eq(sopDocuments.tenantId, tenantId),
      eq(sopDocuments.producesDeliverableId, id),
      isNull(sopDocuments.deletedAt),
    )
  ).orderBy(sopDocuments.title)
  ```
- Return: `{ ...deliverable, module: module ?? null, producingSops }`

**`create(tenantId, data)`**
- `data` erwartet: `{ name, description?, format?, umfang?, trigger?, category?, categoryCode?, moduleId?, status?, version? }`
- `const [doc] = await db.insert(deliverables).values({ tenantId, name: data.name as string, ... }).returning()`
- Alle optionalen Felder mit `|| null` als Fallback
- Return: `doc`

**`update(tenantId, id, data)`**
- Updates-Objekt aufbauen: `const updates: Record<string, unknown> = { updatedAt: new Date() }`
- Fuer jedes bekannte Feld: `if (data.fieldName !== undefined) updates.fieldName = data.fieldName`
- Felder: `name`, `description`, `format`, `umfang`, `trigger`, `category`, `categoryCode`, `moduleId`, `status`
- `const [doc] = await db.update(deliverables).set(updates).where(and(eq(deliverables.tenantId, tenantId), eq(deliverables.id, id))).returning()`
- Return: `doc ?? null`

**`delete(tenantId, id)`**
- Hard-delete: `const r = await db.delete(deliverables).where(and(eq(deliverables.tenantId, tenantId), eq(deliverables.id, id))).returning({ id: deliverables.id })`
- Return: `r.length > 0`

**`getModulesWithCount(tenantId)`** — DEL-05
- Alle Module des Tenants laden + Anzahl verknuepfter Deliverables:
  ```typescript
  const modules = await db.select().from(deliverableModules)
    .where(eq(deliverableModules.tenantId, tenantId))
    .orderBy(deliverableModules.code)
  const counts = await db.select({
    moduleId: deliverables.moduleId,
    count: sql<number>`count(*)::int`,
  }).from(deliverables)
    .where(eq(deliverables.tenantId, tenantId))
    .groupBy(deliverables.moduleId)
  const countMap = Object.fromEntries(counts.map((c) => [c.moduleId, c.count]))
  return modules.map((m) => ({ ...m, deliverableCount: countMap[m.id] ?? 0 }))
  ```
  </action>
  <verify>
    <automated>cd C:/Daten/xkmu-business-os && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "deliverable.service" || echo "No type errors in deliverable.service"</automated>
  </verify>
  <done>
  `src/lib/services/deliverable.service.ts` existiert und exportiert `DeliverableService` mit den Methoden `list`, `getById`, `create`, `update`, `delete`, `getModulesWithCount`. TypeScript-Kompilierung ohne Fehler in dieser Datei.
  </done>
</task>

<task type="auto">
  <name>Task 2: API-Routen fuer Deliverables implementieren</name>
  <files>
    src/app/api/v1/deliverables/route.ts,
    src/app/api/v1/deliverables/[id]/route.ts,
    src/app/api/v1/deliverables/modules/route.ts
  </files>
  <action>
Drei API-Routen-Dateien erstellen. Alle folgen exakt dem Muster aus `src/app/api/v1/sops/route.ts`. Permission-Modul: `'processes'` (SOPs und Deliverables teilen das Prozess-Modul).

**Datei 1: `src/app/api/v1/deliverables/route.ts`**
```typescript
import { NextRequest } from 'next/server'
import { apiSuccess, apiServerError, parsePaginationParams } from '@/lib/utils/api-response'
import { DeliverableService } from '@/lib/services/deliverable.service'
import { withPermission } from '@/lib/auth/require-permission'

export async function GET(request: NextRequest) {
  return withPermission(request, 'processes', 'read', async (auth) => {
    const { searchParams } = new URL(request.url)
    const moduleId = searchParams.get('moduleId') || undefined
    const categoryCode = searchParams.get('categoryCode') || undefined
    const status = searchParams.get('status') || undefined
    const { page, limit } = parsePaginationParams(searchParams)
    const all = await DeliverableService.list(auth.tenantId, { moduleId, categoryCode, status })
    const total = all.length
    const items = all.slice((page - 1) * limit, page * limit)
    return apiSuccess(items, { page, limit, total, totalPages: Math.ceil(total / limit) })
  })
}

export async function POST(request: NextRequest) {
  return withPermission(request, 'processes', 'create', async (auth) => {
    try {
      const body = await request.json()
      const deliverable = await DeliverableService.create(auth.tenantId, body)
      return apiSuccess(deliverable, undefined, 201)
    } catch { return apiServerError() }
  })
}
```

**Datei 2: `src/app/api/v1/deliverables/[id]/route.ts`**
```typescript
import { NextRequest } from 'next/server'
import { apiSuccess, apiNotFound, apiServerError } from '@/lib/utils/api-response'
import { DeliverableService } from '@/lib/services/deliverable.service'
import { withPermission } from '@/lib/auth/require-permission'

type Params = Promise<{ id: string }>

export async function GET(request: NextRequest, { params }: { params: Params }) {
  return withPermission(request, 'processes', 'read', async (auth) => {
    const { id } = await params
    const deliverable = await DeliverableService.getById(auth.tenantId, id)
    if (!deliverable) return apiNotFound('Deliverable nicht gefunden')
    return apiSuccess(deliverable)
  })
}

export async function PATCH(request: NextRequest, { params }: { params: Params }) {
  return withPermission(request, 'processes', 'update', async (auth) => {
    try {
      const { id } = await params
      const body = await request.json()
      const deliverable = await DeliverableService.update(auth.tenantId, id, body)
      if (!deliverable) return apiNotFound('Deliverable nicht gefunden')
      return apiSuccess(deliverable)
    } catch { return apiServerError() }
  })
}

export async function DELETE(request: NextRequest, { params }: { params: Params }) {
  return withPermission(request, 'processes', 'delete', async (auth) => {
    const { id } = await params
    const deleted = await DeliverableService.delete(auth.tenantId, id)
    if (!deleted) return apiNotFound('Deliverable nicht gefunden')
    return apiSuccess({ deleted: true })
  })
}
```

**Datei 3: `src/app/api/v1/deliverables/modules/route.ts`**
```typescript
import { NextRequest } from 'next/server'
import { apiSuccess, apiServerError } from '@/lib/utils/api-response'
import { DeliverableService } from '@/lib/services/deliverable.service'
import { withPermission } from '@/lib/auth/require-permission'

export async function GET(request: NextRequest) {
  return withPermission(request, 'processes', 'read', async (auth) => {
    try {
      const modules = await DeliverableService.getModulesWithCount(auth.tenantId)
      return apiSuccess(modules)
    } catch { return apiServerError() }
  })
}
```

Verzeichnis-Struktur sicherstellen — Next.js App Router benoetigt:
- `src/app/api/v1/deliverables/route.ts`
- `src/app/api/v1/deliverables/[id]/route.ts`
- `src/app/api/v1/deliverables/modules/route.ts`

Hinweis: `modules` ist ein statisches Segment und muss VOR `[id]` aufgeloest werden — das App Router macht das automatisch korrekt, da statische Routen Vorrang haben.
  </action>
  <verify>
    <automated>cd C:/Daten/xkmu-business-os && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "(deliverables/route|deliverables/\[id\]|deliverables/modules)" || echo "No type errors in deliverable routes"</automated>
  </verify>
  <done>
  Alle drei Route-Dateien existieren. TypeScript-Kompilierung ohne Fehler. Manuelle Pruefung:
  - `src/app/api/v1/deliverables/route.ts` exportiert GET + POST
  - `src/app/api/v1/deliverables/[id]/route.ts` exportiert GET + PATCH + DELETE
  - `src/app/api/v1/deliverables/modules/route.ts` exportiert GET
  </done>
</task>

</tasks>

<verification>
TypeScript-Build ohne Fehler fuer alle neuen Dateien:
```bash
cd C:/Daten/xkmu-business-os && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20
```

Strukturpruefung (alle Dateien vorhanden):
```bash
ls C:/Daten/xkmu-business-os/src/lib/services/deliverable.service.ts
ls C:/Daten/xkmu-business-os/src/app/api/v1/deliverables/route.ts
ls C:/Daten/xkmu-business-os/src/app/api/v1/deliverables/[id]/route.ts
ls C:/Daten/xkmu-business-os/src/app/api/v1/deliverables/modules/route.ts
```
</verification>

<success_criteria>
- `GET /api/v1/deliverables` akzeptiert Query-Params `moduleId`, `categoryCode`, `status`, `page`, `limit` und gibt `{ data: [...], meta: { page, limit, total, totalPages } }` zurueck
- `GET /api/v1/deliverables/[id]` gibt Deliverable-Objekt mit `module` (oder null) und `producingSops: []` zurueck
- `GET /api/v1/deliverables/modules` gibt Module-Array mit `deliverableCount` pro Eintrag zurueck
- `POST /api/v1/deliverables` gibt 201 mit neuem Deliverable-Objekt zurueck
- `PATCH /api/v1/deliverables/[id]` aktualisiert und gibt aktualisiertes Objekt zurueck, 404 wenn nicht gefunden
- `DELETE /api/v1/deliverables/[id]` gibt `{ deleted: true }` oder 404 zurueck
- Alle Endpoints geben 401/403 bei fehlendem/unzureichendem Auth zurueck (via withPermission)
- TypeScript strict mode ohne Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/02-services-api-routen/02-01-SUMMARY.md` erstellen mit:
- Was implementiert wurde (Methoden, Routen)
- Welche Entscheidungen getroffen wurden (z.B. hard-delete vs. soft-delete fuer Deliverables)
- Datei-Liste
</output>
