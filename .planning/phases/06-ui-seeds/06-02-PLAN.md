---
phase: 06-ui-seeds
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/seed-check.ts
  - src/lib/db/seeds/management-framework.seed.ts
  - src/lib/db/seeds/deliverable-catalog.seed.ts
  - src/lib/db/seeds/sop-catalog.seed.ts
autonomous: true
requirements:
  - SEED-01
  - SEED-02
  - SEED-03

must_haves:
  truths:
    - "seed-check.ts: kein 'for (const t of allTenants)' Loop mehr vorhanden"
    - "seedDeliverableCatalog und seedSopCatalog werden einmal direkt mit TENANT_ID aufgerufen"
    - "Alle drei Seed-Funktionen akzeptieren tenantId als Parameter (Signatur unveraendert), ignorieren ihn intern"
    - "seed.ts standalone: unveraendert (slug bereits xkmu-digital-solutions aus Phase 1)"
  artifacts:
    - path: "src/lib/db/seed-check.ts"
      provides: "Kein Tenant-Loop, direkte TENANT_ID-Aufrufe"
    - path: "src/lib/db/seeds/management-framework.seed.ts"
      provides: "tenantId param bleibt, intern TENANT_ID verwendet"
    - path: "src/lib/db/seeds/deliverable-catalog.seed.ts"
      provides: "tenantId param bleibt, intern TENANT_ID verwendet"
    - path: "src/lib/db/seeds/sop-catalog.seed.ts"
      provides: "tenantId param bleibt, intern TENANT_ID verwendet"
  key_links:
    - from: "src/lib/db/seed-check.ts"
      to: "seedDeliverableCatalog / seedSopCatalog"
      via: "direkter Aufruf mit TENANT_ID (kein Loop)"
      pattern: "await seedDeliverableCatalog\\(TENANT_ID\\)"
---

<objective>
Den Tenant-Loop in seed-check.ts entfernen und die drei Seed-Services auf das TENANT_ID-Konstanten-Pattern umstellen (gleiche Soft-Removal-Strategie wie Phase 3/4 Services).

Purpose: Seeds sollen nicht mehr ueber alle Tenants iterieren — es gibt nur noch einen Tenant.
Output: seed-check.ts ruft seedDeliverableCatalog und seedSopCatalog einmal direkt auf; alle drei Seed-Funktionen arbeiten intern mit TENANT_ID statt dem Parameter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/constants/tenant.ts
</context>

<interfaces>
<!-- Bestehende Konstante aus Phase 2 -->
<!-- src/lib/constants/tenant.ts -->
export const TENANT_ID = '7b6c13c5-1800-47b2-a12f-10ccb11f6358' as const

<!-- Aktuelle Seed-Signaturen (unveraendert nach diesem Plan): -->
export async function seedManagementFramework(tenantId: string): Promise<void>
export async function seedDeliverableCatalog(tenantId: string): Promise<void>
export async function seedSopCatalog(tenantId: string): Promise<void>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: seed-check.ts — Tenant-Loop entfernen</name>
  <files>
    src/lib/db/seed-check.ts
  </files>
  <action>
    In src/lib/db/seed-check.ts:

    1. Import TENANT_ID hinzufuegen (sofern noch nicht vorhanden):
       ```typescript
       import { TENANT_ID } from '@/lib/constants/tenant'
       ```

    2. Den Tenant-Loop-Block (aktuell Zeilen ~874-882) ersetzen:

    VORHER:
    ```typescript
    // 15. Seed Deliverable Catalog (16 Module + 70 Deliverables) — fuer alle Tenants
    const allTenants = await db.select({ id: tenants.id }).from(tenants)
    for (const t of allTenants) {
      await seedDeliverableCatalog(t.id)
    }

    // 16. Seed SOP Catalog (alle SOPs aus Framework v2) — fuer alle Tenants
    for (const t of allTenants) {
      await seedSopCatalog(t.id)
    }
    ```

    NACHHER:
    ```typescript
    // 15. Seed Deliverable Catalog (16 Module + 70 Deliverables)
    await seedDeliverableCatalog(TENANT_ID)

    // 16. Seed SOP Catalog (alle SOPs aus Framework v2)
    await seedSopCatalog(TENANT_ID)
    ```

    3. Falls der Import von `tenants` aus dem Schema nur noch fuer den Loop gebraucht wurde
       und anderswo nicht benoetigt wird, den unbenutzten Import entfernen (pruefen per grep).
       Vorsichtig: tenants wird moeglicherweise noch an anderer Stelle in seed-check.ts verwendet
       (Tenant-Erstellung in der checkAndSeed-Funktion) — nur entfernen wenn wirklich unbenutzt.

    Per SEED-01.
  </action>
  <verify>
    grep -n "allTenants\|for.*of.*tenants\|for (const t" src/lib/db/seed-check.ts
    (erwartet: keine Treffer)
    grep -n "seedDeliverableCatalog\|seedSopCatalog" src/lib/db/seed-check.ts
    (erwartet: je genau eine Zeile mit direktem TENANT_ID-Aufruf)
  </verify>
  <done>
    - Kein for-Loop ueber Tenants mehr in seed-check.ts
    - seedDeliverableCatalog(TENANT_ID) und seedSopCatalog(TENANT_ID) direkt aufgerufen
    - npx tsc --noEmit ohne Fehler in dieser Datei
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed-Services auf TENANT_ID-Pattern umstellen</name>
  <files>
    src/lib/db/seeds/management-framework.seed.ts
    src/lib/db/seeds/deliverable-catalog.seed.ts
    src/lib/db/seeds/sop-catalog.seed.ts
  </files>
  <action>
    Gleiche Soft-Removal-Strategie wie Phase 3/4 Services anwenden:
    - Signatur bleibt (tenantId: string) — Rueckwaertskompat. mit seed-check.ts-Aufruf
    - Parameter intern umbenennen zu _tenantId (Prefix _ = "nicht verwendet")
    - Intern TENANT_ID aus @/lib/constants/tenant importieren und verwenden

    Fuer jede der drei Dateien:

    **management-framework.seed.ts:**
    ```typescript
    import { TENANT_ID } from '@/lib/constants/tenant'
    // ...
    export async function seedManagementFramework(_tenantId: string) {
      // Alle Stellen wo tenantId verwendet wurde -> TENANT_ID
      // z.B.:
      //   EosService.getVTO(tenantId)     -> EosService.getVTO(TENANT_ID)
      //   EosService.listRocks(tenantId)  -> EosService.listRocks(TENANT_ID)
      //   OkrService.getActiveCycle(tenantId) -> OkrService.getActiveCycle(TENANT_ID)
      //   SopService.list(tenantId)       -> SopService.list(TENANT_ID)
      //   db.select().from(users).where(eq(users.tenantId, tenantId)) -> eq(users.tenantId, TENANT_ID)
      // Alle weiteren Vorkommen von tenantId als Argument -> TENANT_ID
    }
    ```

    **deliverable-catalog.seed.ts:**
    ```typescript
    import { TENANT_ID } from '@/lib/constants/tenant'
    // ...
    export async function seedDeliverableCatalog(_tenantId: string) {
      // Alle tenantId Referenzen in Queries und Service-Aufrufen -> TENANT_ID
    }
    ```

    **sop-catalog.seed.ts:**
    ```typescript
    import { TENANT_ID } from '@/lib/constants/tenant'
    // ...
    export async function seedSopCatalog(_tenantId: string) {
      // Alle tenantId Referenzen in Queries und Service-Aufrufen -> TENANT_ID
    }
    ```

    Vorgehen pro Datei:
    1. TENANT_ID import oben hinzufuegen
    2. Funktionsparameter `tenantId` -> `_tenantId` umbenennen
    3. Im Funktionskoerper alle `tenantId` (als Variable verwendet) -> `TENANT_ID` ersetzen
       ACHTUNG: Nur die Variable-Verwendungen ersetzen, NICHT Feld-Namen wie `eq(users.tenantId, ...)` —
       dort bleibt `users.tenantId` (Spaltenname), nur der Wert aendert sich: `TENANT_ID`
    4. TypeScript-Check: npx tsc --noEmit

    Per SEED-02.
  </action>
  <verify>
    grep -n "tenantId)" src/lib/db/seeds/management-framework.seed.ts src/lib/db/seeds/deliverable-catalog.seed.ts src/lib/db/seeds/sop-catalog.seed.ts | grep -v "users\.tenantId\|companies\.tenantId\|_tenantId\|TENANT_ID"
    (erwartet: keine Treffer — alle ehemaligen tenantId-Variablen-Verwendungen sind ersetzt)

    npx tsc --noEmit 2>&1 | head -30
    (erwartet: keine Fehler in seed-Dateien)
  </verify>
  <done>
    - Alle drei Seed-Funktionen: Parameter heisst _tenantId, Koerper verwendet TENANT_ID
    - Signaturen unveraendert (string-Parameter bleibt fuer Rueckwaertskompat.)
    - TypeScript-Build gruen
    - seed.ts standalone: unveraendert geprueft (Slug xkmu-digital-solutions korrekt, kein Eingriff noetig — SEED-03 erfuellt)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed-Script -> DB | Schreibzugriff beim initialen Seed-Lauf |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | seed-check.ts TENANT_ID hardcode | accept | TENANT_ID ist die einzige Instanz — bewusste Design-Entscheidung der Uebergangsphase |
| T-06-02-02 | Information Disclosure | seed-check.ts | accept | Seed-Skript laeuft nur in controlled environments (Docker/local), keine externe Exposition |
</threat_model>

<verification>
Nach Ausfuehrung beider Tasks:

1. `grep -rn "allTenants\|for.*of.*tenants" src/lib/db/seed-check.ts` — keine Treffer
2. `grep -n "TENANT_ID" src/lib/db/seeds/management-framework.seed.ts` — mindestens 1 Treffer (Import)
3. `grep -n "TENANT_ID" src/lib/db/seeds/deliverable-catalog.seed.ts` — mindestens 1 Treffer (Import)
4. `grep -n "TENANT_ID" src/lib/db/seeds/sop-catalog.seed.ts` — mindestens 1 Treffer (Import)
5. `npx tsc --noEmit` — Build gruen
</verification>

<success_criteria>
- seed-check.ts: kein Tenant-Loop (SEED-01)
- Alle drei Seed-Services: _tenantId Parameter, intern TENANT_ID (SEED-02)
- seed.ts standalone: unveraendert geprueft, slug = xkmu-digital-solutions (SEED-03)
- TypeScript-Build gruen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06-ui-seeds/06-ui-seeds-02-SUMMARY.md` erstellen.
</output>
