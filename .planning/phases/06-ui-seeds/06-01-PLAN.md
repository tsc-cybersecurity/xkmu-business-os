---
phase: 06-ui-seeds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/intern/(dashboard)/settings/tenant/page.tsx
  - src/app/intern/(dashboard)/settings/organization/page.tsx
  - src/components/layout/sidebar.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "/intern/settings/organization laedt ohne Fehler"
    - "Die Sidebar zeigt 'Organisation' mit Link /intern/settings/organization (nicht /tenant)"
    - "Die Settings-Seite zeigt keine 'Tenant-ID'-Zeile mehr"
    - "Demo-Daten-Button funktioniert weiterhin (POST /api/v1/tenant/seed-demo)"
  artifacts:
    - path: "src/app/intern/(dashboard)/settings/organization/page.tsx"
      provides: "Umbenannte Settings-Seite unter neuer Route"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Sidebar ohne Tenant-Referenz im href"
  key_links:
    - from: "src/components/layout/sidebar.tsx"
      to: "/intern/settings/organization"
      via: "NavChild href"
      pattern: "settings/organization"
---

<objective>
Route /intern/settings/tenant nach /intern/settings/organization verschieben, Sidebar-Link anpassen, Tenant-ID-Anzeigefeld aus der Seite entfernen.

Purpose: UI soll kein Tenant-Konzept mehr zeigen — Nutzer sieht nur "Organisation".
Output: Neue Route organization/, alter tenant/-Ordner entfernt, Sidebar aktualisiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route umbenennen — tenant/ -> organization/</name>
  <files>
    src/app/intern/(dashboard)/settings/organization/page.tsx
  </files>
  <action>
    1. Lege Verzeichnis src/app/intern/(dashboard)/settings/organization/ an.
    2. Kopiere den Inhalt von src/app/intern/(dashboard)/settings/tenant/page.tsx in organization/page.tsx.
    3. In organization/page.tsx: benenne die Komponente von `TenantSettingsPage` in `OrganizationSettingsPage` um (Funktionsname + export default).
    4. Entferne die "Tenant-ID"-Zeile aus dem "Status & Abo"-Card (per UI-02). Das ist der Block ~Zeilen 556-559:
       ```
       <div className="flex items-center justify-between">
         <span className="text-sm text-muted-foreground">Tenant-ID</span>
         <code className="text-xs bg-muted px-2 py-0.5 rounded">{tenant?.id}</code>
       </div>
       ```
       Diesen Block komplett loeschen — er war read-only, enthielt keine Funktionalitaet.
    5. Das Interface `Tenant` und alle API-Calls (/api/v1/tenant GET/PUT, /api/v1/tenant/seed-demo, /api/v1/tenant/analyze) bleiben unveraendert — die API-Routen werden erst in Phase 7 umbenannt.
    6. Loesche den alten Ordner src/app/intern/(dashboard)/settings/tenant/ (page.tsx entfernen).
    Per UI-01, UI-02, UI-03.
  </action>
  <verify>
    curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/intern/settings/organization
    (erwartet: 200 nach Login, oder Redirect zur Login-Seite — kein 404)
  </verify>
  <done>
    - /intern/settings/organization/ existiert als page.tsx
    - /intern/settings/tenant/ existiert nicht mehr
    - Kein "Tenant-ID"-Feld sichtbar im Status-Card
    - Demo-Daten-Button und KI-Analyse-Button unveraendert vorhanden
  </done>
</task>

<task type="auto">
  <name>Task 2: Sidebar-Link auf /organization umbiegen</name>
  <files>
    src/components/layout/sidebar.tsx
  </files>
  <action>
    In src/components/layout/sidebar.tsx, im `navigation`-Array, Einstellungen-Gruppe:
    Aendere den NavChild-Eintrag fuer "Organisation":

    Vorher (Zeile ~145):
    ```typescript
    { name: 'Organisation', href: '/intern/settings/tenant', requiredModule: 'settings' },
    ```

    Nachher:
    ```typescript
    { name: 'Organisation', href: '/intern/settings/organization', requiredModule: 'settings' },
    ```

    Der Label "Organisation" ist bereits korrekt (kein "Tenant" sichtbar). Nur href aendern.
    Per UI-04.

    Pruefe die gesamte Datei auf weitere Vorkommnisse von "tenant" im Navigations-Kontext — keines erwartet, aber sicherheitshalber bestaetigen.
  </action>
  <verify>
    grep -n "settings/tenant" src/components/layout/sidebar.tsx
    (erwartet: keine Treffer mehr)
  </verify>
  <done>
    - sidebar.tsx hat href: '/intern/settings/organization'
    - Kein href mit '/intern/settings/tenant' mehr in der Datei
    - Build bleibt gruen: npx tsc --noEmit (kein Fehler in geaenderten Dateien)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Next.js | Seitenaufruf neuer Route durch eingeloggten Nutzer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | organization/page.tsx | accept | Seite bleibt hinter Auth-Guard (require-permission), keine neuen Daten exponiert |
| T-06-01-02 | Tampering | sidebar.tsx href-Aenderung | accept | Reines Rename, kein Sicherheits-Impact — alte Route existiert nicht mehr |
</threat_model>

<verification>
Nach Ausfuehrung beider Tasks:

1. `npx tsc --noEmit` — kein TypeScript-Fehler
2. `grep -rn "settings/tenant" src/components/ src/app/intern/` — keine Treffer
3. Verzeichnis `src/app/intern/(dashboard)/settings/tenant/` existiert nicht mehr
4. Verzeichnis `src/app/intern/(dashboard)/settings/organization/page.tsx` existiert
</verification>

<success_criteria>
- /intern/settings/organization erreichbar (UI-01)
- Keine "Tenant-ID"-Anzeige in der UI (UI-02)
- Demo-Seed-Button unveraendert funktionsfaehig (UI-03)
- Sidebar: href zeigt auf /organization, kein /tenant (UI-04)
- Login-UI unveraendert (UI-05 — kein Eingriff noetig)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06-ui-seeds/06-ui-seeds-01-SUMMARY.md` erstellen.
</output>
