---
phase: 02-auth-layer-vereinfachen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants/tenant.ts
  - src/lib/types/auth.types.ts
  - src/lib/services/user.service.ts
  - src/lib/auth/auth-context.ts
  - src/lib/auth/session.ts
  - src/app/api/v1/auth/login/route.ts
  - src/__tests__/unit/auth/auth-context.test.ts
  - src/__tests__/integration/api/auth.route.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "Login findet User direkt per Email (kein cross-tenant Iterieren)"
    - "SessionUser.tenantId existiert nicht mehr im JWT — alte Sessions werden abgewiesen"
    - "AuthContext.tenantId liefert immer TENANT_ID-Konstante (7b6c13c5-...)"
    - "API-Keys mit tenantId-Feld im Payload funktionieren weiter"
    - "users-Tabelle tenantId-FK bleibt im DB-Schema unveraendert"
  artifacts:
    - path: "src/lib/constants/tenant.ts"
      provides: "TENANT_ID-Konstante (7b6c13c5-1800-47b2-a12f-10ccb11f6358)"
      exports: ["TENANT_ID"]
    - path: "src/lib/types/auth.types.ts"
      provides: "SessionUser ohne tenantId, Session mit version-Feld"
      contains: "v: 2"
    - path: "src/lib/auth/auth-context.ts"
      provides: "getAuthContext gibt TENANT_ID statt session.user.tenantId zurueck"
    - path: "src/lib/services/user.service.ts"
      provides: "authenticate() ohne tenantId-Parameter, SessionUser ohne tenantId"
    - path: "src/app/api/v1/auth/login/route.ts"
      provides: "Login ohne cross-tenant Logik"
  key_links:
    - from: "src/app/api/v1/auth/login/route.ts"
      to: "src/lib/services/user.service.ts#authenticate"
      via: "UserService.authenticate(email, password) — kein tenantId-Parameter"
    - from: "src/lib/auth/auth-context.ts"
      to: "src/lib/constants/tenant.ts"
      via: "import { TENANT_ID } from '@/lib/constants/tenant'"
    - from: "src/lib/auth/session.ts"
      to: "src/lib/types/auth.types.ts"
      via: "Session-Version v=2 erzwingt Force-Logout alter Sessions"
---

<objective>
Auth-Layer von cross-tenant auf single-tenant umbauen.

Purpose: Phase 1 hat alle Daten in den einen xKMU-Tenant konsolidiert. Jetzt muss der
Auth-Code diese Realitaet widerspiegeln. Cross-tenant Suche, per-tenant Session-Felder
und Tenant-ID-Lookups aus dem JWT werden entfernt. Eine TENANT_ID-Konstante dient als
Uebergang bis Phase 7 alle tenantId-Referenzen loescht.

Output:
- src/lib/constants/tenant.ts (neu)
- src/lib/types/auth.types.ts (SessionUser ohne tenantId, Session + version)
- src/lib/auth/session.ts (getSession prueft version=2)
- src/lib/auth/auth-context.ts (TENANT_ID statt session.user.tenantId)
- src/lib/services/user.service.ts (authenticate ohne tenantId-Param)
- src/app/api/v1/auth/login/route.ts (direkter authenticate-Aufruf)
- Test-Updates fuer geaenderte Signaturen
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Key types before change (executor MUST understand what changes) -->
<interfaces>
<!-- CURRENT state — wird in Task 1+2 geaendert -->

From src/lib/types/auth.types.ts (CURRENT):
```typescript
export interface SessionUser {
  id: string
  tenantId: string        // <-- wird entfernt (AUTH-02)
  email: string
  firstName: string | null
  lastName: string | null
  role: UserRole
  roleId: string | null
}

export interface Session {
  user: SessionUser
  expiresAt: Date
  // kein version-Feld — altes Format
}

export interface ApiKeyPayload {
  tenantId: string        // <-- bleibt (AUTH-04)
  keyId: string
  permissions: string[]
}
```

From src/lib/auth/auth-context.ts (CURRENT):
```typescript
export interface AuthContext {
  tenantId: string        // bleibt, Wert kommt jetzt aus TENANT_ID
  userId: string | null
  role: string
  roleId: string | null
  apiKeyPermissions: string[] | null
}
// session-Zweig: tenantId: session.user.tenantId  <-- wird zu TENANT_ID
// api-key-Zweig: tenantId: payload.tenantId       <-- wird zu TENANT_ID
```

From src/lib/services/user.service.ts (CURRENT):
```typescript
authenticate(tenantId: string, email: string, password: string): Promise<AuthResult>
// intern: this.getByEmail(tenantId, email)
// gibt SessionUser zurueck mit tenantId gesetzt

getByEmail(tenantId: string, email: string): Promise<User | null>
// WHERE tenantId = $1 AND email = $2

findByEmail(email: string): Promise<User | null>
// WHERE email = $1 (existiert bereits, keine tenantId-Filterung!)
```

From src/app/api/v1/auth/login/route.ts (CURRENT):
```typescript
// Schritt 1: findByEmail(email) -> user (user.tenantId wird dann genutzt)
// Schritt 2: UserService.authenticate(user.tenantId, email, password)
// Problem: Schritt 1 war als cross-tenant gedacht; mit 1 Tenant redundant
```

ZIEL-STATE nach dem Plan:
```typescript
// auth.types.ts — SessionUser OHNE tenantId, Session MIT version
export interface SessionUser {
  id: string
  // tenantId entfernt
  email: string
  firstName: string | null
  lastName: string | null
  role: UserRole
  roleId: string | null
}
export interface Session {
  user: SessionUser
  expiresAt: Date
  v: 2                    // Versioniert — Force-Logout alter Sessions
}

// tenant.ts (neu)
export const TENANT_ID = '7b6c13c5-1800-47b2-a12f-10ccb11f6358'

// user.service.ts — authenticate ohne tenantId
authenticate(email: string, password: string): Promise<AuthResult>
// intern: findByEmail(email) dann bcrypt

// auth-context.ts — TENANT_ID statt session-Feld
import { TENANT_ID } from '@/lib/constants/tenant'
// tenantId: TENANT_ID (beide Zweige: session + api-key)

// login/route.ts — direkt authenticate
const result = await UserService.authenticate(email, password)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Konstante + Typen (Contracts first)</name>
  <files>
    src/lib/constants/tenant.ts
    src/lib/types/auth.types.ts
    src/lib/auth/session.ts
  </files>
  <behavior>
    - TENANT_ID exportiert den exakten Wert '7b6c13c5-1800-47b2-a12f-10ccb11f6358'
    - SessionUser hat kein tenantId-Feld mehr (TS-Fehler an allen Stellen, die es nutzen)
    - Session-Typ hat Feld `v: 2` als Literal-Type
    - getSession() gibt null zurueck wenn payload.v !== 2 (Force-Logout alter Sessions)
    - createSession() setzt v: 2 im JWT-Payload
  </behavior>
  <action>
1. Erstelle `src/lib/constants/tenant.ts`:
```typescript
/**
 * Einzige Tenant-ID der xKMU-Instanz.
 * Uebergangsphase bis Phase 7 (DB Hard Drop) — danach entfernt.
 */
export const TENANT_ID = '7b6c13c5-1800-47b2-a12f-10ccb11f6358' as const
```

2. Aendere `src/lib/types/auth.types.ts`:
- `SessionUser`: Feld `tenantId: string` ENTFERNEN (per AUTH-02)
- `Session`: Feld `v: 2` hinzufuegen als `v: 2` (Literal-Type, kein number)
- `ApiKeyPayload`: unveraendert lassen — `tenantId: string` bleibt (per AUTH-04)

Resultat:
```typescript
export type UserRole = 'owner' | 'admin' | 'member' | 'viewer'
export type UserStatus = 'active' | 'inactive' | 'pending'

export interface SessionUser {
  id: string
  // tenantId entfernt — AUTH-02
  email: string
  firstName: string | null
  lastName: string | null
  role: UserRole
  roleId: string | null
}

export interface Session {
  user: SessionUser
  expiresAt: Date
  v: 2    // Version — Force-Logout bei fehlendem v oder v !== 2
}

// AuthContext in auth.types.ts entfernen falls doppelt (es existiert auch in auth-context.ts)
export interface LoginCredentials {
  email: string
  password: string
}

export interface AuthResult {
  success: boolean
  user?: SessionUser
  error?: string
}

export interface ApiKeyPayload {
  tenantId: string   // bleibt — AUTH-04 (API-Keys bleiben kompatibel)
  keyId: string
  permissions: string[]
}
```

3. Aendere `src/lib/auth/session.ts`:

createSession(): `v: 2` in den JWT-Payload aufnehmen:
```typescript
const token = await new SignJWT({
  user,
  expiresAt: expiresAt.toISOString(),
  v: 2,                              // NEU
})
```

getSession(): Nach jwtVerify pruefe ob `v === 2`, sonst `return null` (Force-Logout):
```typescript
export async function getSession(): Promise<Session | null> {
  const cookieStore = await cookies()
  const token = cookieStore.get(SESSION_COOKIE_NAME)?.value
  if (!token) return null

  try {
    const { payload } = await jwtVerify(token, getJwtSecret())
    const session = payload as unknown as Session

    // Force-Logout: alte Sessions ohne v=2 werden abgewiesen
    if (session.v !== 2) return null

    if (new Date(session.expiresAt) < new Date()) return null

    return session
  } catch {
    return null
  }
}
```

HINWEIS fuer den Executor: Nach dieser Aenderung hat `SessionUser` kein `tenantId` mehr.
TypeScript wird an folgenden Stellen Fehler zeigen — die werden in Task 2 behoben:
- src/lib/services/user.service.ts (authenticate() baut SessionUser)
- src/lib/auth/auth-context.ts (liest session.user.tenantId)
- src/app/api/v1/auth/login/route.ts (liest user.tenantId)
- src/__tests__/helpers/fixtures.ts (sessionUser-Fixture hat tenantId)
- src/__tests__/integration/api/auth.route.test.ts (sessionUser-Objekt)
- src/__tests__/unit/services/user.service.test.ts (userFixture hat tenantId im SessionUser)

Diese Fehler sind erwartet und werden in Task 2 behoben.
  </action>
  <verify>
    <automated>cd /c/Daten/xkmu-business-os && npx tsc --noEmit 2>&1 | grep -E "auth.types|session.ts|tenant.ts" | head -20</automated>
  </verify>
  <done>
    - src/lib/constants/tenant.ts existiert, exportiert TENANT_ID mit korrektem UUID-Wert
    - SessionUser in auth.types.ts hat kein tenantId-Feld
    - Session in auth.types.ts hat Feld v: 2
    - session.ts createSession() schreibt v: 2 in JWT
    - session.ts getSession() gibt null wenn v !== 2
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Services + Routes + Tests reparieren</name>
  <files>
    src/lib/services/user.service.ts
    src/lib/auth/auth-context.ts
    src/app/api/v1/auth/login/route.ts
    src/__tests__/helpers/fixtures.ts
    src/__tests__/unit/auth/auth-context.test.ts
    src/__tests__/integration/api/auth.route.test.ts
    src/__tests__/unit/services/user.service.test.ts
  </files>
  <behavior>
    - UserService.authenticate(email, password) — kein tenantId-Parameter mehr
    - UserService.authenticate() nutzt intern findByEmail() (kein getByEmail mit tenantId)
    - SessionUser den authenticate() zurueckgibt: kein tenantId-Feld
    - getAuthContext() Session-Zweig: tenantId: TENANT_ID (aus Konstante, nicht aus session.user)
    - getAuthContext() API-Key-Zweig: tenantId: TENANT_ID (AUTH-04: payload.tenantId ignoriert)
    - login/route.ts ruft UserService.authenticate(email, password) direkt auf — kein findByEmail Vorschritt
    - Alle Tests kompilieren und laufen gruen durch
  </behavior>
  <action>
1. `src/lib/services/user.service.ts` — authenticate() umbauen (per AUTH-01, AUTH-02):

VORHER:
```typescript
async authenticate(tenantId: string, email: string, password: string): Promise<AuthResult> {
  const user = await this.getByEmail(tenantId, email)
  ...
  const sessionUser: SessionUser = {
    id: user.id,
    tenantId: user.tenantId,   // <-- entfernen
    email: user.email,
    ...
  }
}
```

NACHHER:
```typescript
async authenticate(email: string, password: string): Promise<AuthResult> {
  const user = await this.findByEmail(email)  // kein tenantId-Parameter

  if (!user) {
    return { success: false, error: 'Invalid credentials' }
  }

  if (user.status !== 'active') {
    return { success: false, error: 'Account is not active' }
  }

  const isValid = await bcrypt.compare(password, user.passwordHash)
  if (!isValid) {
    return { success: false, error: 'Invalid credentials' }
  }

  await db
    .update(users)
    .set({ lastLoginAt: new Date() })
    .where(eq(users.id, user.id))

  const sessionUser: SessionUser = {
    id: user.id,
    // tenantId entfernt — AUTH-02
    email: user.email,
    firstName: user.firstName,
    lastName: user.lastName,
    role: user.role as SessionUser['role'],
    roleId: user.roleId ?? null,
  }

  return { success: true, user: sessionUser }
}
```

Die Methoden `getByEmail(tenantId, email)` und die anderen tenantId-parametrisierten
Methoden BLEIBEN unveraendert — sie werden in Phase 3 (Services) bereinigt.
AUTH-05: user.tenantId in der DB bleibt, wir schreiben es nur nicht mehr in den JWT.

2. `src/lib/auth/auth-context.ts` — TENANT_ID nutzen (per AUTH-03):

```typescript
import { NextRequest } from 'next/server'
import { getSession } from '@/lib/auth/session'
import { validateApiKey, getApiKeyFromRequest } from '@/lib/auth/api-key'
import { TENANT_ID } from '@/lib/constants/tenant'

export interface AuthContext {
  tenantId: string
  userId: string | null
  role: string
  roleId: string | null
  apiKeyPermissions: string[] | null
}

export async function getAuthContext(
  request: NextRequest
): Promise<AuthContext | null> {
  // Session-Auth pruefen
  const session = await getSession()
  if (session) {
    return {
      tenantId: TENANT_ID,              // AUTH-03: statische Konstante, nicht aus JWT
      userId: session.user.id,
      role: session.user.role,
      roleId: session.user.roleId ?? null,
      apiKeyPermissions: null,
    }
  }

  // API-Key-Auth pruefen
  const apiKey = getApiKeyFromRequest(request)
  if (apiKey) {
    const payload = await validateApiKey(apiKey)
    if (payload) {
      return {
        tenantId: TENANT_ID,            // AUTH-04: payload.tenantId ignoriert, TENANT_ID stattdessen
        userId: null,
        role: 'api',
        roleId: null,
        apiKeyPermissions: payload.permissions,
      }
    }
  }

  return null
}
```

3. `src/app/api/v1/auth/login/route.ts` — direkter authenticate-Aufruf (per AUTH-01):

VORHER:
```typescript
const user = await UserService.findByEmail(email)
if (!user) { return apiError(...) }
const result = await UserService.authenticate(user.tenantId, email, password)
```

NACHHER:
```typescript
// AUTH-01: kein cross-tenant findByEmail Vorschritt mehr noetig.
// authenticate() sucht intern per findByEmail() direkt.
const result = await UserService.authenticate(email, password)

if (!result.success || !result.user) {
  return apiError('INVALID_CREDENTIALS', result.error || 'Invalid credentials', 401)
}

await createSession(result.user)
return apiSuccess({ user: result.user })
```

Vollstaendige Route nach dem Umbau:
```typescript
import { NextRequest } from 'next/server'
import { apiSuccess, apiError, apiValidationError } from '@/lib/utils/api-response'
import { loginSchema, validateAndParse, formatZodErrors } from '@/lib/utils/validation'
import { UserService } from '@/lib/services/user.service'
import { createSession } from '@/lib/auth/session'
import { logger } from '@/lib/utils/logger'
import { rateLimit } from '@/lib/utils/rate-limit'

export async function POST(request: NextRequest) {
  try {
    const limited = await rateLimit(request, 'auth-login', 10, 60_000)
    if (limited) return limited

    const body = await request.json()

    const validation = validateAndParse(loginSchema, body)
    if (!validation.success) {
      return apiValidationError(formatZodErrors(validation.errors))
    }

    const { email, password } = validation.data

    // AUTH-01: direkte Email-Suche, kein cross-tenant Iterieren
    const result = await UserService.authenticate(email, password)

    if (!result.success || !result.user) {
      return apiError('INVALID_CREDENTIALS', result.error || 'Invalid credentials', 401)
    }

    await createSession(result.user)

    return apiSuccess({ user: result.user })
  } catch (error) {
    logger.error('Login failed', error, { module: 'AuthLogin' })
    return apiError('LOGIN_FAILED', 'Login failed', 500)
  }
}
```

4. Test-Fixtures aktualisieren:

`src/__tests__/helpers/fixtures.ts`:
- `sessionUser`-Objekte: `tenantId`-Feld entfernen
- `authFixture()`: `tenantId` bleibt in AuthContext (kommt jetzt aus TENANT_ID, nicht SessionUser)

```typescript
// fixtures.ts — sessionUser-shape (wo auch immer sie vorkommen):
// VORHER: { id, tenantId, email, firstName, lastName, role, roleId }
// NACHHER: { id, email, firstName, lastName, role, roleId }
// AuthContext bleibt unveraendert (hat weiterhin tenantId aus TENANT_ID)
```

`src/__tests__/integration/api/auth.route.test.ts`:
- `sessionUser`-Konstante am Anfang der Datei: `tenantId`-Feld entfernen
- `dbUser`-Objekt: `tenantId` kann bleiben (kommt aus DB-User-Typ, nicht SessionUser)
- Mock fuer `UserService.authenticate`: Signatur aendern — kein tenantId-Parameter
  - `findByEmail` Mock entfernen (nicht mehr in login-route genutzt)
  - `authenticate` wird jetzt direkt mit (email, password) gemockt

Konkrete Aenderung im auth.route.test.ts fuer den Login-200-Test:
```typescript
// VORHER:
vi.doMock('@/lib/services/user.service', () => ({
  UserService: {
    findByEmail: vi.fn().mockResolvedValue(dbUser),
    authenticate: vi.fn().mockResolvedValue({ success: true, user: sessionUser }),
  },
}))

// NACHHER:
vi.doMock('@/lib/services/user.service', () => ({
  UserService: {
    // findByEmail entfernt — login-route nutzt es nicht mehr direkt
    authenticate: vi.fn().mockResolvedValue({ success: true, user: sessionUser }),
  },
}))
```

`src/__tests__/unit/services/user.service.test.ts`:
- authenticate-Tests: tenantId-Parameter aus allen Aufrufen entfernen
- sessionUser-Fixtures in authenticate-Tests: tenantId-Feld entfernen
- getByEmail-Tests und andere tenantId-parametrisierten Methoden: unveraendert lassen

`src/__tests__/unit/auth/auth-context.test.ts` (NEU erstellen falls nicht vorhanden):
Prueft den TENANT_ID-Fluss:
```typescript
// auth-context.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { TENANT_ID } from '@/lib/constants/tenant'

vi.mock('@/lib/auth/session', () => ({ getSession: vi.fn() }))
vi.mock('@/lib/auth/api-key', () => ({
  getApiKeyFromRequest: vi.fn(),
  validateApiKey: vi.fn(),
}))

describe('getAuthContext()', () => {
  beforeEach(() => { vi.resetModules() })

  it('session-Zweig: tenantId ist TENANT_ID-Konstante', async () => {
    const { getSession } = await import('@/lib/auth/session')
    vi.mocked(getSession).mockResolvedValue({
      user: { id: 'user-1', email: 'a@b.de', firstName: null, lastName: null, role: 'admin', roleId: null },
      expiresAt: new Date('2099-01-01'),
      v: 2,
    })

    const { getAuthContext } = await import('@/lib/auth/auth-context')
    const req = new Request('http://localhost/') as any
    const ctx = await getAuthContext(req)

    expect(ctx?.tenantId).toBe(TENANT_ID)
    expect(ctx?.userId).toBe('user-1')
  })

  it('api-key-Zweig: tenantId ist TENANT_ID-Konstante, nicht payload.tenantId', async () => {
    const { getSession } = await import('@/lib/auth/session')
    vi.mocked(getSession).mockResolvedValue(null)

    const { getApiKeyFromRequest, validateApiKey } = await import('@/lib/auth/api-key')
    vi.mocked(getApiKeyFromRequest).mockReturnValue('xkmu_somekey')
    vi.mocked(validateApiKey).mockResolvedValue({
      tenantId: 'different-tenant-id',   // wird ignoriert
      keyId: 'key-1',
      permissions: ['*'],
    })

    const { getAuthContext } = await import('@/lib/auth/auth-context')
    const req = new Request('http://localhost/') as any
    const ctx = await getAuthContext(req)

    expect(ctx?.tenantId).toBe(TENANT_ID)   // TENANT_ID, nicht 'different-tenant-id'
  })

  it('gibt null zurueck wenn weder Session noch API-Key vorhanden', async () => {
    const { getSession } = await import('@/lib/auth/session')
    vi.mocked(getSession).mockResolvedValue(null)

    const { getApiKeyFromRequest } = await import('@/lib/auth/api-key')
    vi.mocked(getApiKeyFromRequest).mockReturnValue(null)

    const { getAuthContext } = await import('@/lib/auth/auth-context')
    const req = new Request('http://localhost/') as any
    const ctx = await getAuthContext(req)

    expect(ctx).toBeNull()
  })
})
```
  </action>
  <verify>
    <automated>cd /c/Daten/xkmu-business-os && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/__tests__/unit/auth/ src/__tests__/integration/api/auth.route.test.ts src/__tests__/unit/services/user.service.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` gibt 0 Fehler zurueck
    - Alle Tests in src/__tests__/unit/auth/ gruen
    - src/__tests__/integration/api/auth.route.test.ts gruen
    - src/__tests__/unit/services/user.service.test.ts gruen
    - `grep -r "session.user.tenantId" src/lib/auth/` liefert keine Treffer
    - `grep -rn "tenantId" src/lib/types/auth.types.ts` zeigt nur ApiKeyPayload.tenantId
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Auth-Layer komplett umgebaut:
    - TENANT_ID-Konstante in src/lib/constants/tenant.ts
    - SessionUser.tenantId entfernt (breaking change)
    - Session v: 2 erzwingt Force-Logout alter Sessions
    - getAuthContext() nutzt TENANT_ID statt session.user.tenantId
    - UserService.authenticate() ohne tenantId-Parameter
    - Login-Route ohne cross-tenant findByEmail Vorschritt
  </what-built>
  <how-to-verify>
    1. App bauen: `npm run build` — muss ohne Fehler durchlaufen
    2. Login testen auf bos.dev.xkmu.de (oder lokal):
       - Alten Cookie loeschen (oder anderen Browser nutzen)
       - Login mit xkmu-Admin-Credentials — muss 200 zurueckgeben
       - Nach Login: /intern/dashboard muss laden (zeigt dass Session funktioniert)
    3. API-Key testen (falls verfuegbar):
       - curl -H "x-api-key: xkmu_..." https://bos.dev.xkmu.de/api/v1/companies
       - Muss 200 zurueckgeben (nicht 401)
    4. Force-Logout pruefen:
       - Alten Session-Cookie (ohne v=2) in Browser setzen — muss zu Login umleiten
       - (Optional) JWT auf jwt.io dekodieren: muss `"v": 2` im Payload zeigen
  </how-to-verify>
  <resume-signal>Tippe "approved" oder beschreibe aufgetretene Fehler</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/v1/auth/login | Unvertrauenswuerdige Credentials kommen rein |
| JWT-Cookie → getSession() | Manipulierter Cookie moeglich |
| x-api-key Header → validateApiKey() | Gestohlene/ungueltige Keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | POST /api/v1/auth/login | mitigate | Rate-Limit (10/min per IP) existiert bereits in rateLimit(); bleibt erhalten |
| T-02-02 | Tampering | JWT-Cookie (session.ts) | mitigate | JWT wird mit HS256 + JWT_SECRET signiert; Manipulation erkennbar. v: 2 Pruefung verhindert Token-Downgrade. |
| T-02-03 | Repudiation | Login-Events | accept | Kein Audit-Log fuer Logins — akzeptiert fuer Single-Tenant-Instanz mit 1 User |
| T-02-04 | Information Disclosure | Login-Fehler | mitigate | Login gibt immer 'Invalid credentials' zurueck (kein User-Enumeration-Leak durch gleichen Fehlertext fuer falsches PW und unbekannte Email) |
| T-02-05 | Elevation of Privilege | TENANT_ID-Konstante in Auth-Context | accept | TENANT_ID ist eine hartcodierte Konstante im Source-Code — kein Angriffsvector; Wert ist ohnehin oeffentlich in der DB-Migration sichtbar |
| T-02-06 | Denial of Service | bcrypt in authenticate() | accept | bcrypt ist CPU-intensiv; Rate-Limit (T-02-01) begrenzt Angriffe ausreichend fuer Single-Tenant |
</threat_model>

<verification>
## Phase-weite Checks nach Plan-Abschluss

```bash
# 1. Kein session.user.tenantId mehr in auth-Layer
grep -rn "session.user.tenantId\|session\.tenantId" src/lib/auth/
# Erwartung: keine Treffer

# 2. TENANT_ID-Konstante korrekt
grep -rn "TENANT_ID" src/lib/constants/tenant.ts
# Erwartung: 7b6c13c5-1800-47b2-a12f-10ccb11f6358

# 3. SessionUser.tenantId entfernt
grep -n "tenantId" src/lib/types/auth.types.ts
# Erwartung: nur in ApiKeyPayload (Zeile ~37 ca.)

# 4. TypeScript sauber
npx tsc --noEmit
# Erwartung: exit code 0, keine Fehler

# 5. Tests
npx vitest run src/__tests__/unit/auth/ src/__tests__/integration/api/auth.route.test.ts
# Erwartung: alle gruen

# 6. Build
npm run build
# Erwartung: kein Error
```
</verification>

<success_criteria>
1. `npx tsc --noEmit` — exit code 0
2. `npm run build` — baut erfolgreich durch
3. Login auf der App funktioniert mit xkmu-Credentials
4. getAuthContext() liefert immer tenantId = '7b6c13c5-1800-47b2-a12f-10ccb11f6358'
5. Alte Session-Cookies ohne v=2 werden abgewiesen (Force-Logout)
6. API-Keys mit tenantId-Feld im Payload funktionieren weiter
7. `grep -rn "session.user.tenantId" src/lib/auth/` — keine Treffer
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/02-auth-layer-vereinfachen/02-01-SUMMARY.md` mit:
- Was geaendert wurde (welche Methoden, welche Typen)
- Force-Logout-Mechanismus dokumentiert (v: 2)
- Verifikations-Ergebnisse
- Abgedeckte Requirements: AUTH-01 bis AUTH-05
- Offene Punkte fuer Phase 3 (Services-Batch: tenantId-Parameter in getByEmail etc. bleiben noch)
</output>
