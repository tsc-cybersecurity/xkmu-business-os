---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - src/lib/services/grundschutz-asset.service.ts
  - src/lib/services/grundschutz-audit.service.ts
  - src/lib/services/wiba-audit.service.ts
  - src/lib/services/din-audit.service.ts
  - src/lib/services/document-calculation.service.ts
  - src/lib/services/document-template.service.ts
  - src/lib/services/email-template.service.ts
  - src/lib/services/contract-template.service.ts
  - src/lib/services/contract-clause.service.ts
  - src/lib/services/cockpit.service.ts
  - src/lib/services/receipt.service.ts
  - src/lib/services/webhook.service.ts
  - src/lib/services/activity.service.ts
  - src/lib/services/business-document.service.ts
  - src/lib/services/business-profile.service.ts
  - src/lib/services/company-research.service.ts
  - src/lib/services/firecrawl-research.service.ts
  - src/lib/services/firecrawl.service.ts
  - src/lib/services/idea.service.ts
  - src/lib/services/serpapi.service.ts
  - src/lib/services/tenant-seed.service.ts
autonomous: true
requirements: [SVC-21, SVC-22]

must_haves:
  truths:
    - "No eq(X.tenantId, tenantId) filter remains in any of the 21 targeted services"
    - "All INSERT statements use TENANT_ID constant instead of passed tenantId"
    - "All method signatures use _tenantId prefix for the now-unused parameter"
    - "Build stays green — no TypeScript errors introduced"
  artifacts:
    - path: "src/lib/services/grundschutz-asset.service.ts"
      provides: "Grundschutz asset queries without tenant filter"
    - path: "src/lib/services/wiba-audit.service.ts"
      provides: "WiBA audit queries without tenant filter"
    - path: "src/lib/services/din-audit.service.ts"
      provides: "DIN audit queries without tenant filter"
    - path: "src/lib/services/cockpit.service.ts"
      provides: "Cockpit aggregation without tenant filter"
    - path: "src/lib/services/tenant-seed.service.ts"
      provides: "Seed service using TENANT_ID constant for all inserts"
  key_links:
    - from: "src/lib/services/*.service.ts"
      to: "src/lib/constants/tenant.ts"
      via: "import { TENANT_ID }"
      pattern: "import.*TENANT_ID.*from.*constants/tenant"
---

<objective>
Apply the soft-removal pattern to 21 Compliance/Audit/Document services — the first half of Phase 4.

Purpose: Remove all tenant-scoped WHERE filters so every query operates on the single-tenant dataset without requiring a tenantId context.

Output: 21 service files refactored, build green, no functional regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-services-entkoppeln-batch-1/03-01-SUMMARY.md
@src/lib/constants/tenant.ts
@src/lib/services/lead.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grundschutz, Audit and Compliance Services (11 services)</name>
  <files>
    src/lib/services/grundschutz-asset.service.ts,
    src/lib/services/grundschutz-audit.service.ts,
    src/lib/services/wiba-audit.service.ts,
    src/lib/services/din-audit.service.ts,
    src/lib/services/document-calculation.service.ts,
    src/lib/services/document-template.service.ts,
    src/lib/services/email-template.service.ts,
    src/lib/services/contract-template.service.ts,
    src/lib/services/contract-clause.service.ts,
    src/lib/services/cockpit.service.ts,
    src/lib/services/receipt.service.ts
  </files>
  <action>
Apply the Phase 3 soft-removal pattern (documented in 03-01-SUMMARY.md and visible in lead.service.ts) identically to each of the 11 services listed above.

For every service file:

1. Add `import { TENANT_ID } from '@/lib/constants/tenant'` at the top (skip if already present).

2. Rename every `tenantId: string` parameter to `_tenantId: string` (or `_tenantId?: string` if already optional). This marks it as intentionally unused without breaking callers.

3. Remove every `eq(X.tenantId, tenantId)` clause from WHERE conditions:
   - If it was the sole condition: remove the entire WHERE clause (returns all rows — correct for single-tenant).
   - If it was inside `and(eq(X.tenantId, tenantId), ...rest)`: simplify to `and(...rest)` or bare `eq(...rest)` if only one remains.
   - Clean up `conditions` arrays: remove the tenantId push, adjust `and(...conditions)` accordingly.

4. In INSERT `values({ ... })`: replace `tenantId` (the parameter) with `TENANT_ID` (the constant). Do NOT remove the tenantId column from the values object — the DB column still exists until Phase 7.

5. cockpit.service.ts may aggregate across multiple tables — remove tenant filter from every sub-query.

6. receipt.service.ts — same pattern; watch for any join that also filters by tenantId on the joined table.

Commit after all 11 are done: `feat(04-01): soft-remove tenant filters from 11 compliance/audit services`
  </action>
  <verify>
    <automated>grep -rn "eq(grundschutzAssets.tenantId\|eq(grundschutzAudits.tenantId\|eq(wibaAudits.tenantId\|eq(dinAudits.tenantId\|eq(documentCalculations.tenantId\|eq(documentTemplates.tenantId\|eq(emailTemplates.tenantId\|eq(contractTemplates.tenantId\|eq(contractClauses.tenantId\|eq(receipts.tenantId" /c/Daten/xkmu-business-os/src/lib/services/ 2>/dev/null | grep -v "_tenantId" || echo "CLEAN"</automated>
  </verify>
  <done>No eq(X.tenantId, tenantId) filter (using the function parameter) remains in any of the 11 service files. TENANT_ID constant used for all inserts. TypeScript compiles without errors on these files.</done>
</task>

<task type="auto">
  <name>Task 2: Utility, Research, Seed Services (10 services)</name>
  <files>
    src/lib/services/webhook.service.ts,
    src/lib/services/activity.service.ts,
    src/lib/services/business-document.service.ts,
    src/lib/services/business-profile.service.ts,
    src/lib/services/company-research.service.ts,
    src/lib/services/firecrawl-research.service.ts,
    src/lib/services/firecrawl.service.ts,
    src/lib/services/idea.service.ts,
    src/lib/services/serpapi.service.ts,
    src/lib/services/tenant-seed.service.ts
  </files>
  <action>
Apply the same soft-removal pattern to these 10 services. Same rules as Task 1 with two special cases:

SPECIAL — serpapi.service.ts: This is an admin/utility service wrapping external API calls. Apply the standard pattern to any internal DB queries; external API calls do not use tenantId in queries so nothing to remove there.

SPECIAL — tenant-seed.service.ts: This service SEEDS demo data and therefore writes rows WITH a tenantId. The goal is NOT to remove seeding functionality — it is to stop accepting a dynamic tenantId from callers.
  - Rename the `tenantId` parameter to `_tenantId` to mark it unused.
  - In every INSERT `values({ tenantId: tenantId, ... })` replace with `values({ tenantId: TENANT_ID, ... })`.
  - Do NOT remove the tenantId column from INSERT values — it still inserts into the DB column.
  - Do NOT remove WHERE filters that look up existing records during seeding (e.g. "find if already seeded") — these are correct lookups, not tenant scoping filters.
  - If there is a check like `eq(someTable.tenantId, tenantId)` used to detect "already seeded for this tenant": replace with `eq(someTable.tenantId, TENANT_ID)` (the constant, not the param).

After all 10 files are done, run a TypeScript build check:
  `cd /c/Daten/xkmu-business-os && npx tsc --noEmit 2>&1 | head -50`

Fix any type errors introduced (typically: unused import, wrong parameter type after rename).

Commit: `feat(04-01): soft-remove tenant filters from utility/research/seed services`
  </action>
  <verify>
    <automated>grep -rn "eq(webhooks.tenantId\|eq(activities.tenantId\|eq(businessDocuments.tenantId\|eq(businessProfiles.tenantId\|eq(ideas.tenantId\|eq(serpapi" /c/Daten/xkmu-business-os/src/lib/services/ 2>/dev/null | grep -v "_tenantId" || echo "CLEAN"</automated>
  </verify>
  <done>All 10 service files apply TENANT_ID constant for inserts and do not filter queries by the passed tenantId parameter. tenant-seed.service.ts still seeds correctly using TENANT_ID. Build check returns zero errors for modified files.</done>
</task>

<task type="auto">
  <name>Task 3: Build validation</name>
  <files></files>
  <action>
Run full TypeScript check to confirm no regressions across all 21 files:

```
cd /c/Daten/xkmu-business-os && npx tsc --noEmit 2>&1 | head -80
```

If errors appear:
- Identify the file and line.
- Common causes: removed a variable that was still used (e.g. forgot to replace tenantId in one spot), or `and()` called with undefined after partial removal.
- Fix, re-run until clean.

Then run a quick smoke grep to confirm completeness:
```
grep -rn "eq(.*\.tenantId, tenantId)" /c/Daten/xkmu-business-os/src/lib/services/grundschutz-asset.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/grundschutz-audit.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/wiba-audit.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/din-audit.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/document-calculation.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/document-template.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/email-template.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/contract-template.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/contract-clause.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/cockpit.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/receipt.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/webhook.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/activity.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/business-document.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/business-profile.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/company-research.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/firecrawl-research.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/firecrawl.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/idea.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/serpapi.service.ts \
  /c/Daten/xkmu-business-os/src/lib/services/tenant-seed.service.ts 2>/dev/null
```

Expected: no output (all clean).
  </action>
  <verify>
    <automated>cd /c/Daten/xkmu-business-os && npx tsc --noEmit 2>&1 | grep -E "error TS" | wc -l</automated>
  </verify>
  <done>TypeScript reports 0 errors. Smoke grep returns no matches for eq(X.tenantId, tenantId) pattern across all 21 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service layer | Internal only — no new public surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | All queries after tenant filter removal | accept | Single-tenant instance — all data belongs to one org, no cross-org leakage possible |
| T-04-02 | Tampering | tenant-seed.service.ts using hardcoded TENANT_ID | accept | Seeding is admin-triggered, TENANT_ID is the only valid tenant ID in system |
</threat_model>

<verification>
After both commits:

1. `grep -rn "eq(.*\.tenantId, tenantId)" src/lib/services/` on all 21 files returns no output.
2. `npx tsc --noEmit` returns 0 errors.
3. `grep -rn "import.*TENANT_ID" src/lib/services/` shows the import in all files that do INSERT operations.
</verification>

<success_criteria>
- All 21 services listed in files_modified have been refactored.
- No tenantId function parameter is used in WHERE clauses (renamed to _tenantId, unused).
- All INSERT tenantId values use the TENANT_ID constant.
- TypeScript build is clean.
- tenant-seed.service.ts still seeds data correctly (uses TENANT_ID constant, not caller-supplied value).
</success_criteria>

<output>
After completion, create `.planning/phases/04-services-entkoppeln-batch-2/04-01-SUMMARY.md` following the template at @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
