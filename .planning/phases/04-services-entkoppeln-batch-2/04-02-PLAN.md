---
phase: "04"
plan: "02"
type: execute
wave: 1
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - src/lib/services/social-publishing.service.ts
  - src/lib/services/social-media-post.service.ts
  - src/lib/services/social-media-topic.service.ts
  - src/lib/services/marketing-template.service.ts
  - src/lib/services/marketing-task.service.ts
  - src/lib/services/marketing-campaign.service.ts
  - src/lib/services/chat.service.ts
  - src/lib/services/feedback.service.ts
  - src/lib/services/product-category.service.ts
  - src/lib/services/media-upload.service.ts
  - src/lib/services/image-optimizer.service.ts
  - src/lib/services/email.service.ts
  - src/lib/services/api-key.service.ts
  - src/lib/services/cron.service.ts
  - src/lib/services/cms-navigation.service.ts
  - src/lib/services/cms-block-template.service.ts
  - src/lib/services/cms-block.service.ts
  - src/lib/services/user.service.ts
autonomous: true
requirements: [SVC-21, SVC-22]

must_haves:
  truths:
    - "No eq(X.tenantId, tenantId) filter remains in any of the 18 targeted services"
    - "user.service.ts tenantId handling left untouched (AUTH-05)"
    - "All INSERT statements in refactored services use TENANT_ID constant"
    - "CMS services (cms-navigation, cms-block-template, cms-block) are global — their trivial tenantId refs removed"
    - "Build stays green — no TypeScript errors"
  artifacts:
    - path: "src/lib/services/social-publishing.service.ts"
      provides: "Social publishing without tenant filter"
    - path: "src/lib/services/marketing-campaign.service.ts"
      provides: "Marketing campaign queries without tenant filter"
    - path: "src/lib/services/user.service.ts"
      provides: "User service — tenantId left in place per AUTH-05"
    - path: "src/lib/services/api-key.service.ts"
      provides: "API key service without tenant filter on lookups"
  key_links:
    - from: "src/lib/services/*.service.ts"
      to: "src/lib/constants/tenant.ts"
      via: "import { TENANT_ID }"
      pattern: "import.*TENANT_ID.*from.*constants/tenant"
---

<objective>
Apply the soft-removal pattern to 18 Marketing/Communication/Misc services — the second half of Phase 4. Together with Plan 04-01, this completes the full 40-service batch.

Purpose: Remove remaining tenant-scoped WHERE filters so every query operates globally on the single-tenant dataset.

Output: 18 service files refactored (or confirmed as needing no changes), build green, Phase 4 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-services-entkoppeln-batch-1/03-01-SUMMARY.md
@src/lib/constants/tenant.ts
@src/lib/services/lead.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Social, Marketing and Communication Services (9 services)</name>
  <files>
    src/lib/services/social-publishing.service.ts,
    src/lib/services/social-media-post.service.ts,
    src/lib/services/social-media-topic.service.ts,
    src/lib/services/marketing-template.service.ts,
    src/lib/services/marketing-task.service.ts,
    src/lib/services/marketing-campaign.service.ts,
    src/lib/services/chat.service.ts,
    src/lib/services/feedback.service.ts,
    src/lib/services/product-category.service.ts
  </files>
  <action>
Apply the Phase 3 soft-removal pattern (documented in 03-01-SUMMARY.md, visible in lead.service.ts) identically to each of the 9 services listed above.

For every service file:

1. Add `import { TENANT_ID } from '@/lib/constants/tenant'` at the top (skip if already present).

2. Rename every `tenantId: string` parameter to `_tenantId: string` (or `_tenantId?: string` if already optional).

3. Remove every `eq(X.tenantId, tenantId)` clause from WHERE conditions:
   - Sole condition: remove entire WHERE / drop the filter entirely (all rows returned — correct for single-tenant).
   - Inside `and(eq(X.tenantId, tenantId), ...rest)`: simplify to `and(...rest)` or bare `eq(...rest)` if only one arg remains.
   - `conditions` array pattern: remove the tenantId push; adjust the final `and(...conditions)` if array can now be empty.

4. In INSERT `values({ ... })`: replace `tenantId` (param) with `TENANT_ID` (constant). Keep the column in the values object.

5. social-publishing.service.ts may call external APIs (social platform posting) — apply pattern only to internal DB queries, leave external API parameters unchanged.

6. feedback.service.ts — check if it queries a global feedback table (no tenant in feedback concept per memory). Still apply the pattern if tenantId refs exist; they were added historically.

Commit after all 9 are done: `feat(04-02): soft-remove tenant filters from social/marketing/communication services`
  </action>
  <verify>
    <automated>grep -rn "eq(socialPosts.tenantId\|eq(socialTopics.tenantId\|eq(marketingTemplates.tenantId\|eq(marketingTasks.tenantId\|eq(marketingCampaigns.tenantId\|eq(chats.tenantId\|eq(feedback.tenantId\|eq(productCategories.tenantId" /c/Daten/xkmu-business-os/src/lib/services/ 2>/dev/null | grep -v "_tenantId" || echo "CLEAN"</automated>
  </verify>
  <done>No eq(X.tenantId, tenantId) filter (using the function parameter) remains in any of the 9 service files. TENANT_ID constant used for all inserts. TypeScript compiles without errors on these files.</done>
</task>

<task type="auto">
  <name>Task 2: Media, Infrastructure and CMS Services (8 services + user special case)</name>
  <files>
    src/lib/services/media-upload.service.ts,
    src/lib/services/image-optimizer.service.ts,
    src/lib/services/email.service.ts,
    src/lib/services/api-key.service.ts,
    src/lib/services/cron.service.ts,
    src/lib/services/cms-navigation.service.ts,
    src/lib/services/cms-block-template.service.ts,
    src/lib/services/cms-block.service.ts,
    src/lib/services/user.service.ts
  </files>
  <action>
Apply the same soft-removal pattern to the 8 infrastructure/CMS services, with one strict special case for user.service.ts.

**Standard services (apply full pattern):** media-upload, image-optimizer, email, api-key, cron, cms-navigation, cms-block-template, cms-block.

Notes:
- cms-navigation, cms-block-template, cms-block are global (no tenant concept per project memory). They likely have very few refs (1 each). Remove any tenantId filter found; these tables serve all users equally.
- api-key.service.ts — tenantId in API key payload is metadata kept for backwards compat (AUTH-04), but DB query filters by tenantId must be removed. Lookups should work by key value alone.
- image-optimizer.service.ts likely has 1 ref or none — still verify and apply if found.
- email.service.ts may wrap an SMTP/external call. Apply pattern to any DB persistence queries; leave SMTP parameters alone.
- cron.service.ts likely has 2 refs — apply standard pattern.

**SPECIAL — user.service.ts (AUTH-05: DO NOT REFACTOR tenantId):**
The users table keeps tenantId for user-organisation assignment. The auth layer was already updated in Phase 2. For user.service.ts:
- Do NOT rename tenantId parameters.
- Do NOT remove tenantId from WHERE clauses.
- Do NOT replace values.tenantId with TENANT_ID.
- Simply read the file to confirm it is already in an acceptable state, and leave it unchanged.
- If you find any cross-tenant iteration patterns (e.g., looping over multiple tenantIds), those can be removed — but single-tenant tenantId equality checks stay.

After all 9 files are processed (8 refactored + 1 verified-unchanged), run:
`cd /c/Daten/xkmu-business-os && npx tsc --noEmit 2>&1 | head -50`

Commit: `feat(04-02): soft-remove tenant filters from media/infra/cms services`
  </action>
  <verify>
    <automated>grep -rn "eq(mediaUploads.tenantId\|eq(apiKeys.tenantId\|eq(cronJobs.tenantId\|eq(cmsNavigation.tenantId\|eq(cmsBlockTemplates.tenantId\|eq(cmsBlocks.tenantId" /c/Daten/xkmu-business-os/src/lib/services/ 2>/dev/null | grep -v "_tenantId" || echo "CLEAN"</automated>
  </verify>
  <done>All 8 infrastructure/CMS service files refactored. user.service.ts left unchanged. Build check returns no errors for modified files.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 4 completion validation</name>
  <files></files>
  <action>
Run full TypeScript check to confirm Phase 4 is complete with no regressions:

```
cd /c/Daten/xkmu-business-os && npx tsc --noEmit 2>&1 | head -80
```

Fix any errors introduced by this plan's changes (not pre-existing errors from other phases).

Then run a broad completeness check across ALL service files to detect any remaining tenantId filter patterns that were missed across Phase 3 AND Phase 4:

```
grep -rn "eq(.*\.tenantId, tenantId)" /c/Daten/xkmu-business-os/src/lib/services/ 2>/dev/null | grep -v "user.service.ts"
```

Expected: no output (user.service.ts is the only allowed exception).

If unexpected matches appear:
- Identify the service.
- Determine if it was supposed to be in Phase 3 or Phase 4.
- Apply the standard soft-removal fix immediately and commit with `fix(04-02): missed tenantId filter in {service-name}`.

Final summary commit if no fixes needed:
`chore(04-02): phase 4 batch 2 complete — 18 services decoupled`
  </action>
  <verify>
    <automated>grep -rn "eq(.*\.tenantId, tenantId)" /c/Daten/xkmu-business-os/src/lib/services/ 2>/dev/null | grep -v "user.service.ts" | wc -l</automated>
  </verify>
  <done>
- TypeScript build is clean (0 errors in this plan's files).
- grep across all service files returns 0 matches for eq(X.tenantId, tenantId) — excluding user.service.ts.
- Phase 4 is fully complete: all ~40 services from Batch 2 processed.
- Together with Phase 3, all 60 services are decoupled from tenant filtering.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service layer | Internal only — no new public surface introduced |
| user.service.ts | Intentionally preserves tenantId for user-org assignment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | api-key.service.ts after tenant filter removal | accept | Single-tenant — all API keys belong to the one org; no cross-org leakage possible |
| T-04-04 | Information Disclosure | chat.service.ts after tenant filter removal | accept | Single-tenant instance; all chat data is for one org |
| T-04-05 | Elevation of Privilege | user.service.ts unchanged (intentional) | accept | AUTH-05 decision: users.tenantId kept for user-org assignment; no privilege escalation risk |
</threat_model>

<verification>
After all commits in this plan:

1. `grep -rn "eq(.*\.tenantId, tenantId)" src/lib/services/` excluding user.service.ts returns 0 matches.
2. `npx tsc --noEmit` returns 0 errors.
3. user.service.ts diff shows no changes (confirmed untouched).
4. Phase 4 combined (04-01 + 04-02): all ~40 services processed, together with Phase 3 covering all ~60 services.
</verification>

<success_criteria>
- All 17 non-user services listed in files_modified have been refactored (tenantId → _tenantId, TENANT_ID constant for inserts, no WHERE filters).
- user.service.ts is verified unchanged (AUTH-05 compliance).
- CMS services (navigation, block-template, block) confirmed as global with no remaining tenant filters.
- TypeScript build is clean.
- Phase 4 milestone: combined with Plan 04-01, all ~40 Batch 2 services are decoupled. All 60 services across Phase 3 + Phase 4 are now tenant-filter-free.
</success_criteria>

<output>
After completion, create `.planning/phases/04-services-entkoppeln-batch-2/04-02-SUMMARY.md` following the template at @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
