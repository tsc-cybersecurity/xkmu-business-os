---
phase: 01-db-schema-migrations
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib/db/schema.ts
autonomous: true
requirements:
  - DEL-01
  - DEL-02
  - SOP-01
  - SOP-02
  - SOP-03
  - SOP-04
  - SOP-05
  - SOP-06
  - SOP-07
  - SOP-08
  - SOP-09
  - EXEC-01

must_haves:
  truths:
    - "Tabellen deliverable_modules und deliverables existieren in der DB mit allen Spalten und tenant_id-FKs"
    - "Tabelle execution_logs existiert mit allen Spalten gemaess Framework-Schema"
    - "sopDocuments hat alle 7 neuen Felder — alle nullable"
    - "sopSteps hat das neue Feld executor — nullable"
    - "Drizzle-Push wurde erfolgreich ausgefuehrt und DB-Schema ist synchron"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Vollstaendiges DB-Schema mit allen neuen Tabellen und ALTER-TABLE-Feldern"
      contains:
        - "export const deliverableModules"
        - "export const deliverables"
        - "export const executionLogs"
        - "automationLevel"
        - "aiCapable"
        - "maturityLevel"
        - "producesDeliverableId"
        - "subprocess"
        - "sourceTaskId"
        - "estimatedDurationMinutes"
        - "executor"
  key_links:
    - from: "deliverables"
      to: "tenants"
      via: "tenantId FK — .references(() => tenants.id, { onDelete: 'cascade' })"
    - from: "deliverables"
      to: "deliverableModules"
      via: "moduleId FK — .references(() => deliverableModules.id)"
    - from: "sopDocuments"
      to: "deliverables"
      via: "producesDeliverableId FK — nullable, references deliverables.id"
    - from: "executionLogs"
      to: "tenants"
      via: "tenantId FK — .references(() => tenants.id, { onDelete: 'cascade' })"
---

<objective>
Drizzle-Schema in schema.ts um alle neuen Tabellen und Felder erweitern, dann DB-Migration ausfuehren.

Purpose: Alle nachfolgenden Phasen (Services, Seeds, UI) brauchen die physischen Tabellen und Spalten in der PostgreSQL-DB. Ohne erfolgreiche Migration kann keine der Folge-Phasen ausgefuehrt werden.

Output:
- `deliverableModules` Tabelle (15 Module A1-D3)
- `deliverables` Tabelle (63 Deliverables mit Modul-FK)
- `executionLogs` Tabelle (Ausfuehrungs-Protokoll)
- sopDocuments: 7 neue nullable Felder
- sopSteps: 1 neues nullable Feld `executor`
- Drizzle-Push erfolgreich ausgefuehrt
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-db-schema-migrations/01-01-SUMMARY.md

<interfaces>
<!-- Bestehendes Schema — relevante Ausschnitte aus src/lib/db/schema.ts -->

Imports am Anfang der Datei (Zeile 1-19):
```typescript
import {
  pgTable, uuid, varchar, text, timestamp, jsonb,
  boolean, integer, decimal, numeric, real, inet,
  index, serial, smallint, char,
} from 'drizzle-orm/pg-core'
import { relations } from 'drizzle-orm'
```

Bestehendes sopDocuments-Schema (Zeile 3118-3141):
```typescript
export const sopDocuments = pgTable('sop_documents', {
  id: uuid('id').primaryKey().defaultRandom(),
  tenantId: uuid('tenant_id').notNull().references(() => tenants.id, { onDelete: 'cascade' }),
  title: varchar('title', { length: 255 }).notNull(),
  category: varchar('category', { length: 50 }).notNull(),
  version: varchar('version', { length: 20 }).default('1.0.0'),
  status: varchar('status', { length: 20 }).default('draft'),
  ownerId: uuid('owner_id').references(() => users.id, { onDelete: 'set null' }),
  purpose: text('purpose'),
  scope: text('scope'),
  tools: text('tools').array().default([]),
  linkedSopIds: uuid('linked_sop_ids').array().default([]),
  tags: text('tags').array().default([]),
  approvedAt: timestamp('approved_at', { withTimezone: true }),
  approvedBy: uuid('approved_by').references(() => users.id),
  reviewDate: timestamp('review_date', { withTimezone: true }),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow(),
  deletedAt: timestamp('deleted_at', { withTimezone: true }),
}, ...)
```

Bestehendes sopSteps-Schema (Zeile 3146-3158):
```typescript
export const sopSteps = pgTable('sop_steps', {
  id: uuid('id').primaryKey().defaultRandom(),
  sopId: uuid('sop_id').notNull().references(() => sopDocuments.id, { onDelete: 'cascade' }),
  sequence: integer('sequence').notNull(),
  title: varchar('title', { length: 255 }).notNull(),
  description: text('description'),
  responsible: varchar('responsible', { length: 100 }),
  estimatedMinutes: integer('estimated_minutes'),
  checklistItems: text('checklist_items').array().default([]),
  warnings: text('warnings').array().default([]),
}, ...)
```

Aktuelle Type-Exports am Ende (Zeile 3175-3190):
```typescript
export type VTO = typeof vto.$inferSelect
export type Rock = typeof rocks.$inferSelect
// ... (weitere bestehende Types)
export type SopDocument = typeof sopDocuments.$inferSelect
export type NewSopDocument = typeof sopDocuments.$inferInsert
export type SopStep = typeof sopSteps.$inferSelect
export type SopVersion = typeof sopVersions.$inferSelect
```

Enum-Konstanten aus Plan 01-01:
  AutomationLevelEnum = 'manual' | 'semi' | 'full'
  ExecutorEnum        = 'agent' | 'human' | 'flex'
  EntityTypeEnum      = 'sop' | 'deliverable'
  ExecutedByEnum      = 'agent' | 'human'
  ExecutionStatusEnum = 'completed' | 'aborted' | 'escalated'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Neue Tabellen und geaenderte Tabellen in schema.ts einfuegen</name>
  <files>src/lib/db/schema.ts</files>
  <action>
Die Datei `src/lib/db/schema.ts` an DREI Stellen erweitern:

---

### Stelle A: Neue Tabellen nach dem letzten Management-Framework-Block einfuegen

Direkt NACH `export const sopVersions = pgTable(...)` und dem zugehoerigen Index-Block (aktuell ab Zeile ~3163), aber BEVOR die Type-Exports beginnen (Zeile ~3175), folgende Bloecke einfuegen:

```typescript
// ============================================
// Framework v2: Deliverable-Module
// ============================================
export const deliverableModules = pgTable('deliverable_modules', {
  id: uuid('id').primaryKey().defaultRandom(),
  tenantId: uuid('tenant_id').notNull().references(() => tenants.id, { onDelete: 'cascade' }),
  // Modul-Identifikation
  code: varchar('code', { length: 10 }).notNull(),        // z.B. 'A1', 'B3', 'D2'
  name: varchar('name', { length: 255 }).notNull(),        // z.B. 'Neukundenakquise'
  // Kategorie (Buchstabe A-D aus Deliverable-Katalog)
  category: varchar('category', { length: 10 }).notNull(), // 'A' | 'B' | 'C' | 'D'
  categoryCode: varchar('category_code', { length: 10 }),  // z.B. 'V', 'M', 'IT' (Framework-Kategorie)
  // Inhalt
  ziel: text('ziel'),                                      // Modulziel aus Katalog
  preis: varchar('preis', { length: 100 }),                // z.B. '1.200 EUR'
  // Meta
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow(),
}, (table) => [
  index('idx_deliverable_modules_tenant').on(table.tenantId),
  index('idx_deliverable_modules_code').on(table.tenantId, table.code),
])

// ============================================
// Framework v2: Deliverables
// ============================================
export const deliverables = pgTable('deliverables', {
  id: uuid('id').primaryKey().defaultRandom(),
  tenantId: uuid('tenant_id').notNull().references(() => tenants.id, { onDelete: 'cascade' }),
  // Modul-Zuordnung
  moduleId: uuid('module_id').references(() => deliverableModules.id, { onDelete: 'set null' }),
  // Identifikation
  name: varchar('name', { length: 255 }).notNull(),
  description: text('description'),
  // Typ & Format
  format: varchar('format', { length: 100 }),              // z.B. 'PDF', 'DOCX', 'Excel'
  umfang: varchar('umfang', { length: 100 }),              // z.B. '5-10 Seiten'
  trigger: varchar('trigger', { length: 255 }),            // Was loest die Erstellung aus?
  // Kategorisierung (Framework-Kategorie)
  category: varchar('category', { length: 50 }),           // z.B. 'IT & Cybersicherheit'
  categoryCode: varchar('category_code', { length: 10 }),  // z.B. 'IT', 'V', 'M'
  // Status & Versionierung
  status: varchar('status', { length: 20 }).default('draft'),
  version: varchar('version', { length: 20 }).default('1.0.0'),
  // Meta
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow(),
}, (table) => [
  index('idx_deliverables_tenant').on(table.tenantId),
  index('idx_deliverables_module').on(table.tenantId, table.moduleId),
  index('idx_deliverables_category').on(table.tenantId, table.categoryCode),
  index('idx_deliverables_status').on(table.tenantId, table.status),
])

// ============================================
// Framework v2: Execution Logs
// ============================================
export const executionLogs = pgTable('execution_logs', {
  id: uuid('id').primaryKey().defaultRandom(),
  tenantId: uuid('tenant_id').notNull().references(() => tenants.id, { onDelete: 'cascade' }),
  // Was wurde ausgefuehrt?
  entityType: varchar('entity_type', { length: 20 }).notNull(), // 'sop' | 'deliverable'
  entityId: uuid('entity_id').notNull(),
  entityVersion: varchar('entity_version', { length: 20 }),
  // Zeitstempel
  startedAt: timestamp('started_at', { withTimezone: true }).notNull(),
  completedAt: timestamp('completed_at', { withTimezone: true }),
  // Wer/Wie
  executedBy: varchar('executed_by', { length: 10 }).notNull(), // 'agent' | 'human'
  // Ergebnis
  status: varchar('status', { length: 20 }).notNull(),          // 'completed' | 'aborted' | 'escalated'
  abortReason: text('abort_reason'),
  qualityScore: real('quality_score'),                           // 0.0 - 1.0
  durationMinutes: real('duration_minutes'),
  costEstimateUsd: real('cost_estimate_usd'),
  flags: text('flags').array().default([]),
  // Verknuepfungen
  linkedClientId: uuid('linked_client_id'),
  linkedProjectId: uuid('linked_project_id'),
  // Genehmigung
  humanApproved: boolean('human_approved').default(false),
  humanApprovedBy: uuid('human_approved_by').references(() => users.id, { onDelete: 'set null' }),
  humanApprovedAt: timestamp('human_approved_at', { withTimezone: true }),
  // Meta
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
}, (table) => [
  index('idx_execution_logs_tenant').on(table.tenantId),
  index('idx_execution_logs_entity').on(table.tenantId, table.entityType, table.entityId),
  index('idx_execution_logs_status').on(table.tenantId, table.status),
  index('idx_execution_logs_started').on(table.tenantId, table.startedAt),
])
```

---

### Stelle B: sopDocuments — 7 neue nullable Felder hinzufuegen

Im bestehenden `sopDocuments = pgTable(...)` Block, NACH der Zeile `deletedAt: timestamp('deleted_at', { withTimezone: true }),` und BEVOR die schliessende `}, (table) => [` Klammer, folgende Felder einfuegen:

```typescript
  // Framework v2: Neue Felder (alle nullable fuer Abwaertskompatibilitaet)
  automationLevel: varchar('automation_level', { length: 20 }),           // SOP-01: 'manual' | 'semi' | 'full'
  aiCapable: boolean('ai_capable'),                                         // SOP-02
  maturityLevel: integer('maturity_level'),                                 // SOP-03: 1-5
  estimatedDurationMinutes: integer('estimated_duration_minutes'),          // SOP-05
  producesDeliverableId: uuid('produces_deliverable_id').references(() => deliverables.id, { onDelete: 'set null' }), // SOP-06
  subprocess: varchar('subprocess', { length: 255 }),                      // SOP-07
  sourceTaskId: varchar('source_task_id', { length: 50 }),                 // SOP-08: z.B. 'KP1-01'
```

WICHTIG: Kein `.notNull()` auf diesen Feldern — alle bleiben implizit nullable.

---

### Stelle C: sopSteps — 1 neues nullable Feld hinzufuegen

Im bestehenden `sopSteps = pgTable(...)` Block, NACH der Zeile `warnings: text('warnings').array().default([]),` und BEVOR die schliessende `}, (table) => [` Klammer, folgendes Feld einfuegen:

```typescript
  // Framework v2: Neues Feld (nullable fuer Abwaertskompatibilitaet)
  executor: varchar('executor', { length: 10 }),  // SOP-04: 'agent' | 'human' | 'flex'
```

---

### Stelle D: Type-Exports am Ende der Datei erweitern

Direkt nach den bestehenden Type-Exports (nach `export type SopVersion = typeof sopVersions.$inferSelect`) folgende Types anhaengen:

```typescript
// Type exports: Framework v2
export type DeliverableModule = typeof deliverableModules.$inferSelect
export type NewDeliverableModule = typeof deliverableModules.$inferInsert
export type Deliverable = typeof deliverables.$inferSelect
export type NewDeliverable = typeof deliverables.$inferInsert
export type ExecutionLog = typeof executionLogs.$inferSelect
export type NewExecutionLog = typeof executionLogs.$inferInsert
```

---

REIHENFOLGE-HINWEIS: `deliverables` muss VOR `sopDocuments` im Schema definiert werden, weil `sopDocuments.producesDeliverableId` auf `deliverables.id` referenziert. Die aktuelle Reihenfolge in schema.ts:

1. ... bestehende Tabellen ...
2. sopDocuments (bestehend, jetzt erweitert)
3. sopSteps (bestehend, jetzt erweitert)
4. sopVersions (bestehend, unveraendert)

Da `producesDeliverableId` in sopDocuments auf `deliverables` verweist, und Drizzle forward-references via arrow functions `() => deliverables.id` erlaubt, ist die Einfuereihenfolge so:

- Neue Tabellen (deliverableModules, deliverables, executionLogs) direkt NACH sopVersions einfuegen
- Die sopDocuments-Erweiterung mit `() => deliverables.id` forward-reference funktioniert auch wenn deliverables spaeter im File steht — Drizzle loest das lazy auf

Alternativ (sicherer): deliverableModules und deliverables VOR sopDocuments einfuegen (also vor Zeile 3118). In diesem Fall:
- deliverableModules vor sopDocuments
- deliverables vor sopDocuments
- executionLogs nach sopVersions
- sopDocuments-Erweiterung kann dann direkt auf deliverables.id referenzieren
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - schema.ts enthaelt die 3 neuen Tabellen: deliverableModules, deliverables, executionLogs
    - sopDocuments enthaelt alle 7 neuen Felder: automationLevel, aiCapable, maturityLevel, estimatedDurationMinutes, producesDeliverableId, subprocess, sourceTaskId — alle ohne .notNull()
    - sopSteps enthaelt das neue Feld: executor — ohne .notNull()
    - 6 neue Type-Exports am Ende der Datei
    - `npx tsc --noEmit` gibt keine neuen Fehler aus
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Drizzle-Push ausfuehren</name>
  <files></files>
  <action>
WICHTIG: Diesen Schritt erst ausfuehren, nachdem Task 1 vollstaendig abgeschlossen und TypeScript-Compiler fehlerfrei ist.

Ausfuehren im Projekt-Root:

```bash
npx drizzle-kit push
```

Erwartetes Ergebnis:
- Drizzle erkennt die Aenderungen am Schema
- Es werden neue Tabellen erstellt: `deliverable_modules`, `deliverables`, `execution_logs`
- Neue Spalten werden zu `sop_documents` hinzugefuegt: `automation_level`, `ai_capable`, `maturity_level`, `estimated_duration_minutes`, `produces_deliverable_id`, `subprocess`, `source_task_id`
- Neue Spalte wird zu `sop_steps` hinzugefuegt: `executor`
- Drizzle fragt bei destruktiven Aenderungen nach — bei reinen ADD TABLE / ADD COLUMN keine destruktiven Aenderungen erwartet, daher sollte der Push ohne manuelle Bestaetigung durchlaufen

Falls der Push nach Bestaetigung fragt (interaktiv): Mit `y` oder `yes` bestaetigen.

Falls ein Fehler auftritt (z.B. forward-reference-Problem mit producesDeliverableId):
  Option A: deliverableModules und deliverables im File VOR sopDocuments verschieben, dann erneut pushen.
  Option B: producesDeliverableId vorlaeufig ohne FK-constraint definieren:
    `producesDeliverableId: uuid('produces_deliverable_id'),`
    Dann FK nachtraeglich in Task 3 per SQL hinzufuegen (aber bevorzuge Option A).

Nach erfolgreichem Push: Ausgabe des Push-Kommandos protokollieren.
  </action>
  <verify>
    <automated>npx drizzle-kit push --dry-run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npx drizzle-kit push` laeuft ohne Fehler durch
    - Tabellen deliverable_modules, deliverables, execution_logs existieren in der DB
    - sop_documents hat die 7 neuen Spalten
    - sop_steps hat die neue Spalte executor
    - Dry-run zeigt "No schema changes detected" (alles synchron)
  </done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks folgende Checks ausfuehren:

1. TypeScript-Compiler sauber:
   ```bash
   npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"
   ```

2. Neue Tabellen per SQL pruefen (psql oder Drizzle Studio):
   ```sql
   SELECT table_name FROM information_schema.tables
   WHERE table_schema = 'public'
     AND table_name IN ('deliverable_modules', 'deliverables', 'execution_logs')
   ORDER BY table_name;
   ```
   Erwartet: 3 Zeilen

3. Neue Spalten in sop_documents pruefen:
   ```sql
   SELECT column_name, data_type, is_nullable
   FROM information_schema.columns
   WHERE table_name = 'sop_documents'
     AND column_name IN (
       'automation_level', 'ai_capable', 'maturity_level',
       'estimated_duration_minutes', 'produces_deliverable_id',
       'subprocess', 'source_task_id'
     )
   ORDER BY column_name;
   ```
   Erwartet: 7 Zeilen, alle is_nullable = 'YES'

4. Neue Spalte in sop_steps pruefen:
   ```sql
   SELECT column_name, data_type, is_nullable
   FROM information_schema.columns
   WHERE table_name = 'sop_steps' AND column_name = 'executor';
   ```
   Erwartet: 1 Zeile, is_nullable = 'YES'

5. Drizzle-Studio optional (visuell):
   ```bash
   npx drizzle-kit studio
   ```
</verification>

<success_criteria>
1. Tabellen `deliverable_modules` und `deliverables` existieren mit allen Spalten und tenant_id-FKs (DEL-01, DEL-02)
2. Tabelle `execution_logs` existiert mit allen Spalten aus Framework-Schema (EXEC-01)
3. `sop_documents` hat alle 7 neuen Felder — alle nullable (SOP-01 bis SOP-09)
4. `sop_steps` hat das neue Feld `executor` — nullable (SOP-04)
5. `npx tsc --noEmit` meldet keine Fehler
6. `npx drizzle-kit push` war erfolgreich — DB und Schema sind synchron
7. 6 neue TypeScript Type-Exports in schema.ts (DeliverableModule, NewDeliverableModule, Deliverable, NewDeliverable, ExecutionLog, NewExecutionLog)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/01-db-schema-migrations/01-02-SUMMARY.md` erstellen mit:
- Ergebnis des Drizzle-Push (Ausgabe / Tabellen erstellt)
- Tatsaechliche Reihenfolge der Tabellen in schema.ts (fuer Referenz in Phase 2)
- Alle neuen TypeScript-Typen und ihre Felder (vollstaendig auflisten)
- Aufgetretene Probleme (z.B. forward-reference) und wie geloest
- Zeilen-Nummern der neuen Bloecke in schema.ts (fuer Phase 2 / schnelles Nachschlagen)
</output>
