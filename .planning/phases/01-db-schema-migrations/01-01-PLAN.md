---
phase: 01-db-schema-migrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants/framework.ts
autonomous: true
requirements:
  - ENUM-01
  - ENUM-02
  - ENUM-03

must_haves:
  truths:
    - "TypeScript-Konstanten fuer alle Kategorien (V, M, IT, P, C, F, HR, Q) existieren und sind exportiert"
    - "TypeScript-Konstanten fuer status_enum, automation_level_enum, executor_enum und severity_enum existieren"
    - "Alle Enum-Konstanten sind typisiert und ohne Compilerfehler importierbar"
  artifacts:
    - path: "src/lib/constants/framework.ts"
      provides: "Alle gemeinsamen Enum-Konstanten fuer das xKMU Framework"
      exports:
        - FRAMEWORK_CATEGORIES
        - STATUS_ENUM
        - AUTOMATION_LEVEL_ENUM
        - EXECUTOR_ENUM
        - SEVERITY_ENUM
        - FrameworkCategory
        - StatusEnum
        - AutomationLevelEnum
        - ExecutorEnum
        - SeverityEnum
  key_links:
    - from: "src/lib/constants/framework.ts"
      to: "src/lib/db/schema.ts"
      via: "Enum-Werte werden als varchar-Constraints referenziert"
    - from: "src/lib/constants/framework.ts"
      to: "src/lib/services/*.service.ts"
      via: "Services importieren Enum-Typen fuer Typsicherheit"
---

<objective>
Shared TypeScript-Konstanten und abgeleitete Typen fuer alle Framework-Enums anlegen.

Purpose: Alle nachfolgenden Phasen (Services, UI, Seeds) muessen konsistente, typsichere Enum-Werte verwenden. Die Konstanten in dieser Datei sind die einzige Quelle der Wahrheit — kein Magic-String-Antipattern.

Output: `src/lib/constants/framework.ts` mit exportierten Konstanten und abgeleiteten TypeScript-Typen fuer Kategorien, Status, Automation-Level, Executor und Severity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Enums aus temp/xKMU_AI_Business_Framework_v2.json shared-Sektion -->

shared.status_enum:        ["draft", "review", "approved", "archived"]
shared.automation_level_enum: ["manual", "semi", "full"]
shared.severity_enum:      ["LOW", "MEDIUM", "HIGH", "CRITICAL"]
shared.executor_enum:      ["agent", "human", "flex"]

shared.categories:
  { code: "V",  label: "Vertrieb" }
  { code: "M",  label: "Marketing" }
  { code: "IT", label: "IT & Cybersicherheit" }
  { code: "P",  label: "Projektmanagement" }
  { code: "C",  label: "Compliance & DSGVO" }
  { code: "F",  label: "Finanzen" }
  { code: "HR", label: "Human Resources" }
  { code: "Q",  label: "Qualitaetssicherung" }

<!-- Coding Conventions (aus CONVENTIONS.md) -->
- camelCase fuer Variablen/Funktionen
- UPPERCASE_SNAKE_CASE fuer Modul-Level-Konstanten
- single quotes, 2-Leerzeichen-Einrueckung
- Exports: named exports (kein default export fuer Utility-Module)
- Pfad-Alias: @/* zeigt auf ./src/*
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Datei src/lib/constants/framework.ts anlegen</name>
  <files>src/lib/constants/framework.ts</files>
  <action>
Neue Datei `src/lib/constants/framework.ts` mit folgendem Inhalt erstellen.

WICHTIG: Dies ist eine reine Konstanten-Datei — kein Import von Drizzle, kein Import von schema.ts. Nur TypeScript-Werte und abgeleitete `typeof`-Typen.

Exakter Inhalt der Datei:

```typescript
// ============================================
// xKMU Framework v2 — Shared Enums & Konstanten
// Quelle: xKMU_AI_Business_Framework_v2.json (shared-Sektion)
// Diese Datei ist die einzige Quelle der Wahrheit fuer alle Enum-Werte.
// ============================================

// ─── Kategorien ───────────────────────────────────────────────────────────────

export const FRAMEWORK_CATEGORIES = [
  { code: 'V',  label: 'Vertrieb' },
  { code: 'M',  label: 'Marketing' },
  { code: 'IT', label: 'IT & Cybersicherheit' },
  { code: 'P',  label: 'Projektmanagement' },
  { code: 'C',  label: 'Compliance & DSGVO' },
  { code: 'F',  label: 'Finanzen' },
  { code: 'HR', label: 'Human Resources' },
  { code: 'Q',  label: 'Qualitaetssicherung' },
] as const

export type FrameworkCategoryCode = typeof FRAMEWORK_CATEGORIES[number]['code']
// => 'V' | 'M' | 'IT' | 'P' | 'C' | 'F' | 'HR' | 'Q'

export type FrameworkCategory = typeof FRAMEWORK_CATEGORIES[number]

// ─── Status Enum ──────────────────────────────────────────────────────────────

export const STATUS_ENUM = ['draft', 'review', 'approved', 'archived'] as const

export type StatusEnum = typeof STATUS_ENUM[number]
// => 'draft' | 'review' | 'approved' | 'archived'

// ─── Automation Level Enum ────────────────────────────────────────────────────

export const AUTOMATION_LEVEL_ENUM = ['manual', 'semi', 'full'] as const

export type AutomationLevelEnum = typeof AUTOMATION_LEVEL_ENUM[number]
// => 'manual' | 'semi' | 'full'

// ─── Executor Enum ────────────────────────────────────────────────────────────

export const EXECUTOR_ENUM = ['agent', 'human', 'flex'] as const

export type ExecutorEnum = typeof EXECUTOR_ENUM[number]
// => 'agent' | 'human' | 'flex'

// ─── Severity Enum ────────────────────────────────────────────────────────────

export const SEVERITY_ENUM = ['LOW', 'MEDIUM', 'HIGH', 'CRITICAL'] as const

export type SeverityEnum = typeof SEVERITY_ENUM[number]
// => 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL'

// ─── Execution Log — Entity Types ─────────────────────────────────────────────

export const ENTITY_TYPE_ENUM = ['sop', 'deliverable'] as const

export type EntityTypeEnum = typeof ENTITY_TYPE_ENUM[number]
// => 'sop' | 'deliverable'

// ─── Execution Log — Executed By ──────────────────────────────────────────────

export const EXECUTED_BY_ENUM = ['agent', 'human'] as const

export type ExecutedByEnum = typeof EXECUTED_BY_ENUM[number]

// ─── Execution Log — Status ───────────────────────────────────────────────────

export const EXECUTION_STATUS_ENUM = ['completed', 'aborted', 'escalated'] as const

export type ExecutionStatusEnum = typeof EXECUTION_STATUS_ENUM[number]

// ─── Hilfsfunktion: Kategorie-Label nachschlagen ──────────────────────────────

export function getCategoryLabel(code: FrameworkCategoryCode): string {
  return FRAMEWORK_CATEGORIES.find((c) => c.code === code)?.label ?? code
}
```

Sicherstellen: Die Datei liegt unter `src/lib/constants/framework.ts`. Kein Barrel-Index (`index.ts`) noetig — direkter Import per `@/lib/constants/framework`.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep -i "framework.ts" || echo "NO_ERRORS"</automated>
  </verify>
  <done>
    - Datei existiert unter src/lib/constants/framework.ts
    - TypeScript-Compiler meldet keine Fehler fuer diese Datei
    - Alle 9 named exports sind vorhanden: FRAMEWORK_CATEGORIES, STATUS_ENUM, AUTOMATION_LEVEL_ENUM, EXECUTOR_ENUM, SEVERITY_ENUM, ENTITY_TYPE_ENUM, EXECUTED_BY_ENUM, EXECUTION_STATUS_ENUM, getCategoryLabel
    - Abgeleitete Typen: FrameworkCategoryCode, FrameworkCategory, StatusEnum, AutomationLevelEnum, ExecutorEnum, SeverityEnum, EntityTypeEnum, ExecutedByEnum, ExecutionStatusEnum
  </done>
</task>

</tasks>

<verification>
Nach Ausfuehrung von Task 1:

1. Datei existiert: `test -f src/lib/constants/framework.ts`
2. TypeScript kompiliert sauber: `npx tsc --noEmit` gibt keine Fehler fuer die neue Datei
3. Alle Konstanten erreichbar — Test-Import:
   ```bash
   node -e "const f = require('./src/lib/constants/framework.ts'); console.log(f.FRAMEWORK_CATEGORIES.length)" 2>/dev/null || echo "use tsx: npx tsx -e \"import { FRAMEWORK_CATEGORIES } from './src/lib/constants/framework.ts'; console.log(FRAMEWORK_CATEGORIES.length)\""
   ```
   Erwartet: `8` (acht Kategorien)
4. Manuell pruefen: grep-Check ob alle Exports vorhanden sind:
   ```bash
   grep -c "^export" src/lib/constants/framework.ts
   ```
   Erwartet: >= 13 Zeilen mit `export`
</verification>

<success_criteria>
- src/lib/constants/framework.ts existiert und ist TypeScript-valide
- FRAMEWORK_CATEGORIES enthaelt genau 8 Eintraege (V, M, IT, P, C, F, HR, Q)
- STATUS_ENUM hat 4 Werte, AUTOMATION_LEVEL_ENUM 3, EXECUTOR_ENUM 3, SEVERITY_ENUM 4
- Abgeleitete Union-Typen sind korrekt (z.B. AutomationLevelEnum = 'manual' | 'semi' | 'full')
- Keine anderen Dateien wurden veraendert
- Keine Imports von drizzle-orm oder schema.ts in dieser Datei
</success_criteria>

<output>
Nach Abschluss `.planning/phases/01-db-schema-migrations/01-01-SUMMARY.md` erstellen mit:
- Was wurde erstellt (Datei, Exports)
- Enum-Werte (fuer Referenz in Plan 01-02 und spaetere Phasen)
- TypeScript-Typen (fuer Import-Referenz in Phase 2/3/4)
- Eventuell aufgetretene Probleme und Loesungen
</output>
