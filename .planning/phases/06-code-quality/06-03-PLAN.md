---
phase: 06-code-quality
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/app/intern/(dashboard)/cockpit/page.tsx
  - src/app/intern/(dashboard)/cockpit/_components/cockpit-stats-cards.tsx
  - src/app/intern/(dashboard)/cockpit/_components/cockpit-systems-table.tsx
  - src/app/intern/(dashboard)/cockpit/_components/cockpit-system-dialog.tsx
  - src/app/intern/(dashboard)/prozesse/dev/page.tsx
  - src/app/intern/(dashboard)/prozesse/dev/_components/dev-task-filters.tsx
  - src/app/intern/(dashboard)/prozesse/dev/_components/dev-task-card.tsx
  - src/app/intern/(dashboard)/prozesse/dev/_components/dev-analysis-dialog.tsx
  - src/app/intern/(dashboard)/cybersecurity/grundschutz/assets/[id]/page.tsx
  - src/app/intern/(dashboard)/cybersecurity/grundschutz/assets/[id]/_components/asset-stammdaten-tab.tsx
  - src/app/intern/(dashboard)/cybersecurity/grundschutz/assets/[id]/_components/asset-beziehungen-tab.tsx
  - src/app/intern/(dashboard)/cybersecurity/grundschutz/assets/[id]/_components/asset-controls-tab.tsx
  - src/app/intern/(dashboard)/cybersecurity/grundschutz/assets/[id]/_components/asset-control-dialog.tsx
  - src/app/intern/(dashboard)/chancen/page.tsx
  - src/app/intern/(dashboard)/chancen/_components/chancen-dialog.tsx
  - src/app/intern/(dashboard)/chancen/_components/chancen-table.tsx
  - src/app/intern/(dashboard)/catalog/_components/product-form.tsx
  - src/app/intern/(dashboard)/catalog/_components/product-basic-fields.tsx
  - src/app/intern/(dashboard)/catalog/_components/product-pricing-fields.tsx
  - src/app/intern/(dashboard)/catalog/_components/product-description-fields.tsx
  - src/components/shared/ai-research-card.tsx
  - src/components/shared/ai-research/addresses-display.tsx
  - src/components/shared/ai-research/research-result-display.tsx
  - src/components/shared/ai-research/crawl-results-display.tsx
  - src/components/shared/ai-research/proposed-changes-panel.tsx
  - src/app/intern/(dashboard)/cms/[id]/blocks/[blockId]/_components/block-field-renderer.tsx
  - src/app/intern/(dashboard)/cms/[id]/blocks/[blockId]/_components/text-blocks-fields.tsx
  - src/app/intern/(dashboard)/cms/[id]/blocks/[blockId]/_components/media-blocks-fields.tsx
  - src/app/intern/(dashboard)/cms/[id]/blocks/[blockId]/_components/list-blocks-fields.tsx
  - src/app/intern/(dashboard)/cms/[id]/blocks/[blockId]/_components/complex-blocks-fields.tsx
autonomous: true
requirements:
  - R4.3

must_haves:
  truths:
    - "No component file in src/app/ or src/components/ exceeds 400 lines after splitting"
    - "`npx next build` succeeds with zero errors after all splits"
    - "All 7 large components are split and functional (no broken imports)"
    - "Sub-components use named exports (export function ComponentName)"
    - "Sub-components live in _components/ directories"
  artifacts:
    - path: "src/app/intern/(dashboard)/cockpit/_components/cockpit-system-dialog.tsx"
      provides: "Extracted dialog (~400 lines)"
      contains: "export function CockpitSystemDialog"
    - path: "src/app/intern/(dashboard)/cockpit/page.tsx"
      provides: "Orchestration-only cockpit page"
      max_lines: 400
    - path: "src/components/shared/ai-research/proposed-changes-panel.tsx"
      provides: "Extracted ProposedChangesPanel component"
      contains: "export function ProposedChangesPanel"
    - path: "src/app/intern/(dashboard)/cms/[id]/blocks/[blockId]/_components/block-field-renderer.tsx"
      provides: "Thin dispatcher"
      max_lines: 100
  key_links:
    - from: "src/app/intern/(dashboard)/cockpit/page.tsx"
      to: "cockpit/_components/cockpit-system-dialog.tsx"
      via: "import { CockpitSystemDialog } from './_components/cockpit-system-dialog'"
      pattern: "import.*CockpitSystemDialog"
    - from: "src/app/intern/(dashboard)/cms/[id]/blocks/[blockId]/_components/block-field-renderer.tsx"
      to: "text-blocks-fields.tsx etc."
      via: "import { TextBlocksFields } from './text-blocks-fields'"
      pattern: "import.*BlocksFields.*from"
---

<objective>
Split all 7 components exceeding 400 lines into sub-components with clear responsibility boundaries.

Purpose: 760-1158 line components are slow to re-render, hard to test, and create long code review diffs. Each split follows the same pattern: state owned by parent stays in parent, UI sections with their own local state become sub-components.

Output: All 7 large files reduced to <= 400 lines. Sub-components created in _components/ directories. Build passes.

Wave 2 dependency: This plan runs AFTER 06-01 (block-field-renderer already has typed arrays — splitting must preserve those types).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-code-quality/06-RESEARCH.md

<interfaces>
<!-- Component splitting rules from CONVENTIONS.md and RESEARCH.md -->

Named export pattern (always use this):
```typescript
'use client'  // only if component uses hooks/state
export function CockpitSystemDialog({ ... }: CockpitSystemDialogProps) { ... }
```

Props interface in same file as component:
```typescript
interface CockpitSystemDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  system: SystemRecord | null
  onSave: (data: SystemFormData) => Promise<void>
}
```

Import with @/ alias:
```typescript
import { CockpitSystemDialog } from '@/app/intern/(dashboard)/cockpit/_components/cockpit-system-dialog'
// OR relative (both work, prefer relative for same-directory):
import { CockpitSystemDialog } from './_components/cockpit-system-dialog'
```

Split boundaries (from RESEARCH.md — use these, do not invent new ones):

cockpit/page.tsx (1158 → ~4 files):
- CockpitStatsCards   → cockpit/_components/cockpit-stats-cards.tsx     (~60 lines)
- CockpitSystemsTable → cockpit/_components/cockpit-systems-table.tsx   (~130 lines)
- CockpitSystemDialog → cockpit/_components/cockpit-system-dialog.tsx   (~400 lines)
- page.tsx stays ~200 lines (state, handlers, header, orchestration)

prozesse/dev/page.tsx (983 → ~4 files):
- DevTaskFilters    → prozesse/dev/_components/dev-task-filters.tsx    (~65 lines)
- DevTaskCard       → prozesse/dev/_components/dev-task-card.tsx       (~200 lines)
- DevAnalysisDialog → prozesse/dev/_components/dev-analysis-dialog.tsx (~80 lines)
- page.tsx stays ~400 lines

grundschutz/assets/[id]/page.tsx (883 → ~5 files):
- AssetStammdatenTab  → .../[id]/_components/asset-stammdaten-tab.tsx  (~140 lines)
- AssetBeziehungenTab → .../[id]/_components/asset-beziehungen-tab.tsx (~75 lines)
- AssetControlsTab    → .../[id]/_components/asset-controls-tab.tsx    (~70 lines)
- AssetControlDialog  → .../[id]/_components/asset-control-dialog.tsx  (~120 lines)
- page.tsx stays ~300 lines

chancen/page.tsx (763 → ~3 files):
- ChancenDialog → chancen/_components/chancen-dialog.tsx (~200 lines)
- ChancenTable  → chancen/_components/chancen-table.tsx  (~150 lines)
- page.tsx stays ~250 lines

catalog/product-form.tsx (760 → ~4 files):
- ProductBasicFields       → catalog/_components/product-basic-fields.tsx       (~100 lines)
- ProductPricingFields     → catalog/_components/product-pricing-fields.tsx     (~120 lines)
- ProductDescriptionFields → catalog/_components/product-description-fields.tsx (~100 lines)
- product-form.tsx stays ~300 lines

ai-research-card.tsx (1047 → ~5 files):
- AddressesDisplay    → shared/ai-research/addresses-display.tsx    (~35 lines)
- ResearchResultDisplay → shared/ai-research/research-result-display.tsx (~130 lines)
- CrawlResultsDisplay → shared/ai-research/crawl-results-display.tsx (~60 lines, includes CrawlPageItem)
- ProposedChangesPanel → shared/ai-research/proposed-changes-panel.tsx (~130 lines)
- ai-research-card.tsx stays ~600 lines (still large but internal functions already separated)

block-field-renderer.tsx (878 → ~5 files):
IMPORTANT: This file was modified by plan 06-01 (as any fixes). Read the current state.
- text-blocks-fields.tsx    → hero, heading, text, cta cases (~150 lines)
- media-blocks-fields.tsx   → image, video, gallery, logo-cloud, banner, divider cases (~130 lines)
- list-blocks-fields.tsx    → features, cards, testimonials, faq, stats, team, timeline, service-cards (~250 lines)
- complex-blocks-fields.tsx → pricing, comparison, blog-listing cases (~130 lines)
- block-field-renderer.tsx  → thin dispatcher ~80 lines

Each group file receives the `content`, `updateContent` props and renders a group of cases.
The thin dispatcher passes through props to the correct group based on block type.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Split cockpit, prozesse/dev, grundschutz/assets, and chancen page components</name>
  <files>
    src/app/intern/(dashboard)/cockpit/page.tsx,
    src/app/intern/(dashboard)/cockpit/_components/cockpit-stats-cards.tsx,
    src/app/intern/(dashboard)/cockpit/_components/cockpit-systems-table.tsx,
    src/app/intern/(dashboard)/cockpit/_components/cockpit-system-dialog.tsx,
    src/app/intern/(dashboard)/prozesse/dev/page.tsx,
    src/app/intern/(dashboard)/prozesse/dev/_components/dev-task-filters.tsx,
    src/app/intern/(dashboard)/prozesse/dev/_components/dev-task-card.tsx,
    src/app/intern/(dashboard)/prozesse/dev/_components/dev-analysis-dialog.tsx,
    src/app/intern/(dashboard)/cybersecurity/grundschutz/assets/[id]/page.tsx,
    src/app/intern/(dashboard)/cybersecurity/grundschutz/assets/[id]/_components/asset-stammdaten-tab.tsx,
    src/app/intern/(dashboard)/cybersecurity/grundschutz/assets/[id]/_components/asset-beziehungen-tab.tsx,
    src/app/intern/(dashboard)/cybersecurity/grundschutz/assets/[id]/_components/asset-controls-tab.tsx,
    src/app/intern/(dashboard)/cybersecurity/grundschutz/assets/[id]/_components/asset-control-dialog.tsx,
    src/app/intern/(dashboard)/chancen/page.tsx,
    src/app/intern/(dashboard)/chancen/_components/chancen-dialog.tsx,
    src/app/intern/(dashboard)/chancen/_components/chancen-table.tsx
  </files>
  <action>
    For each component, follow this procedure:
    1. Read the original page.tsx fully.
    2. Identify state variables used only in one section — move those down to the sub-component.
    3. Identify state variables used across sections — keep those in parent, pass as props.
    4. Create _components/ directory (it may already exist).
    5. Create each sub-component file with the extracted JSX section.
    6. Define Props interface in the sub-component file.
    7. Replace the extracted JSX in page.tsx with the sub-component element.
    8. Add imports in page.tsx.

    Anti-patterns to avoid (from RESEARCH.md):
    - Do not pass more than 8 props to a sub-component — if needed, consider a local context
    - Do not move shared state (used by 2+ siblings) into a sub-component
    - Do not extract inline lambdas — only extract components with clear state/render scope

    cockpit/page.tsx:
    - Extract the {/* Stats Cards */} section → CockpitStatsCards
      Props needed: the 4 stat values (counts/totals shown in cards)
    - Extract the {/* Systems Table */} section → CockpitSystemsTable
      Props needed: systems array, onEdit handler, onDelete handler, loading state
    - Extract the {/* Create/Edit Dialog */} section → CockpitSystemDialog
      Props needed: open, onOpenChange, system (null for create), onSave, onDelete
      This is the largest section — preserve all form fields and credential sub-dialog logic
    - page.tsx retains: state declarations, fetchData, handleSave, handleDelete, header JSX

    prozesse/dev/page.tsx:
    - Extract filters bar → DevTaskFilters
      Props needed: filter state values + their setters
    - Extract per-task card rendering → DevTaskCard
      Props needed: task object, onAnalyze handler, selected state
    - Extract AI analysis dialog → DevAnalysisDialog
      Props needed: open, onOpenChange, analysisResult, loading
    - page.tsx retains: data fetch, state, generate handlers, stats section, header

    grundschutz/assets/[id]/page.tsx:
    - Extract Tab 1 content → AssetStammdatenTab
      Props needed: asset object, onUpdate handler, loading
    - Extract Tab 2 content → AssetBeziehungenTab
      Props needed: asset object, related items, handlers
    - Extract Tab 3 table → AssetControlsTab
      Props needed: controls array, onAdd, onEdit, onDelete handlers
    - Extract control add/edit dialog → AssetControlDialog
      Props needed: open, onOpenChange, control (null for add), onSave
    - page.tsx retains: tab state, fetch handlers, header, tab switcher

    chancen/page.tsx:
    - Extract create/edit dialog → ChancenDialog
      Props needed: open, onOpenChange, chancen (null for create), onSave
    - Extract table section → ChancenTable
      Props needed: chancen array, onEdit, onDelete, loading, filters state
    - page.tsx retains: state, fetch, handlers, header, filter controls

    After creating all sub-components, run:
    ```bash
    npx tsc --noEmit
    ```
    Fix any type errors (missing imports, wrong prop types, undefined variables).

    Then verify line counts:
    ```bash
    wc -l \
      src/app/intern/(dashboard)/cockpit/page.tsx \
      src/app/intern/(dashboard)/prozesse/dev/page.tsx \
      "src/app/intern/(dashboard)/cybersecurity/grundschutz/assets/[id]/page.tsx" \
      src/app/intern/(dashboard)/chancen/page.tsx
    ```
    All must be <= 400 lines.
  </action>
  <verify>
    <automated>wc -l src/app/intern/\(dashboard\)/cockpit/page.tsx src/app/intern/\(dashboard\)/prozesse/dev/page.tsx "src/app/intern/(dashboard)/cybersecurity/grundschutz/assets/[id]/page.tsx" src/app/intern/\(dashboard\)/chancen/page.tsx | awk 'NR<5 && $1>400{print "FAIL: "$2" has "$1" lines"} END{print "done"}'</automated>
  </verify>
  <done>
    - cockpit/page.tsx: <= 400 lines, 3 sub-components created and imported
    - prozesse/dev/page.tsx: <= 400 lines, 3 sub-components created and imported
    - grundschutz/assets/[id]/page.tsx: <= 400 lines, 4 sub-components created and imported
    - chancen/page.tsx: <= 400 lines, 2 sub-components created and imported
    - `npx tsc --noEmit` exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Split product-form, ai-research-card, and block-field-renderer; verify final build</name>
  <files>
    src/app/intern/(dashboard)/catalog/_components/product-form.tsx,
    src/app/intern/(dashboard)/catalog/_components/product-basic-fields.tsx,
    src/app/intern/(dashboard)/catalog/_components/product-pricing-fields.tsx,
    src/app/intern/(dashboard)/catalog/_components/product-description-fields.tsx,
    src/components/shared/ai-research-card.tsx,
    src/components/shared/ai-research/addresses-display.tsx,
    src/components/shared/ai-research/research-result-display.tsx,
    src/components/shared/ai-research/crawl-results-display.tsx,
    src/components/shared/ai-research/proposed-changes-panel.tsx,
    src/app/intern/(dashboard)/cms/[id]/blocks/[blockId]/_components/block-field-renderer.tsx,
    src/app/intern/(dashboard)/cms/[id]/blocks/[blockId]/_components/text-blocks-fields.tsx,
    src/app/intern/(dashboard)/cms/[id]/blocks/[blockId]/_components/media-blocks-fields.tsx,
    src/app/intern/(dashboard)/cms/[id]/blocks/[blockId]/_components/list-blocks-fields.tsx,
    src/app/intern/(dashboard)/cms/[id]/blocks/[blockId]/_components/complex-blocks-fields.tsx
  </files>
  <action>
    --- catalog/product-form.tsx ---
    Read the file first. It is a single form with field sections.
    Extract field groups as "dumb" presentational sub-components that receive:
    - form: UseFormReturn (from react-hook-form) — pass the form object down so sub-components
      can call form.register, form.formState, etc. without prop-drilling individual values
    - Any field-group-specific props (e.g., category list for ProductBasicFields)

    ProductBasicFields receives: { form, categories }
    ProductPricingFields receives: { form }
    ProductDescriptionFields receives: { form }

    product-form.tsx keeps: form initialization, submit handler, form shell (<form>), submit button.

    PITFALL: If react-hook-form's UseFormReturn type is complex, use the form's actual type:
    `import type { UseFormReturn } from 'react-hook-form'`
    `interface ProductBasicFieldsProps { form: UseFormReturn<ProductFormValues> }`
    Where ProductFormValues is the Zod-inferred type already used in product-form.tsx.

    --- components/shared/ai-research-card.tsx ---
    Read the file first. It defines helper functions/components as named functions inside the file.
    The split is mechanical: move each named function to its own file in shared/ai-research/.

    Create src/components/shared/ai-research/ directory.
    Move:
    - function AddressesDisplay → addresses-display.tsx (export it)
    - function ResearchResultDisplay → research-result-display.tsx (export it)
    - function CrawlPageItem + function CrawlResultsDisplay → crawl-results-display.tsx
      (export CrawlResultsDisplay, keep CrawlPageItem as internal function)
    - function ProposedChangesPanel → proposed-changes-panel.tsx (export it)

    In ai-research-card.tsx, add imports for the 4 moved components and remove the function definitions.
    Preserve all other code. After this split, ai-research-card.tsx should be ~600 lines (still large
    but the internal functions were already logically separated).

    IMPORTANT: Check if ai-research-card.tsx is in src/components/shared/index.ts barrel export.
    If yes, do NOT add the new sub-components to index.ts — they are internal helpers.

    --- block-field-renderer.tsx ---
    IMPORTANT: Read the current file first — it was modified by plan 06-01 (as any fixes).
    The typed arrays (Record<string, string>[]) and BadgeContent interface from plan 06-01 must
    be preserved in the split files that use them.

    Create 4 group files. Each receives `content` and `updateContent` props:
    ```typescript
    interface BlockFieldsProps {
      content: Record<string, unknown>
      updateContent: (key: string, value: unknown) => void
      settings?: Record<string, unknown>
    }
    ```

    text-blocks-fields.tsx: cases 'hero', 'heading', 'text', 'cta'
    media-blocks-fields.tsx: cases 'image', 'video', 'gallery', 'logo-cloud', 'banner', 'divider'
    list-blocks-fields.tsx: cases 'features', 'cards', 'testimonials', 'faq', 'stats', 'team', 'timeline', 'service-cards'
    complex-blocks-fields.tsx: cases 'pricing', 'comparison', 'blog-listing'

    Each file exports a single function component with a switch statement for its group.
    block-field-renderer.tsx becomes a thin dispatcher:
    ```typescript
    switch(blockType) {
      case 'hero': case 'heading': case 'text': case 'cta':
        return <TextBlocksFields content={content} updateContent={updateContent} />
      case 'image': case 'video': case 'gallery': case 'logo-cloud': case 'banner': case 'divider':
        return <MediaBlocksFields content={content} updateContent={updateContent} />
      // etc.
    }
    ```

    Move the ArrayField, PricingPlanField, and any other helper components/functions to the group
    files that use them, OR keep them in block-field-renderer.tsx and import them into group files.
    ArrayField is used by multiple groups — keep it in block-field-renderer.tsx and export it
    so group files can import it:
    ```typescript
    // In block-field-renderer.tsx:
    export function ArrayField(...) { ... }
    // In list-blocks-fields.tsx:
    import { ArrayField } from './block-field-renderer'
    ```

    After all splits, run final build gate:
    ```bash
    npx tsc --noEmit
    npx next build
    ```

    Then run the full line count check for ALL 7 original files:
    ```bash
    find src/app -name "*.tsx" -exec wc -l {} \; | awk '$1 > 400 {print $0}' | sort -rn
    ```
    Expected: no files > 400 lines (or only sub-component files that are themselves <= 400 lines).
    Also check src/components/shared/ai-research-card.tsx (may still be ~600 lines — acceptable
    since research says "split further if needed" — but target is <= 400 for full compliance).
    If ai-research-card.tsx is still > 400 lines after moving the 4 helper components, further
    split: identify the next largest internal section (likely the main render JSX) and extract it.
  </action>
  <verify>
    <automated>find src/app -name "*.tsx" -exec wc -l {} \; | awk '$1 > 400 {print "OVER 400: "$2" ("$1" lines)"}' | sort -rn && find src/components -name "*.tsx" -exec wc -l {} \; | awk '$1 > 400 {print "OVER 400: "$2" ("$1" lines)"}' | sort -rn</automated>
  </verify>
  <done>
    - product-form.tsx: <= 400 lines, 3 field-group sub-components created
    - ai-research-card.tsx: <= 400 lines, 4 sub-components in shared/ai-research/
    - block-field-renderer.tsx: <= 100 lines (thin dispatcher), 4 group files created
    - All 7 original large files are now <= 400 lines
    - `npx tsc --noEmit` exits 0
    - `npx next build` succeeds
    - R4.3 acceptance criteria met: no component > 400 lines, build passes
  </done>
</task>

</tasks>

<verification>
```bash
# R4.3 gate — no component > 400 lines:
find src/app src/components -name "*.tsx" -exec wc -l {} \; | awk '$1 > 400 {print $0}' | sort -rn

# Build gate:
npx tsc --noEmit
npx next build
```
</verification>

<success_criteria>
- `find src/app -name "*.tsx" -exec wc -l {} \;` shows no file > 400 lines
- `find src/components -name "*.tsx" -exec wc -l {} \;` shows no file > 400 lines
- `npx tsc --noEmit` exits 0
- `npx next build` succeeds
- R4.3 is complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-code-quality/06-03-SUMMARY.md` with:
- Line counts before and after for each of the 7 original files
- New files created (names and line counts)
- Any deviations from the planned split boundaries
- Build status
</output>
