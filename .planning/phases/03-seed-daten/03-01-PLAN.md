---
phase: 03-seed-daten
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/seeds/deliverable-catalog.seed.ts
  - src/lib/db/seed-check.ts
autonomous: true
requirements:
  - SEED-01
  - SEED-02
  - SEED-03

must_haves:
  truths:
    - "GET /api/v1/deliverables/modules liefert genau 15 Module (A1-A5, B1-B5, C1-C3, D1-D3)"
    - "GET /api/v1/deliverables liefert genau 63 Deliverables mit korrekter Modul-Zuordnung"
    - "Seed kann mehrfach ausgefuehrt werden ohne Duplikate (idempotent by module code + deliverable name)"
  artifacts:
    - path: "src/lib/db/seeds/deliverable-catalog.seed.ts"
      provides: "Idempotenter Seed fuer 15 Module und 63 Deliverables"
      exports: ["seedDeliverableCatalog"]
    - path: "src/lib/db/seed-check.ts"
      provides: "Aufruf von seedDeliverableCatalog nach seedManagementFramework"
      contains: "seedDeliverableCatalog"
  key_links:
    - from: "src/lib/db/seeds/deliverable-catalog.seed.ts"
      to: "deliverableModules table"
      via: "db.insert(deliverableModules)"
      pattern: "db\\.insert\\(deliverableModules\\)"
    - from: "src/lib/db/seeds/deliverable-catalog.seed.ts"
      to: "DeliverableService.create"
      via: "DeliverableService.create(tenantId, ...)"
      pattern: "DeliverableService\\.create"
    - from: "src/lib/db/seed-check.ts"
      to: "seedDeliverableCatalog"
      via: "await seedDeliverableCatalog(tenantId)"
      pattern: "seedDeliverableCatalog"
---

<objective>
Seed alle 15 Deliverable-Module und 63 Deliverables idempotent aus xKMU_Deliverable_Katalog_v1 in die Datenbank.

Purpose: Deliverable-Stammdaten sind Voraussetzung fuer den SOP-Seed (Plan 03-02), da SOPs per produces_deliverable_id auf Deliverables verweisen.
Output: src/lib/db/seeds/deliverable-catalog.seed.ts + Integration in seed-check.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@src/lib/db/seeds/management-framework.seed.ts
@src/lib/db/seed-check.ts
@src/lib/services/deliverable.service.ts
@src/lib/db/schema.ts

<interfaces>
<!-- Relevante Typen und Exports fuer den Executor. -->

From src/lib/db/schema.ts (lines 3118-3166):
```typescript
// deliverableModules table columns:
// id: uuid (primaryKey, defaultRandom)
// tenantId: uuid (notNull, FK -> tenants)
// code: varchar(10) notNull          -- z.B. 'A1', 'B3'
// name: varchar(255) notNull         -- z.B. 'KI-Quick-Start & Potenzialanalyse'
// category: varchar(10) notNull      -- 'A' | 'B' | 'C' | 'D'
// categoryCode: varchar(10)          -- z.B. 'V', 'M', 'IT' (Framework-Kategorie, hier null)
// ziel: text                         -- Modulziel
// preis: varchar(100)                -- z.B. '490 EUR Festpreis'
// createdAt, updatedAt

// deliverables table columns:
// id: uuid (primaryKey, defaultRandom)
// tenantId: uuid (notNull, FK -> tenants)
// moduleId: uuid (FK -> deliverableModules, onDelete: set null)
// name: varchar(255) notNull
// description: text
// format: varchar(100)               -- z.B. 'PDF', 'Excel / Notion-Tabelle'
// umfang: varchar(100)               -- z.B. '1-2 Seiten'
// trigger: varchar(255)              -- Was loest Erstellung aus?
// category: varchar(50)             -- z.B. 'KI-Beratung'
// categoryCode: varchar(10)         -- z.B. 'A', 'B', 'C', 'D'
// status: varchar(20) default 'draft'
// version: varchar(20) default '1.0.0'
```

From src/lib/services/deliverable.service.ts:
```typescript
export const DeliverableService = {
  async create(tenantId: string, data: Record<string, unknown>): Promise<Deliverable>
  // data fields: name, description?, format?, umfang?, trigger?,
  //              category?, categoryCode?, moduleId?, status?, version?
  
  async list(tenantId: string, filters?: { moduleId?, categoryCode?, status? }): Promise<Deliverable[]>
  // Use for idempotency check: check if deliverables with moduleId exist before inserting
}
```

From src/lib/db/seed-check.ts (relevant call site, line 868):
```typescript
// 14. Seed Management Framework
await seedManagementFramework(tenantId)
// ADD after line 868:
// 15. Seed Deliverable Catalog
await seedDeliverableCatalog(tenantId)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deliverable-Catalog-Seed-Datei erstellen (15 Module + 63 Deliverables)</name>
  <files>src/lib/db/seeds/deliverable-catalog.seed.ts</files>
  <action>
Erstelle die Datei `src/lib/db/seeds/deliverable-catalog.seed.ts` mit der exportierten Funktion `seedDeliverableCatalog(tenantId: string)`.

**Imports:**
```typescript
import { db } from '@/lib/db'
import { deliverableModules } from '@/lib/db/schema'
import { eq, and } from 'drizzle-orm'
import { DeliverableService } from '@/lib/services/deliverable.service'
import { logger } from '@/lib/utils/logger'
```

**Idempotenz-Strategie:**
- Fuer Module: `db.select().from(deliverableModules).where(and(eq(deliverableModules.tenantId, tenantId), eq(deliverableModules.code, m.code)))` — wenn gefunden, skip.
- Fuer Deliverables: `DeliverableService.list(tenantId, { moduleId: module.id })` — wenn Liste nicht leer, skip fuer dieses Modul.

**Datenstruktur im Seed-File:** Definiere ein Array `MODULES` mit je `{ code, name, category, ziel, preis, deliverables: [...] }`.

**JSON-Feld-Mapping (aus xKMU_Deliverable_Katalog_v1 JSON -> DB-Spalten):**

| JSON-Feld (module)  | DB-Spalte (deliverableModules) |
|---------------------|-------------------------------|
| `id`                | `code`                        |
| `name`              | `name`                        |
| `category_code`     | `category`                    |
| `ziel`              | `ziel`                        |
| `preis`             | `preis`                       |
| (keine)             | `categoryCode` = null         |

| JSON-Feld (deliverable) | DB-Spalte (deliverables)   |
|-------------------------|---------------------------|
| `name`                  | `name`                    |
| `description`           | `description`             |
| `format`                | `format`                  |
| `umfang`                | `umfang`                  |
| `trigger`               | `trigger`                 |
| (modul.category)        | `category`                |
| (modul.category_code)   | `categoryCode`            |
| (modul.id -> db uuid)   | `moduleId`                |
| (fest)                  | `status` = 'active'       |
| (fest)                  | `version` = '1.0.0'       |

**Alle 15 Module mit ihren Deliverables** (vollstaendig aus xKMU_Deliverable_Katalog_v1 JSON extrahieren):

```
Kategorie A — KI-Beratung (category_code: 'A'):
  A1: KI-Quick-Start & Potenzialanalyse — 7 Deliverables (A1-D01 bis A1-D07)
  A2: KI-Implementierung - Automationen & Workflows — 7 Deliverables (A2-D01 bis A2-D07)
  A3: KI-Assistenten & Chatbots — 5 Deliverables (A3-D01 bis A3-D05)
  A4: Prompting, Templates & Governance — 4 Deliverables (A4-D01 bis A4-D04)
  A5: KI-Schulungen & Enablement — 4 Deliverables (A5-D01 bis A5-D04)

Kategorie B — IT-Beratung (category_code: 'B'):
  B1: IT-Assessment & Stabilitaetscheck — 4 Deliverables (B1-D01 bis B1-D04)
  B2: IT-Architektur & Modernisierung — 4 Deliverables (B2-D01 bis B2-D04)
  B3: Systemintegration & Prozess-IT — 4 Deliverables (B3-D01 bis B3-D04)
  B4: Betrieb, Monitoring & Dokumentation — 5 Deliverables (B4-D01 bis B4-D05)
  B5: IT-Standardisierung & Arbeitsplatz-IT — 4 Deliverables (B5-D01 bis B5-D04)

Kategorie C — Cybersecurity (category_code: 'C'):
  C1: Security Quick Check — lese aus JSON
  C2: Grundschutz & Compliance — lese aus JSON
  C3: Datenschutz & DSGVO — lese aus JSON

Kategorie D — Management & Prozesse (category_code: 'D'):
  D1: ... lese aus JSON
  D2: ... lese aus JSON
  D3: ... lese aus JSON
```

WICHTIG: Fuer C1-C3 und D1-D3 muessen die Deliverables aus dem JSON-File gelesen werden. Das JSON liegt unter `temp/xKMU_Deliverable_Katalog_v1 (1).json`. Lese das File und extrahiere ALLE Deliverables vollstaendig — die obige Liste fuer A und B ist eine Orientierung, aber C und D muessen vollstaendig aus dem JSON gelesen werden. Gesamtzahl muss 63 ergeben.

**Seed-Logik (Pseudocode):**
```typescript
export async function seedDeliverableCatalog(tenantId: string) {
  logger.info('Seeding Deliverable Catalog...', { module: 'DeliverableSeed' })
  let moduleCount = 0, deliverableCount = 0

  for (const m of MODULES) {
    // 1. Module idempotent anlegen
    const [existing] = await db.select().from(deliverableModules)
      .where(and(eq(deliverableModules.tenantId, tenantId), eq(deliverableModules.code, m.code)))
    
    let moduleId: string
    if (existing) {
      moduleId = existing.id
      logger.info(`Module ${m.code} already exists, skipping`, { module: 'DeliverableSeed' })
    } else {
      const [created] = await db.insert(deliverableModules).values({
        tenantId,
        code: m.code,
        name: m.name,
        category: m.categoryCode,   // 'A' | 'B' | 'C' | 'D'
        ziel: m.ziel,
        preis: m.preis,
      }).returning()
      moduleId = created.id
      moduleCount++
    }

    // 2. Deliverables fuer dieses Modul idempotent anlegen
    const existingDels = await DeliverableService.list(tenantId, { moduleId })
    if (existingDels.length > 0) {
      logger.info(`Deliverables fuer ${m.code} existieren bereits, skip`, { module: 'DeliverableSeed' })
      continue
    }

    for (const d of m.deliverables) {
      await DeliverableService.create(tenantId, {
        moduleId,
        name: d.name,
        description: d.description,
        format: d.format,
        umfang: d.umfang,
        trigger: d.trigger,
        category: m.category,       // z.B. 'KI-Beratung'
        categoryCode: m.categoryCode, // 'A' | 'B' | 'C' | 'D'
        status: 'active',
        version: '1.0.0',
      })
      deliverableCount++
    }
  }

  logger.info(`Deliverable Catalog seeded: ${moduleCount} Module, ${deliverableCount} Deliverables`, { module: 'DeliverableSeed' })
}
```

Folge exakt dem Muster aus `management-framework.seed.ts`: logger.info fuer jeden Schritt, async/await, keine raw SQL.
  </action>
  <verify>
    <automated>cd C:/Daten/xkmu-business-os && npx tsc --noEmit 2>&1 | grep "deliverable-catalog"</automated>
  </verify>
  <done>
    - Datei existiert unter src/lib/db/seeds/deliverable-catalog.seed.ts
    - Funktion seedDeliverableCatalog ist exportiert
    - MODULES-Array enthaelt alle 15 Module (A1-A5, B1-B5, C1-C3, D1-D3)
    - Gesamtzahl der Deliverables im Array: genau 63
    - TypeScript kompiliert ohne Fehler (npx tsc --noEmit)
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed-Check um Deliverable-Catalog-Seed erweitern</name>
  <files>src/lib/db/seed-check.ts</files>
  <action>
Ergaenze `src/lib/db/seed-check.ts` an zwei Stellen:

**1. Import hinzufuegen** (nach Zeile 11, neben seedManagementFramework):
```typescript
import { seedDeliverableCatalog } from './seeds/deliverable-catalog.seed'
```

**2. Aufruf hinzufuegen** (nach Zeile 868, nach `await seedManagementFramework(tenantId)`):
```typescript
// 15. Seed Deliverable Catalog (15 Module + 63 Deliverables)
await seedDeliverableCatalog(tenantId)
```

WICHTIG: Der Aufruf muss NACH `seedManagementFramework` und VOR `logger.info('Seed check completed!')` stehen. Die Zeilennummer des Kommentars danach anpassen: aus `logger.info('Seed check completed!')` wird nichts geaendert, nur der neue Aufruf dazwischen eingefuegt.

Keine weiteren Aenderungen an seed-check.ts.
  </action>
  <verify>
    <automated>cd C:/Daten/xkmu-business-os && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Import von seedDeliverableCatalog ist in seed-check.ts vorhanden
    - Aufruf await seedDeliverableCatalog(tenantId) steht nach seedManagementFramework
    - npx tsc --noEmit laeuft ohne Fehler fuer diese Datei
  </done>
</task>

</tasks>

<verification>
Nach Ausfuehrung beider Tasks:

```bash
# TypeScript kompiliert sauber
npx tsc --noEmit

# Seed-Datei existiert
ls -la src/lib/db/seeds/deliverable-catalog.seed.ts

# Export vorhanden
grep -n "export async function seedDeliverableCatalog" src/lib/db/seeds/deliverable-catalog.seed.ts

# Import in seed-check vorhanden
grep -n "seedDeliverableCatalog" src/lib/db/seed-check.ts

# Anzahl Module im MODULES-Array = 15
grep -c '"code"' src/lib/db/seeds/deliverable-catalog.seed.ts  # muss 15 ergeben

# Anzahl Deliverable-Eintraege im Array
# Zaehle alle { name: '-D  ' Eintraege: muss 63 ergeben
```

Manueller Smoke-Test nach DB-Seed (optional, erfordert laufende DB):
```bash
curl -s http://localhost:3000/api/v1/deliverables/modules | jq '.data | length'
# Erwartetes Ergebnis: 15

curl -s http://localhost:3000/api/v1/deliverables | jq '.data | length'
# Erwartetes Ergebnis: 63
```
</verification>

<success_criteria>
- src/lib/db/seeds/deliverable-catalog.seed.ts existiert und exportiert seedDeliverableCatalog
- MODULES-Konstante enthaelt exakt 15 Module (A1-A5, B1-B5, C1-C3, D1-D3) mit je allen Deliverables (Summe: 63)
- Idempotenz implementiert: Modul-Check by (tenantId, code), Deliverable-Check by moduleId (skip if any exist)
- seed-check.ts importiert und ruft seedDeliverableCatalog(tenantId) auf
- npx tsc --noEmit laeuft fehlerfrei
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/03-seed-daten/03-01-SUMMARY.md` mit:
- Erstellte Dateien
- Anzahl Module und Deliverables im Seed
- Wichtige Implementierungsdetails (Idempotenz-Strategie, Feld-Mappings)
- Etwaige Abweichungen vom Plan
</output>
